---
phase: 03-agent-migration-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/smoke-test-agents.js
  - .claude/agents/strategy.md
  - .claude/agent-memory/strategy/MEMORY.md
  - .claude/agents/style-extractor.md
  - .claude/agent-memory/style-extractor/MEMORY.md
  - .claude/agents/editorial-lead.md
  - .claude/agent-memory/editorial-lead/MEMORY.md
autonomous: true
requirements: [AGNT-02, AGNT-05, AGNT-06, AGNT-15, MEMO-01, MEMO-02, MEMO-03, MEMO-04]
must_haves:
  truths:
    - "Smoke test script validates all 12 agents will have correct structure when complete"
    - "Strategy agent presents as unified strategy expert combining competitive intelligence and topic generation"
    - "Style-extractor agent extracts channel voice from reference scripts"
    - "Editorial-lead agent gates research quality without producing artifacts"
    - "All three agents have persistent memory seeded with V5 domain knowledge"
  artifacts:
    - path: "tests/smoke-test-agents.js"
      provides: "Comprehensive agent validation for all Phase 3 requirements"
      contains: "testCases"
    - path: ".claude/agents/strategy.md"
      provides: "Strategy agent definition with consolidated persona"
      contains: "memory: project"
    - path: ".claude/agents/style-extractor.md"
      provides: "Style extractor agent definition"
      contains: "memory: project"
    - path: ".claude/agents/editorial-lead.md"
      provides: "Editorial lead agent definition (quality gating only)"
      contains: "memory: project"
    - path: ".claude/agent-memory/strategy/MEMORY.md"
      provides: "Strategy memory seeded from V5 strategy-lead + market-analyst"
      contains: "## Key Files"
    - path: ".claude/agent-memory/style-extractor/MEMORY.md"
      provides: "Style extractor memory seeded from V5"
      contains: "## Key Files"
    - path: ".claude/agent-memory/editorial-lead/MEMORY.md"
      provides: "Editorial lead memory seeded from V5"
      contains: "## Key Files"
  key_links:
    - from: ".claude/agents/strategy.md"
      to: ".claude/agent-memory/strategy/MEMORY.md"
      via: "memory: project frontmatter"
      pattern: "memory: project"
    - from: ".claude/agents/strategy.md"
      to: ".claude/skills/agent-protocols/SKILL.md"
      via: "skills: frontmatter"
      pattern: "agent-protocols"
---

<objective>
Create the Phase 3 smoke test scaffold and the Strategy/Editorial domain agents (strategy, style-extractor, editorial-lead) with seeded MEMORY.md files.

Purpose: Establishes the test infrastructure for the entire phase and delivers the first wave of agents covering strategy analysis, voice extraction, and editorial quality gating.
Output: 1 test file, 3 agent definitions, 3 MEMORY.md files
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-migration-memory/03-CONTEXT.md
@.planning/phases/03-agent-migration-memory/03-RESEARCH.md

<interfaces>
<!-- Established agent definition pattern from Phase 1 -->

From .claude/agents/researcher.md (frontmatter pattern):
```yaml
---
name: researcher
description: >-
  Multi-line description of agent purpose.
model: sonnet
memory: project
color: blue
skills:
  - agent-protocols
---
```

From .claude/agents/researcher.md (body pattern):
```markdown
<project_context>
Read ./CLAUDE.md for project-wide rules, platform constraints, and agent reference table.
</project_context>

# Agent Display Name

## Identity
[Rich persona paragraph]

## Channel Context
@channel/channel.md

## [Domain-Specific Procedures]

## Python Scripts Available

## File Conventions

## Task Classification
```

From .claude/agent-memory/researcher/MEMORY.md (memory structure):
```markdown
# Agent Name Memory

## Key Files
- item: path/to/file

## Decisions
(none yet)

## Patterns
(none yet)

## Observations
(none yet)

## Open Questions
(none yet)
```

From .claude/skills/agent-protocols/SKILL.md:
- Memory lifecycle: read MEMORY.md at start, append after work
- Feedback signals: read/write SIGNALS.md
- Project context: read CLAUDE.md at start

From tests/smoke-test-skills.js (test pattern):
- Uses `const testCases = []` array pattern
- Each test: `{ name, check: () => boolean }`
- Reports PASS/FAIL per case, exits 0/1
- Uses `path.resolve(__dirname, '..')` for project root

From .planning/config.json (skill mappings):
- strategy: [agent-protocols, data-analysis, structured-output]
- style-extractor: [agent-protocols] (new, discretionary)
- editorial-lead: [agent-protocols, documentary-research] (new, discretionary)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke-test-agents.js test scaffold</name>
  <files>tests/smoke-test-agents.js</files>
  <read_first>
    - tests/smoke-test-skills.js (pattern reference for testCases array structure)
    - tests/smoke-test-paths.js (pattern reference for project root resolution)
    - .claude/agents/researcher.md (example of valid agent frontmatter to validate against)
  </read_first>
  <action>
Create `tests/smoke-test-agents.js` following the same `testCases` array pattern used in `smoke-test-skills.js`.

The test must validate ALL 12 agents listed in CONTEXT.md D-01:
```
const agents = [
  'researcher', 'writer', 'strategy', 'style-extractor', 'editorial-lead',
  'visual-researcher', 'visual-planner', 'asset-processor', 'asset-curator',
  'meta', 'code-reviewer', 'compiler'
];
```

Per-agent checks (12 agents x N checks):
1. Agent file exists at `.claude/agents/{name}.md`
2. Frontmatter contains `name: {name}`
3. Frontmatter contains `model: sonnet`
4. Frontmatter contains `memory: project`
5. Frontmatter contains `tools:` field (AGNT-15 tool scoping)
6. Frontmatter `skills:` contains `agent-protocols` (MEMO-03)
7. Agent body contains `<project_context>` block
8. Agent body contains `## Identity` section
9. MEMORY.md exists at `.claude/agent-memory/{name}/MEMORY.md`
10. MEMORY.md contains all 5 sections: `## Key Files`, `## Decisions`, `## Patterns`, `## Observations`, `## Open Questions` (MEMO-02)
11. MEMORY.md `## Key Files` section has at least one entry (not just header + "(none yet)") (MEMO-04 -- seeded, not empty)

Global checks:
12. No agent file contains `.pi/multi-team/` (V5 path leak detection)
13. No agent file contains `{{SESSION_DIR}}` or `{{CONVERSATION_LOG}}` or `{{EXPERTISE_BLOCK}}` (V5 template variable leak)
14. No agent file contains the word `delegate` in a delegation context (check for `delegate to` or `Delegate` -- but allow the word in other contexts like "the user may delegate")
15. CLAUDE.md agent reference table has no "(Phase 3)" markers remaining

Use `fs.readFileSync` and `path.join` for all file reads. Use `const projectRoot = path.resolve(__dirname, '..')` for the root. Exit 0 if all pass, exit 1 otherwise. Print `PASS`/`FAIL` per test case and summary count at end.

Parse frontmatter by splitting on `---` delimiters and checking content between them. For MEMORY.md seeded check, verify `## Key Files` section has content after the header that is NOT just `(none yet)`.
  </action>
  <verify>
    <automated>node tests/smoke-test-agents.js 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/smoke-test-agents.js exists and is valid JavaScript
    - File contains `const agents = [` with all 12 agent names
    - File contains `const testCases = []`
    - File contains checks for `memory: project`, `model: sonnet`, `tools:`, `agent-protocols`
    - File contains MEMORY.md existence check and 5-section structure check
    - File contains V5 leak detection (`.pi/multi-team/`, `{{SESSION_DIR}}`)
    - File contains CLAUDE.md "(Phase 3)" marker check
    - Running `node tests/smoke-test-agents.js` produces FAIL output (agents don't exist yet) but does not crash with syntax errors
  </acceptance_criteria>
  <done>Smoke test script exists, runs without syntax errors, validates all 12 agents against AGNT and MEMO requirements. Currently fails (expected -- agents not yet created).</done>
</task>

<task type="auto">
  <name>Task 2: Create strategy agent definition and seeded MEMORY.md</name>
  <files>.claude/agents/strategy.md, .claude/agent-memory/strategy/MEMORY.md</files>
  <read_first>
    - .claude/agents/researcher.md (established agent definition pattern -- replicate structure exactly)
    - .claude/agents/writer.md (second pattern reference)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/strategy-lead.md (V5 source -- primary for procedures)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/market-analyst.md (V5 source -- merge into strategy)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/topic-generation.md (seeding source for MEMORY.md)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/trends-analysis.md (seeding source for MEMORY.md)
    - .claude/skills/agent-protocols/SKILL.md (shared protocol reference)
  </read_first>
  <action>
**Create `.claude/agents/strategy.md`** -- consolidated from V5 strategy-lead + market-analyst per D-01, D-07, D-08.

Frontmatter:
```yaml
---
name: strategy
description: >-
  Performs competitor analysis, trend detection, topic generation, and project
  initialization for the documentary channel. Runs Python scraping and analysis
  scripts, produces scored topic briefs, and scaffolds new project directories.
  Invoke when the user needs competitive intelligence or topic recommendations.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
memory: project
color: yellow
skills:
  - agent-protocols
  - data-analysis
  - structured-output
---
```

Body structure (adapt from V5, follow researcher.md pattern):
1. `<project_context>` block: `Read ./CLAUDE.md for project-wide rules, platform constraints, and agent reference table.`
2. `# Strategy Expert`
3. `## Identity` -- Rich persona paragraph per D-06. Unified expert who does BOTH competitive intelligence AND statistical analysis (per D-08 -- no internal domain splits). Explicit guardrails: does NOT conduct documentary research, does NOT write scripts, does NOT handle visual assets. Domain is market position, topic selection, and project setup.
4. `## Channel Context` -- `@channel/channel.md` only (strategy doesn't need voice profile)
5. `## Competitor Analysis` -- Adapt from V5 strategy-lead: channel discovery, scraping pipeline, analysis dimensions. REMOVE all delegation language ("delegate to market-analyst"). The strategy agent runs scripts ITSELF.
6. `## Topic Generation` -- Adapt from V5 strategy-lead + market-analyst: scoring rubric (obscurity, complexity, shock factor, verifiability, pillar fit), candidate generation, ranking logic, near-duplicate handling.
7. `## Project Initialization` -- Adapt from V5 strategy-lead: project directory scaffolding, initial metadata files, topic brief output.
8. `## Python Scripts Available` -- List strategy scripts: `strategy/cli.py` with subcommands (add, scrape, analyze, topics, init). Include note: "may not yet be fully connected in V0.6". Path pattern: `strategy/cli.py <command>`.
9. `## File Conventions` -- Strategy output paths: `strategy/competitors/`, `strategy/topic_briefs.md`, `projects/<name>/`. Data: `data/channel_assistant.db`.
10. `## Task Classification` -- DETERMINISTIC (scraping, data extraction, file scaffolding) vs HEURISTIC (topic scoring rationale, trend interpretation, narrative potential assessment).

CRITICAL adaptation rules per D-10:
- REMOVE: `{{SESSION_DIR}}`, `{{CONVERSATION_LOG}}`, `{{EXPERTISE_BLOCK}}`, `{{SKILLS_BLOCK}}`
- REMOVE: `.pi/` paths -- replace with V0.6 paths (`strategy/`, `data/`)
- REMOVE: `delegate`, `report to`, `hand off to` language
- REMOVE: V5-only skills (active-listener, conversational-response, zero-micro-management, precise-worker, verification-first)
- REMOVE: domain: YAML block
- KEEP: All domain knowledge about scoring rubrics, analysis dimensions, competitor patterns
- RESOLVE conflicts: If strategy-lead says "never run scripts" but market-analyst says "run all scripts yourself", resolve in favor of the agent doing the work directly (per D-08)

Target: 120-200 lines (adjust based on content needs per discretion).

**Create `.claude/agent-memory/strategy/MEMORY.md`** -- seeded per D-09, D-10.

Seed from V5 agent body content AND read-only expertise (topic-generation.md, trends-analysis.md). NOT from empty expertise YAMLs.

Structure:
```markdown
# Strategy Memory

## Key Files
- Competitor database: data/channel_assistant.db (SQLite)
- Strategy CLI: strategy/cli.py (add, scrape, analyze, topics, init)
- Topic briefs output: strategy/topic_briefs.md
- Channel DNA: channel/channel.md
- Past topics: channel/past_topics.md (if exists)
- Analysis output: strategy/competitors/

## Decisions
- [2026-04-10] Topics scored across 5 dimensions: obscurity, complexity, shock factor, verifiability, pillar fit
- [2026-04-10] Topic briefs produce exactly 5 candidates ranked by total score descending
- [2026-04-10] Near-duplicates tagged and included, never silently dropped
[Add 2-3 more decisions extracted from V5 strategy-lead + market-analyst bodies]

## Patterns
- [2026-04-10] Underserved topic clusters from competitor analysis are highest-value generation targets
- [2026-04-10] Cross-product entity queries (person + institution) surface less-discovered topics
- [2026-04-10] Channels with < 1K subscribers AND AI content signals indicate content farm saturation
[Add 2-3 more patterns from V5 read-only expertise files]

## Observations
(none yet)

## Open Questions
(none yet)
```

Ensure Key Files section references ACTUAL V0.6 paths, not V5 paths.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const a=fs.readFileSync(p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6/.claude/agents/strategy.md'),'utf8');const m=fs.readFileSync(p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6/.claude/agent-memory/strategy/MEMORY.md'),'utf8');console.log('agent:',a.includes('memory: project')&&a.includes('agent-protocols')&&a.includes('tools:')&&a.includes('## Identity')&&!a.includes('.pi/multi-team'));console.log('memory:',m.includes('## Key Files')&&m.includes('## Decisions')&&m.includes('## Patterns')&&!m.includes('(none yet)'))"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/strategy.md exists with frontmatter containing `name: strategy`, `model: sonnet`, `memory: project`, `color: yellow`, `tools: Read, Write, Edit, Bash, Grep, Glob`
    - strategy.md frontmatter `skills:` includes `agent-protocols`, `data-analysis`, `structured-output`
    - strategy.md body contains `<project_context>`, `## Identity`, `## Channel Context`, `@channel/channel.md`
    - strategy.md body does NOT contain `.pi/multi-team/`, `{{SESSION_DIR}}`, `{{CONVERSATION_LOG}}`, `delegate to`, `report to`
    - strategy.md body contains `## Task Classification` with `[DETERMINISTIC]` and `[HEURISTIC]`
    - .claude/agent-memory/strategy/MEMORY.md exists with all 5 sections
    - MEMORY.md `## Key Files` lists at least 3 file paths (not "(none yet)")
    - MEMORY.md `## Decisions` has at least 3 timestamped entries
    - MEMORY.md `## Patterns` has at least 3 timestamped entries
    - MEMORY.md does NOT contain `.pi/` paths
  </acceptance_criteria>
  <done>Strategy agent definition exists as unified expert combining strategy-lead + market-analyst, with seeded MEMORY.md containing domain knowledge from V5 sources. No V5 artifacts leak through.</done>
</task>

<task type="auto">
  <name>Task 3: Create style-extractor and editorial-lead agent definitions with seeded MEMORY.md files</name>
  <files>.claude/agents/style-extractor.md, .claude/agent-memory/style-extractor/MEMORY.md, .claude/agents/editorial-lead.md, .claude/agent-memory/editorial-lead/MEMORY.md</files>
  <read_first>
    - .claude/agents/researcher.md (established pattern reference)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/style-extractor.md (V5 source)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/extraction.md (V5 read-only expertise for style-extractor MEMORY.md)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/editorial-lead.md (V5 source)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/survey-evaluation.md (V5 read-only expertise -- relevant to editorial quality)
    - .claude/skills/agent-protocols/SKILL.md (shared protocol)
    - channel/voice-profile.md (style-extractor needs voice profile awareness)
  </read_first>
  <action>
**Create `.claude/agents/style-extractor.md`** -- from V5 style-extractor per D-07.

Frontmatter:
```yaml
---
name: style-extractor
description: >-
  Extracts and codifies the channel's narrative voice from reference scripts.
  Analyzes existing documentary scripts to identify tone, pacing, vocabulary,
  and structural patterns. Produces voice profile documents. Invoke when the
  user wants to update or refine the channel's voice profile.
tools: Read, Write, Edit, Grep, Glob
model: sonnet
memory: project
color: pink
skills:
  - agent-protocols
---
```

No Bash tool -- style-extractor reads scripts and writes profiles, no Python scripts to run.

Body structure:
1. `<project_context>` block
2. `# Style Extractor`
3. `## Identity` -- Rich persona per D-06. Extracts narrator voice from reference scripts. Analyzes linguistic patterns, sentence structure, vocabulary choices, pacing strategies. Does NOT write scripts. Does NOT conduct research. Does NOT make editorial decisions. Domain is voice analysis and codification.
4. `## Channel Context` -- `@channel/channel.md` AND `@channel/voice-profile.md` (style-extractor is deeply concerned with voice)
5. `## Voice Extraction Procedure` -- Adapt from V5: script corpus analysis, pattern identification, voice dimension mapping, profile document generation. Include auto-caption detection signals from V5 read-only extraction.md.
6. `## Voice Dimensions` -- The dimensions to extract: tone register, sentence length distribution, vocabulary preferences, attribution style, emotional restraint patterns, pacing structure.
7. `## Output Format` -- Voice profile document structure (what the extracted profile looks like).
8. `## File Conventions` -- Reads from: `projects/*/script/Script.md`, `channel/voice-profile.md`. Writes to: `channel/voice-profile.md` (updates), `channel/voice-analysis/`.
9. `## Task Classification` -- DETERMINISTIC (word frequency, sentence length stats) vs HEURISTIC (tone identification, pattern naming, style codification).

Strip all V5 artifacts per D-10. Remove delegation language. Remove Pi-specific skills.

**Create `.claude/agent-memory/style-extractor/MEMORY.md`**

Seed from V5 style-extractor agent body + extraction.md read-only expertise:
```markdown
# Style Extractor Memory

## Key Files
- Voice profile: channel/voice-profile.md
- Channel DNA: channel/channel.md
- Reference scripts: projects/*/script/Script.md
- Visual style guide: channel/VISUAL_STYLE_GUIDE.md

## Decisions
- [2026-04-10] Auto-generated captions detected by: repetitive filler phrases, missing punctuation, run-on sentences, phonetic misspellings
- [2026-04-10] Voice extraction requires minimum 2 reference scripts for reliable pattern identification
[Add 1-2 more from V5 source]

## Patterns
- [2026-04-10] Channel voice is clinical/neutral/deadpan -- gravity comes from the story, not from performance
- [2026-04-10] Short declarative sentences (under 10 words) serve as structural breathing points after heavy information
[Add 1-2 more from V5 extraction.md]

## Observations
(none yet)

## Open Questions
(none yet)
```

---

**Create `.claude/agents/editorial-lead.md`** -- from V5 editorial-lead, RESTRICTED to quality gating per D-03.

Frontmatter:
```yaml
---
name: editorial-lead
description: >-
  Reviews research dossiers and scripts for quality, accuracy, and editorial
  standards. Gates content quality before downstream pipeline stages. Provides
  structured feedback. Invoke when research or scripts need quality review
  before proceeding.
tools: Read, Grep, Glob
model: sonnet
memory: project
color: red
skills:
  - agent-protocols
  - documentary-research
---
```

CRITICAL: tools are READ-ONLY per research recommendation (D-03 quality gating role). No Write, Edit, or Bash. Editorial-lead reviews and provides verbal feedback to the user, does not produce artifacts.

Body structure:
1. `<project_context>` block
2. `# Editorial Lead`
3. `## Identity` -- Rich persona per D-06. Quality gatekeeper for research and scripts. Reviews for factual accuracy, source quality, narrative coherence, voice consistency. Does NOT write scripts. Does NOT conduct research. Does NOT coordinate pipeline stages (coordination moves to Phase 4 pipeline skills per D-03). Does NOT produce documents -- provides verbal feedback and structured assessments.
4. `## Channel Context` -- `@channel/channel.md` AND `@channel/voice-profile.md` (reviews scripts for voice consistency)
5. `## Research Quality Review` -- Adapt from V5 editorial-lead: source verification, claim-source linkage, entity index completeness, timeline consistency, open question assessment.
6. `## Script Quality Review` -- Voice profile compliance, fact-checking against dossier, narrative structure evaluation, act balance, hook effectiveness.
7. `## Quality Dimensions` -- The rubric: source quality, factual accuracy, narrative coherence, voice consistency, completeness, entity coverage.
8. `## Feedback Format` -- How editorial-lead structures its verbal feedback: overall assessment, dimension scores, specific issues with line references, recommended actions.
9. `## File Conventions` -- Reads from: `projects/*/research/Research.md`, `projects/*/script/Script.md`, `projects/*/research/entity_index.json`. Writes to: NOTHING (verbal feedback only).
10. `## Task Classification` -- DETERMINISTIC (source counting, claim-source linkage verification, entity completeness check) vs HEURISTIC (narrative quality assessment, voice consistency judgment, hook effectiveness).

Strip all V5 artifacts. Remove ALL delegation language (editorial-lead in V5 coordinated teams -- remove all of that). Remove pipeline coordination procedures (Phase 4).

**Create `.claude/agent-memory/editorial-lead/MEMORY.md`**

Seed from V5 editorial-lead agent body + survey-evaluation.md read-only expertise:
```markdown
# Editorial Lead Memory

## Key Files
- Research dossier: projects/*/research/Research.md
- Entity index: projects/*/research/entity_index.json
- Scripts: projects/*/script/Script.md
- Voice profile: channel/voice-profile.md
- Channel DNA: channel/channel.md

## Decisions
- [2026-04-10] Source hierarchy: court documents > government reports > contemporary news > books > retrospective articles > blogs > forums
- [2026-04-10] Every factual claim must trace to a cited source -- unsourced claims flagged as blockers
- [2026-04-10] Research quality gate: minimum 3 primary sources required before script generation proceeds
[Add 1-2 more from V5 editorial-lead body]

## Patterns
- [2026-04-10] Wikipedia-only sourcing indicates insufficient research depth -- always verify downstream references
- [2026-04-10] Timeline inconsistencies between sources should be presented side-by-side, not silently resolved
[Add 1-2 more from V5 source]

## Observations
(none yet)

## Open Questions
(none yet)
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve.bind(p,'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const se=fs.readFileSync(r('.claude/agents/style-extractor.md'),'utf8');const el=fs.readFileSync(r('.claude/agents/editorial-lead.md'),'utf8');const sem=fs.readFileSync(r('.claude/agent-memory/style-extractor/MEMORY.md'),'utf8');const elm=fs.readFileSync(r('.claude/agent-memory/editorial-lead/MEMORY.md'),'utf8');console.log('se-agent:',se.includes('memory: project')&&se.includes('agent-protocols')&&!se.includes('Bash')&&!se.includes('.pi/'));console.log('el-agent:',el.includes('memory: project')&&el.includes('tools: Read, Grep, Glob')&&!el.includes('.pi/'));console.log('se-mem:',sem.includes('## Key Files')&&!sem.includes('(none yet)'));console.log('el-mem:',elm.includes('## Key Files')&&!elm.includes('(none yet)'))"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/style-extractor.md exists with `name: style-extractor`, `model: sonnet`, `memory: project`, `color: pink`
    - style-extractor.md `tools:` field is `Read, Write, Edit, Grep, Glob` (no Bash)
    - style-extractor.md `skills:` includes `agent-protocols`
    - style-extractor.md body contains `## Identity`, `## Voice Extraction Procedure`, `## Task Classification`
    - .claude/agents/editorial-lead.md exists with `name: editorial-lead`, `model: sonnet`, `memory: project`, `color: red`
    - editorial-lead.md `tools:` field is `Read, Grep, Glob` (read-only -- no Write, Edit, Bash)
    - editorial-lead.md `skills:` includes `agent-protocols` and `documentary-research`
    - editorial-lead.md body contains `## Identity` with explicit "does NOT produce documents" guardrail
    - Both MEMORY.md files exist with all 5 sections
    - Both MEMORY.md `## Key Files` sections have real entries (not "(none yet)")
    - Neither agent file contains `.pi/multi-team/`, `{{SESSION_DIR}}`, `delegate to`
  </acceptance_criteria>
  <done>Style-extractor and editorial-lead agent definitions exist with rich personas, correct tool scoping, and MEMORY.md files seeded with V5 domain knowledge. Editorial-lead is correctly restricted to read-only tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent tool scoping | `tools:` field restricts agent capabilities -- editorial-lead limited to read-only |
| V5 content adaptation | V5 artifacts must not leak into V0.6 agent bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Agent tool scoping | mitigate | editorial-lead `tools: Read, Grep, Glob` enforced in frontmatter; smoke test validates tools: field exists for all agents |
| T-03-02 | Information Disclosure | MEMORY.md seeding | accept | MEMORY.md contains domain knowledge, not secrets; V5 paths stripped but content is operational knowledge |
| T-03-03 | Elevation of Privilege | V5 artifact leakage | mitigate | smoke-test-agents.js checks for `.pi/multi-team/`, `{{SESSION_DIR}}`, delegation language in all agent files |
</threat_model>

<verification>
Run smoke test (will partially pass after this plan):
```
node tests/smoke-test-agents.js
```
Expect: strategy, style-extractor, editorial-lead pass all checks. Other agents still FAIL (expected).
</verification>

<success_criteria>
- Smoke test script runs without syntax errors
- Strategy agent definition is a unified expert (no internal domain splits)
- Style-extractor has no Bash tool
- Editorial-lead has read-only tools (Read, Grep, Glob)
- All 3 MEMORY.md files seeded with real V5 domain knowledge (not empty placeholders)
- Zero V5 artifacts in any created file
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-migration-memory/03-01-SUMMARY.md`
</output>
