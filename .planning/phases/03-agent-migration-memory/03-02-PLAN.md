---
phase: 03-agent-migration-memory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/visual-researcher.md
  - .claude/agent-memory/visual-researcher/MEMORY.md
  - .claude/agents/visual-planner.md
  - .claude/agent-memory/visual-planner/MEMORY.md
  - .claude/agents/asset-processor.md
  - .claude/agent-memory/asset-processor/MEMORY.md
autonomous: true
requirements: [AGNT-07, AGNT-08, AGNT-09, AGNT-15, MEMO-01, MEMO-02, MEMO-03, MEMO-04]
must_haves:
  truths:
    - "Visual-researcher agent gathers visual intent, mood-to-visual mapping, and primary resources"
    - "Visual-planner agent generates shotlists and curates b-roll from archive sources"
    - "Asset-processor agent runs CLIP embeddings, downloads, and semantic search via GPU scripts"
    - "All three agents have persistent memory seeded with V5 operational knowledge"
    - "Each agent's tools field restricts it to its domain capabilities"
  artifacts:
    - path: ".claude/agents/visual-researcher.md"
      provides: "Visual researcher agent definition"
      contains: "memory: project"
    - path: ".claude/agents/visual-planner.md"
      provides: "Visual planner agent definition"
      contains: "memory: project"
    - path: ".claude/agents/asset-processor.md"
      provides: "Asset processor agent with GPU script access"
      contains: "memory: project"
    - path: ".claude/agent-memory/visual-researcher/MEMORY.md"
      provides: "Visual researcher memory seeded from V5"
      contains: "## Key Files"
    - path: ".claude/agent-memory/visual-planner/MEMORY.md"
      provides: "Visual planner memory seeded from V5"
      contains: "## Key Files"
    - path: ".claude/agent-memory/asset-processor/MEMORY.md"
      provides: "Asset processor memory with operational guides from V5"
      contains: "## Key Files"
  key_links:
    - from: ".claude/agents/asset-processor.md"
      to: "media/embed.py"
      via: "Bash tool invocation"
      pattern: "embed.py"
    - from: ".claude/agents/visual-planner.md"
      to: "media/ia_search.py"
      via: "Bash tool invocation"
      pattern: "ia_search.py"
---

<objective>
Create the Media domain agents (visual-researcher, visual-planner, asset-processor) with seeded MEMORY.md files from V5 expertise.

Purpose: Delivers the visual and asset processing pipeline agents that handle everything from visual intent definition through CLIP embedding and semantic search.
Output: 3 agent definitions, 3 MEMORY.md files
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-migration-memory/03-CONTEXT.md
@.planning/phases/03-agent-migration-memory/03-RESEARCH.md

<interfaces>
<!-- Established agent definition pattern -->

From .claude/agents/researcher.md (frontmatter pattern):
```yaml
---
name: researcher
description: >-
  Multi-line description.
model: sonnet
memory: project
color: blue
skills:
  - agent-protocols
---
```

From .claude/agents/researcher.md (body structure):
```
<project_context>
Read ./CLAUDE.md for project-wide rules, platform constraints, and agent reference table.
</project_context>
# Display Name
## Identity
## Channel Context
## [Domain Procedures]
## Python Scripts Available
## File Conventions
## Task Classification
```

From .planning/config.json (skill mappings):
- visual-researcher: [agent-protocols, visual-narrative, archive-search, crawl4ai-scraping]
- visual-planner: [agent-protocols, visual-narrative, archive-search, media-evaluation]
- asset-processor: [agent-protocols, media-evaluation]

From 03-RESEARCH.md (tool scoping):
- visual-researcher: Read, Write, Edit, Bash, Grep, Glob (Bash for crawl_images.py, wiki_screenshots.py)
- visual-planner: Read, Write, Edit, Bash, Grep, Glob (Bash for ia_search.py)
- asset-processor: Read, Write, Edit, Bash, Grep, Glob (Bash critical for embed.py, search.py, download.py)

From 03-RESEARCH.md (color assignments):
- visual-researcher: cyan
- visual-planner: purple
- asset-processor: orange

From 03-RESEARCH.md (V5 read-only expertise mapping):
- visual-researcher <- search-queries.md
- visual-planner <- youtube-evaluation.md
- asset-processor <- operational-guide.md, pe-core-usage.md, scoring-guide.md, known-issues.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visual-researcher and visual-planner agent definitions with seeded MEMORY.md files</name>
  <files>.claude/agents/visual-researcher.md, .claude/agent-memory/visual-researcher/MEMORY.md, .claude/agents/visual-planner.md, .claude/agent-memory/visual-planner/MEMORY.md</files>
  <read_first>
    - .claude/agents/researcher.md (established agent definition pattern)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/visual-researcher.md (V5 source for visual-researcher)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/visual-planner.md (V5 source for visual-planner)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/search-queries.md (V5 expertise for visual-researcher MEMORY.md)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/youtube-evaluation.md (V5 expertise for visual-planner MEMORY.md)
    - .claude/skills/visual-narrative/SKILL.md (domain skill reference)
    - .claude/skills/archive-search/SKILL.md (domain skill reference)
  </read_first>
  <action>
**Create `.claude/agents/visual-researcher.md`** -- adapted from V5 visual-researcher per D-07.

Frontmatter:
```yaml
---
name: visual-researcher
description: >-
  Defines visual intent for documentary segments, maps narrative moods to visual
  approaches, and gathers primary visual resources. Produces visual briefs and
  media leads lists. Invoke when a script is ready and visual planning needs
  to begin.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
memory: project
color: cyan
skills:
  - agent-protocols
  - visual-narrative
  - archive-search
  - crawl4ai-scraping
---
```

Body structure:
1. `<project_context>` block
2. `# Visual Researcher`
3. `## Identity` -- Rich persona per D-06. Translates documentary scripts into visual intent. Maps narrative moods to visual approaches (camera angles, lighting, color palettes). Gathers primary visual resources (archive footage leads, image candidates, location references). Does NOT generate shotlists (that's visual-planner). Does NOT download or process assets (that's asset-processor). Does NOT write scripts. Domain is visual interpretation and resource discovery.
4. `## Channel Context` -- `@channel/channel.md` AND `@channel/VISUAL_STYLE_GUIDE.md`
5. `## Visual Intent Definition` -- Adapt from V5: script-to-visual analysis, mood extraction per act/chapter, visual brief generation.
6. `## Resource Discovery` -- Adapt from V5: image search strategies, Wikipedia commons, Wikimedia, archive.org browsing, crawl4ai for visual sources. Include entity cross-product query patterns from V5 search-queries.md.
7. `## Visual Brief Format` -- Output structure: per-act visual intent, mood tags, suggested visual approaches, resource leads with URLs.
8. `## Python Scripts Available` -- `media/crawl_images.py`, `media/wiki_screenshots.py`. Note: "may not yet be fully connected in V0.6".
9. `## File Conventions` -- Reads from: `projects/<name>/script/Script.md`, `projects/<name>/research/entity_index.json`. Writes to: `projects/<name>/visuals/visual_brief.json`, `projects/<name>/visuals/media_leads.json`.
10. `## Task Classification` -- DETERMINISTIC (entity extraction from script, URL cataloging, file scaffolding) vs HEURISTIC (mood-to-visual mapping, visual intent definition, resource relevance judgment).

Strip all V5 artifacts per D-10.

**Create `.claude/agent-memory/visual-researcher/MEMORY.md`**

Seed from V5 visual-researcher body + search-queries.md:
```markdown
# Visual Researcher Memory

## Key Files
- Script input: projects/*/script/Script.md
- Entity index: projects/*/research/entity_index.json
- Visual brief output: projects/*/visuals/visual_brief.json
- Media leads output: projects/*/visuals/media_leads.json
- Visual style guide: channel/VISUAL_STYLE_GUIDE.md
- Image crawler: media/crawl_images.py
- Wiki screenshots: media/wiki_screenshots.py

## Decisions
- [2026-04-10] Visual intent defined per act/chapter, not per scene -- matches script structure
- [2026-04-10] Resource leads include URL, source type, estimated quality, and rights status
[Add 1-2 more from V5 body]

## Patterns
- [2026-04-10] Cross-product entity queries (person + location, event + organization) surface more specific visual results than single-entity searches
- [2026-04-10] Historical events before 1930 have better archive coverage at IA/Prelinger than YouTube
- [2026-04-10] Wikipedia article images are often the highest-quality freely available portraits for historical figures
[Add 1-2 more from search-queries.md]

## Observations
(none yet)

## Open Questions
(none yet)
```

---

**Create `.claude/agents/visual-planner.md`** -- adapted from V5 visual-planner per D-07.

Frontmatter:
```yaml
---
name: visual-planner
description: >-
  Generates shotlists from visual briefs, curates b-roll selections, and searches
  archive sources for footage. Produces structured shotlists with timing and
  asset requirements. Invoke when visual research is complete and a shotlist
  is needed.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
memory: project
color: purple
skills:
  - agent-protocols
  - visual-narrative
  - archive-search
  - media-evaluation
---
```

Body structure:
1. `<project_context>` block
2. `# Visual Planner`
3. `## Identity` -- Rich persona per D-06. Transforms visual briefs into actionable shotlists with specific asset requirements and timing cues. Curates b-roll selections from archive sources. Evaluates YouTube content for suitability. Does NOT define visual intent (that's visual-researcher). Does NOT download or embed assets (that's asset-processor). Does NOT write scripts. Domain is shot planning, b-roll curation, and archive footage sourcing.
4. `## Channel Context` -- `@channel/channel.md` AND `@channel/VISUAL_STYLE_GUIDE.md`
5. `## Shotlist Generation` -- Adapt from V5: visual-brief-to-shotlist conversion, shot type assignment, timing estimation, b-roll duration planning, transition type planning.
6. `## Archive Search` -- Adapt from V5: Internet Archive/Prelinger search, YouTube evaluation criteria. Include hard filters and AI content detection from V5 youtube-evaluation.md.
7. `## B-Roll Curation` -- Asset type selection, relevance scoring, rights assessment, quantity estimation per act.
8. `## Shotlist Format` -- Output structure: per-shot entries with shot type, duration, source, asset ID, visual notes.
9. `## Python Scripts Available` -- `media/ia_search.py`. Note: "may not yet be fully connected in V0.6".
10. `## File Conventions` -- Reads from: `projects/<name>/visuals/visual_brief.json`, `projects/<name>/visuals/media_leads.json`. Writes to: `projects/<name>/visuals/shotlist.json`.
11. `## Task Classification` -- DETERMINISTIC (shotlist formatting, timing calculation, asset cataloging) vs HEURISTIC (shot type selection, b-roll relevance judgment, YouTube content evaluation, archive source assessment).

Strip all V5 artifacts per D-10.

**Create `.claude/agent-memory/visual-planner/MEMORY.md`**

Seed from V5 visual-planner body + youtube-evaluation.md:
```markdown
# Visual Planner Memory

## Key Files
- Visual brief input: projects/*/visuals/visual_brief.json
- Media leads input: projects/*/visuals/media_leads.json
- Shotlist output: projects/*/visuals/shotlist.json
- Visual style guide: channel/VISUAL_STYLE_GUIDE.md
- IA search script: media/ia_search.py

## Decisions
- [2026-04-10] Shotlist entries include: shot_type, duration_seconds, source_url, asset_id, visual_notes, act_reference
- [2026-04-10] B-roll target: 2-4 candidate assets per shot to provide editor choice
[Add 1-2 more from V5 body]

## Patterns
- [2026-04-10] YouTube channels with <1K subscribers and AI-generated thumbnails are likely content farms -- hard filter
- [2026-04-10] Archive.org footage from Prelinger collection has most reliable public domain status
- [2026-04-10] Videos over 30 minutes are lower priority -- shorter clips (2-10 min) yield more usable b-roll segments
[Add 1-2 more from youtube-evaluation.md]

## Observations
(none yet)

## Open Questions
(none yet)
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve.bind(p,'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');['visual-researcher','visual-planner'].forEach(a=>{const ag=fs.readFileSync(r('.claude/agents/'+a+'.md'),'utf8');const mm=fs.readFileSync(r('.claude/agent-memory/'+a+'/MEMORY.md'),'utf8');console.log(a+'-agent:',ag.includes('memory: project')&&ag.includes('agent-protocols')&&ag.includes('tools:')&&!ag.includes('.pi/'));console.log(a+'-mem:',mm.includes('## Key Files')&&mm.includes('## Patterns')&&!mm.includes('(none yet)'))})"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/visual-researcher.md exists with `name: visual-researcher`, `model: sonnet`, `memory: project`, `color: cyan`
    - visual-researcher.md `tools:` is `Read, Write, Edit, Bash, Grep, Glob`
    - visual-researcher.md `skills:` includes `agent-protocols`, `visual-narrative`, `archive-search`, `crawl4ai-scraping`
    - visual-researcher.md body contains `## Identity` with explicit guardrails (does NOT generate shotlists)
    - .claude/agents/visual-planner.md exists with `name: visual-planner`, `model: sonnet`, `memory: project`, `color: purple`
    - visual-planner.md `tools:` is `Read, Write, Edit, Bash, Grep, Glob`
    - visual-planner.md `skills:` includes `agent-protocols`, `visual-narrative`, `archive-search`, `media-evaluation`
    - visual-planner.md body contains `## Identity` with explicit guardrails (does NOT define visual intent)
    - All 4 MEMORY.md files exist with 5 sections each
    - All MEMORY.md `## Key Files` have real entries
    - No file contains `.pi/multi-team/`, `{{SESSION_DIR}}`, `delegate to`
  </acceptance_criteria>
  <done>Visual-researcher and visual-planner agent definitions exist with correct tool and skill scoping, and MEMORY.md files seeded with V5 domain knowledge including search query patterns and YouTube evaluation criteria.</done>
</task>

<task type="auto">
  <name>Task 2: Create asset-processor agent definition with seeded MEMORY.md</name>
  <files>.claude/agents/asset-processor.md, .claude/agent-memory/asset-processor/MEMORY.md</files>
  <read_first>
    - .claude/agents/researcher.md (established pattern)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/asset-processor.md (V5 source)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/operational-guide.md (V5 expertise -- FFmpeg safety, embedding performance)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/pe-core-usage.md (V5 expertise -- model loading, VRAM budget)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/scoring-guide.md (V5 expertise -- cosine similarity ranges)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/known-issues.md (V5 expertise -- known failure modes)
    - .claude/skills/media-evaluation/SKILL.md (domain skill reference)
  </read_first>
  <action>
**Create `.claude/agents/asset-processor.md`** -- adapted from V5 asset-processor per D-07.

This is the most technically complex agent -- it invokes GPU scripts for CLIP embeddings and semantic search.

Frontmatter:
```yaml
---
name: asset-processor
description: >-
  Downloads media assets, generates CLIP embeddings, runs semantic search against
  the asset library, and scores asset relevance. Operates GPU-accelerated Python
  scripts for embedding and search operations. Invoke when visual planning is
  complete and assets need to be acquired and indexed.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
memory: project
color: orange
skills:
  - agent-protocols
  - media-evaluation
---
```

Body structure:
1. `<project_context>` block
2. `# Asset Processor`
3. `## Identity` -- Rich persona per D-06. Handles the technical media processing pipeline: downloading assets from URLs, generating CLIP vector embeddings, running semantic similarity search, scoring relevance. Operates GPU-accelerated Python scripts via the perception-models conda environment. Does NOT define visual intent (that's visual-researcher). Does NOT generate shotlists (that's visual-planner). Does NOT manage the global library (that's asset-curator). Does NOT write scripts. Domain is asset acquisition, embedding, search, and scoring.
4. `## Channel Context` -- `@channel/channel.md` only (asset processing is technical, not voice-dependent)
5. `## Asset Download` -- Adapt from V5: URL-based download, file naming conventions, format validation, directory organization.
6. `## CLIP Embedding Pipeline` -- Adapt from V5 + operational-guide.md + pe-core-usage.md: model loading (VRAM budget for RTX 4070 8GB), batch size constraints, embedding storage, `embed.py` usage. CRITICAL: Use conda env at `C:/Users/iorda/miniconda3/envs/perception-models/python.exe` for GPU scripts.
7. `## Semantic Search` -- Adapt from V5 + scoring-guide.md: query formulation, cosine similarity interpretation (ranges from scoring-guide), result ranking, `search.py` usage.
8. `## Relevance Scoring` -- Adapt from V5: multi-dimensional scoring (topical, temporal, visual quality), combined score matrix.
9. `## Known Issues` -- Adapt from V5 known-issues.md: known failure modes, pre-pipeline checks, VRAM management, FFmpeg safety rules.
10. `## Python Scripts Available` -- `media/embed.py` (CLIP embeddings), `media/search.py` (semantic search), `media/download.py` (asset download), `media/evaluate.py` (quality evaluation). Run with: `C:/Users/iorda/miniconda3/envs/perception-models/python.exe media/embed.py` for GPU scripts. Non-GPU scripts can use standard Python.
11. `## File Conventions` -- Reads from: `projects/<name>/visuals/shotlist.json`. Writes to: `projects/<name>/assets/`, `projects/<name>/assets/embeddings/`. Data: asset catalog at `data/asset_catalog.db`.
12. `## Task Classification` -- DETERMINISTIC (download, embed, file naming, directory creation) vs HEURISTIC (relevance scoring rationale, query refinement, result quality assessment).

Strip all V5 artifacts per D-10. Key adaptations:
- Replace `.pi/multi-team/scripts/media/` with `media/`
- Replace `{{SESSION_DIR}}` references with direct project paths
- Remove delegation to other agents
- Keep ALL GPU/VRAM operational knowledge from read-only expertise

**Create `.claude/agent-memory/asset-processor/MEMORY.md`**

This agent gets the RICHEST MEMORY.md -- it has 4 read-only expertise files to draw from.

Seed from V5 asset-processor body + operational-guide.md + pe-core-usage.md + scoring-guide.md + known-issues.md:
```markdown
# Asset Processor Memory

## Key Files
- Embedding script: media/embed.py (GPU -- use perception-models conda env)
- Search script: media/search.py (GPU -- use perception-models conda env)
- Download script: media/download.py
- Evaluate script: media/evaluate.py
- Shotlist input: projects/*/visuals/shotlist.json
- Asset output: projects/*/assets/
- Asset catalog: data/asset_catalog.db (SQLite)
- Conda env: C:/Users/iorda/miniconda3/envs/perception-models/python.exe

## Decisions
- [2026-04-10] GPU scripts MUST use perception-models conda env, never system Python
- [2026-04-10] CLIP model: ViT-B/32 default, ViT-L/14 for higher quality (2x VRAM)
- [2026-04-10] Batch size for embedding: max 32 images on 8GB VRAM with ViT-B/32
- [2026-04-10] Cosine similarity thresholds: >0.3 relevant, >0.25 marginal, <0.25 irrelevant
[Add 2-3 more from V5 operational guides]

## Patterns
- [2026-04-10] FFmpeg operations should always use -y flag but NEVER overwrite source files -- write to temp then rename
- [2026-04-10] VRAM exhaustion manifests as CUDA OOM -- reduce batch size or switch to CPU fallback for that batch
- [2026-04-10] Re-running embed.py on already-embedded assets is safe -- it skips existing embeddings by default
- [2026-04-10] Query refinement: if initial search returns <3 results above 0.25, broaden query terms before lowering threshold
[Add 2-3 more from V5 known-issues.md and scoring-guide.md]

## Observations
(none yet)

## Open Questions
(none yet)
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve.bind(p,'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const ag=fs.readFileSync(r('.claude/agents/asset-processor.md'),'utf8');const mm=fs.readFileSync(r('.claude/agent-memory/asset-processor/MEMORY.md'),'utf8');console.log('agent:',ag.includes('memory: project')&&ag.includes('agent-protocols')&&ag.includes('media-evaluation')&&ag.includes('perception-models')&&!ag.includes('.pi/'));console.log('memory:',mm.includes('## Key Files')&&mm.includes('perception-models')&&mm.includes('embed.py')&&!mm.includes('(none yet)'))"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/asset-processor.md exists with `name: asset-processor`, `model: sonnet`, `memory: project`, `color: orange`
    - asset-processor.md `tools:` is `Read, Write, Edit, Bash, Grep, Glob`
    - asset-processor.md `skills:` includes `agent-protocols` and `media-evaluation`
    - asset-processor.md body contains `perception-models` conda env path
    - asset-processor.md body contains `## CLIP Embedding Pipeline` and `## Semantic Search`
    - asset-processor.md body contains `## Known Issues` section with VRAM/FFmpeg safety
    - asset-processor.md does NOT contain `.pi/multi-team/`, `{{SESSION_DIR}}`, `delegate to`
    - .claude/agent-memory/asset-processor/MEMORY.md has all 5 sections
    - MEMORY.md `## Key Files` lists embed.py, search.py, download.py with correct paths
    - MEMORY.md `## Decisions` has at least 4 timestamped entries including VRAM/batch size info
    - MEMORY.md `## Patterns` has at least 4 timestamped entries including FFmpeg safety and CUDA OOM handling
  </acceptance_criteria>
  <done>Asset-processor agent definition exists with GPU script references, CLIP pipeline procedures, and MEMORY.md richly seeded from 4 V5 operational guides covering embedding, search, scoring, and known issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPU script invocation | Asset-processor invokes Python scripts with Bash -- scripts must use correct conda env |
| Asset download | Downloads from external URLs into project directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | GPU script path | mitigate | Agent body explicitly specifies `C:/Users/iorda/miniconda3/envs/perception-models/python.exe`; MEMORY.md records this as a decision |
| T-03-05 | Denial of Service | VRAM exhaustion | mitigate | Known Issues section documents batch size limits; MEMORY.md records CUDA OOM recovery pattern |
| T-03-06 | Tampering | Downloaded assets | accept | Asset downloads are from user-curated shotlist URLs; no arbitrary URL execution. Phase 4 hooks will add directory scoping |
</threat_model>

<verification>
After plan completion, verify agents pass the relevant checks in the smoke test:
```
node tests/smoke-test-agents.js 2>&1 | grep -E "(visual-researcher|visual-planner|asset-processor)"
```
</verification>

<success_criteria>
- All 3 agent definitions follow established pattern (frontmatter + body structure)
- Asset-processor references correct GPU conda environment path
- All 3 MEMORY.md files have rich seeded content from V5 expertise files
- Zero V5 artifacts in any created file
- Tool scoping correct: all 3 agents have Read, Write, Edit, Bash, Grep, Glob
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-migration-memory/03-02-SUMMARY.md`
</output>
