---
phase: 03-agent-migration-memory
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/asset-curator.md
  - .claude/agent-memory/asset-curator/MEMORY.md
  - .claude/agents/meta.md
  - .claude/agent-memory/meta/MEMORY.md
  - .claude/agents/code-reviewer.md
  - .claude/agent-memory/code-reviewer/MEMORY.md
  - .claude/agents/compiler.md
  - .claude/agent-memory/compiler/MEMORY.md
autonomous: true
requirements: [AGNT-10, AGNT-11, AGNT-12, AGNT-15, MEMO-01, MEMO-02, MEMO-03, MEMO-04]
must_haves:
  truths:
    - "Asset-curator agent manages global video library and handles cross-project deduplication"
    - "Meta agent combines pipeline observation, UX improvement, and improvement proposals as a unified expert"
    - "Code-reviewer agent is a separate focused agent for code quality review"
    - "Compiler agent produces DaVinci Resolve edit sheets from processed assets"
    - "All four agents have persistent memory seeded with V5 domain knowledge"
  artifacts:
    - path: ".claude/agents/asset-curator.md"
      provides: "Asset curator agent for library management"
      contains: "memory: project"
    - path: ".claude/agents/meta.md"
      provides: "Meta agent consolidated from 3 V5 agents"
      contains: "memory: project"
    - path: ".claude/agents/code-reviewer.md"
      provides: "Code reviewer agent (standalone per D-04)"
      contains: "memory: project"
    - path: ".claude/agents/compiler.md"
      provides: "Compiler agent for edit sheet generation"
      contains: "memory: project"
    - path: ".claude/agent-memory/asset-curator/MEMORY.md"
      provides: "Asset curator memory"
      contains: "## Key Files"
    - path: ".claude/agent-memory/meta/MEMORY.md"
      provides: "Meta memory consolidated from 3 V5 sources"
      contains: "## Key Files"
    - path: ".claude/agent-memory/code-reviewer/MEMORY.md"
      provides: "Code reviewer memory"
      contains: "## Key Files"
    - path: ".claude/agent-memory/compiler/MEMORY.md"
      provides: "Compiler memory"
      contains: "## Key Files"
  key_links:
    - from: ".claude/agents/meta.md"
      to: ".claude/agents/"
      via: "reads other agent definitions for pipeline health"
      pattern: ".claude/agents/"
    - from: ".claude/agents/compiler.md"
      to: "media/organize_assets.py"
      via: "Bash tool invocation"
      pattern: "organize_assets.py"
---

<objective>
Create the remaining agents (asset-curator, meta, code-reviewer, compiler) with seeded MEMORY.md files from V5 expertise. This completes all 10 new agent definitions.

Purpose: Delivers the library management, pipeline oversight, code quality, and compilation agents that round out the full agent roster.
Output: 4 agent definitions, 4 MEMORY.md files
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-migration-memory/03-CONTEXT.md
@.planning/phases/03-agent-migration-memory/03-RESEARCH.md

<interfaces>
<!-- Established agent definition pattern -->

From .claude/agents/researcher.md (body pattern):
```
<project_context>
Read ./CLAUDE.md for project-wide rules, platform constraints, and agent reference table.
</project_context>
# Display Name
## Identity
## Channel Context
## [Domain Procedures]
## Python Scripts Available
## File Conventions
## Task Classification
```

From .planning/config.json (skill mappings):
- asset-curator: [agent-protocols, media-evaluation]
- meta: [agent-protocols, autoresearch, structured-output]
- code-reviewer: [agent-protocols, autoresearch] (new, discretionary)
- compiler: [agent-protocols, structured-output] (new, discretionary)

From 03-RESEARCH.md (tool scoping):
- asset-curator: Read, Write, Edit, Bash, Grep, Glob
- meta: Read, Write, Edit, Bash, Grep, Glob
- code-reviewer: Read, Write, Edit, Bash, Grep, Glob
- compiler: Read, Write, Edit, Bash, Grep, Glob

From 03-RESEARCH.md (color assignments):
- asset-curator: orange (reuse ok)
- meta: red (reuse ok)
- code-reviewer: yellow (reuse ok)
- compiler: cyan (reuse ok)

From 03-RESEARCH.md (consolidation):
- meta: consolidated from V5 meta-lead + pipeline-observer + ux-improver (D-04)
- code-reviewer: standalone from V5 code-reviewer (D-04, 1:1 mapping)

From 03-RESEARCH.md (V5 read-only expertise):
- asset-curator <- taxonomy-global.yaml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create asset-curator and compiler agent definitions with seeded MEMORY.md files</name>
  <files>.claude/agents/asset-curator.md, .claude/agent-memory/asset-curator/MEMORY.md, .claude/agents/compiler.md, .claude/agent-memory/compiler/MEMORY.md</files>
  <read_first>
    - .claude/agents/researcher.md (established pattern)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/asset-curator.md (V5 source)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/compiler.md (V5 source)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/expertise/read-only/taxonomy-global.yaml (V5 expertise for asset-curator)
    - .claude/skills/media-evaluation/SKILL.md (domain skill for asset-curator)
    - .claude/skills/structured-output/SKILL.md (domain skill for compiler)
  </read_first>
  <action>
**Create `.claude/agents/asset-curator.md`** -- adapted from V5 asset-curator per D-07.

Frontmatter:
```yaml
---
name: asset-curator
description: >-
  Manages the global video asset library, evaluates assets for cross-project
  reuse, handles deduplication, and promotes project-level assets to the global
  library. Invoke when assets need organization, deduplication, or library
  management.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
memory: project
color: orange
skills:
  - agent-protocols
  - media-evaluation
---
```

Body structure:
1. `<project_context>` block
2. `# Asset Curator`
3. `## Identity` -- Rich persona per D-06. Manages the global video library at `D:/VideoLibrary/`. Evaluates assets for quality and reuse potential. Handles cross-project deduplication (prevents same asset from being downloaded/embedded multiple times). Promotes high-quality project-level assets to global library. Does NOT download assets (that's asset-processor). Does NOT generate shotlists. Does NOT define visual intent. Domain is library management, asset evaluation, and promotion.
4. `## Channel Context` -- `@channel/channel.md` only
5. `## Library Management` -- Adapt from V5: global library structure, category taxonomy (from taxonomy-global.yaml), asset cataloging, metadata management.
6. `## Deduplication` -- Adapt from V5: cross-project duplicate detection, similarity thresholds, merge strategy for duplicate entries.
7. `## Asset Promotion` -- Adapt from V5: promotion criteria (quality score, reuse potential, uniqueness), promotion workflow (project assets -> global library).
8. `## Python Scripts Available` -- `media/promote.py` (asset promotion to global library). Note: "may not yet be fully connected in V0.6".
9. `## File Conventions` -- Reads from: `projects/<name>/assets/`, `data/asset_catalog.db`. Writes to: `D:/VideoLibrary/` (global library). Data: `data/asset_catalog.db` (SQLite).
10. `## Task Classification` -- DETERMINISTIC (catalog queries, duplicate detection, file operations) vs HEURISTIC (quality assessment, reuse potential evaluation, promotion decisions).

Strip all V5 artifacts. Replace `.pi/` paths. Remove delegation language.

**Create `.claude/agent-memory/asset-curator/MEMORY.md`**

Seed from V5 asset-curator body + taxonomy-global.yaml:
```markdown
# Asset Curator Memory

## Key Files
- Global video library: D:/VideoLibrary/
- Asset catalog: data/asset_catalog.db (SQLite)
- Project assets: projects/*/assets/
- Promotion script: media/promote.py
- LanceDB vectors: data/vectors/ (if applicable)

## Decisions
- [2026-04-10] Global library organized by taxonomy categories from taxonomy-global.yaml
- [2026-04-10] Promotion requires quality score >= 4/5 AND reuse potential assessment
- [2026-04-10] Deduplication uses perceptual hash + filename similarity, not just exact match
[Add 1-2 more from V5 body]

## Patterns
- [2026-04-10] Assets used in 2+ projects are strong promotion candidates
- [2026-04-10] Historical footage (pre-1960) rarely has rights issues but quality varies widely
[Add 1-2 more from V5 source]

## Observations
(none yet)

## Open Questions
(none yet)
```

---

**Create `.claude/agents/compiler.md`** -- adapted from V5 compiler per D-07.

Frontmatter:
```yaml
---
name: compiler
description: >-
  Compiles final edit sheets for DaVinci Resolve, organizes processed assets
  into editor-ready structure, and generates timing-synced asset manifests.
  Invoke when all assets are processed and the video is ready for editing.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
memory: project
color: cyan
skills:
  - agent-protocols
  - structured-output
---
```

Body structure:
1. `<project_context>` block
2. `# Compiler`
3. `## Identity` -- Rich persona per D-06. Final stage of the production pipeline. Compiles processed assets, shotlists, and scripts into a DaVinci Resolve-ready edit sheet. Organizes asset files into editor-friendly directory structure. Generates timing-synced manifests matching assets to script chapters. Does NOT process or embed assets (that's asset-processor). Does NOT plan shots (that's visual-planner). Does NOT write scripts. Domain is compilation, organization, and edit sheet generation.
4. `## Channel Context` -- `@channel/channel.md` only
5. `## Edit Sheet Compilation` -- Adapt from V5: edit sheet format (markdown with timing cues, asset references, chapter markers), assembly from shotlist + assets + script.
6. `## Asset Organization` -- Adapt from V5: file naming conventions for editor, directory structure for DaVinci Resolve import, asset manifest generation.
7. `## Timing Synchronization` -- Adapt from V5: script word count -> timing estimation (150 words/min), chapter timing from script act structure, asset duration alignment.
8. `## Python Scripts Available` -- `media/organize_assets.py` (asset organization for editor). Note: "may not yet be fully connected in V0.6".
9. `## File Conventions` -- Reads from: `projects/<name>/script/Script.md`, `projects/<name>/visuals/shotlist.json`, `projects/<name>/assets/`. Writes to: `projects/<name>/edit/edit_sheet.md`, `projects/<name>/edit/manifest.json`, `projects/<name>/edit/assets/` (organized copies).
10. `## Task Classification` -- DETERMINISTIC (timing calculation, file copying, manifest generation, directory scaffolding) vs HEURISTIC (edit sheet annotation quality, asset-to-chapter matching judgment).

Strip all V5 artifacts.

**Create `.claude/agent-memory/compiler/MEMORY.md`**

Seed from V5 compiler body:
```markdown
# Compiler Memory

## Key Files
- Script input: projects/*/script/Script.md
- Shotlist input: projects/*/visuals/shotlist.json
- Asset source: projects/*/assets/
- Edit sheet output: projects/*/edit/edit_sheet.md
- Manifest output: projects/*/edit/manifest.json
- Asset organizer: media/organize_assets.py

## Decisions
- [2026-04-10] Edit sheet uses markdown format with embedded timing cues in brackets: [00:00-01:30]
- [2026-04-10] Asset naming convention: {project}_{act}_{shot_number}_{descriptor}.{ext}
- [2026-04-10] Timing calculation base: 150 words per minute of narration
[Add 1-2 more from V5 body]

## Patterns
- [2026-04-10] Acts with fewer than 3 asset references signal gaps in visual planning
- [2026-04-10] Edit sheet should include fallback text for shots without matched assets
[Add 1 more from V5 source]

## Observations
(none yet)

## Open Questions
(none yet)
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve.bind(p,'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');['asset-curator','compiler'].forEach(a=>{const ag=fs.readFileSync(r('.claude/agents/'+a+'.md'),'utf8');const mm=fs.readFileSync(r('.claude/agent-memory/'+a+'/MEMORY.md'),'utf8');console.log(a+'-agent:',ag.includes('memory: project')&&ag.includes('agent-protocols')&&ag.includes('tools:')&&!ag.includes('.pi/'));console.log(a+'-mem:',mm.includes('## Key Files')&&mm.includes('## Decisions')&&!mm.includes('(none yet)'))})"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/asset-curator.md exists with `name: asset-curator`, `model: sonnet`, `memory: project`, `color: orange`
    - asset-curator.md references `D:/VideoLibrary/` for global library
    - asset-curator.md `skills:` includes `agent-protocols` and `media-evaluation`
    - .claude/agents/compiler.md exists with `name: compiler`, `model: sonnet`, `memory: project`, `color: cyan`
    - compiler.md `skills:` includes `agent-protocols` and `structured-output`
    - compiler.md body contains edit sheet format and timing calculation procedures
    - All 4 MEMORY.md files have 5 sections with seeded Key Files, Decisions, Patterns
    - No file contains `.pi/multi-team/`, `{{SESSION_DIR}}`, `delegate to`
  </acceptance_criteria>
  <done>Asset-curator and compiler agent definitions exist with correct scoping and domain procedures. MEMORY.md files seeded with V5 knowledge.</done>
</task>

<task type="auto">
  <name>Task 2: Create meta and code-reviewer agent definitions with seeded MEMORY.md files</name>
  <files>.claude/agents/meta.md, .claude/agent-memory/meta/MEMORY.md, .claude/agents/code-reviewer.md, .claude/agent-memory/code-reviewer/MEMORY.md</files>
  <read_first>
    - .claude/agents/researcher.md (established pattern)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/meta-lead.md (V5 source -- primary for meta consolidation)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/pipeline-observer.md (V5 source -- merge into meta)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/ux-improver.md (V5 source -- merge into meta)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/agents/code-reviewer.md (V5 source -- 1:1 mapping)
    - .claude/skills/autoresearch/SKILL.md (domain skill for meta + code-reviewer)
    - .claude/skills/structured-output/SKILL.md (domain skill for meta)
  </read_first>
  <action>
**Create `.claude/agents/meta.md`** -- consolidated from V5 meta-lead + pipeline-observer + ux-improver per D-04, D-07, D-08.

Frontmatter:
```yaml
---
name: meta
description: >-
  Monitors pipeline health, detects cross-stage patterns, reviews output
  usability, and proposes process improvements. Combines pipeline observation,
  UX review, and improvement synthesis into a unified expert. Invoke when you
  want a pipeline health check or process improvement analysis.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
memory: project
color: red
skills:
  - agent-protocols
  - autoresearch
  - structured-output
---
```

Body structure:
1. `<project_context>` block
2. `# Meta Expert`
3. `## Identity` -- Rich persona per D-06. Unified pipeline health expert (per D-08 -- no internal domain splits). Observes cross-stage patterns, reviews output usability, synthesizes improvement proposals. Monitors agent efficiency, detects bottlenecks, tracks recurring issues across pipeline runs. Reviews checkpoint UX and edit sheet readability. Does NOT conduct research. Does NOT write scripts. Does NOT review code (that's code-reviewer per D-04). Does NOT manage assets. Domain is pipeline observation, UX improvement, and process optimization.
4. `## Channel Context` -- `@channel/channel.md` only
5. `## Pipeline Observation` -- Adapt from V5 pipeline-observer: cross-stage pattern detection, bottleneck identification, cost tracking heuristics, performance trend analysis.
6. `## Output Usability Review` -- Adapt from V5 ux-improver: edit sheet readability, checkpoint UX, research dossier navigability, script metadata completeness.
7. `## Improvement Proposals` -- Adapt from V5 meta-lead: proposal format, priority assessment, approval tracking, implementation suggestions.
8. `## Python Scripts Available` -- No dedicated Python scripts. Meta uses Bash for running other scripts to test them, reading logs, etc.
9. `## File Conventions` -- Reads from: ALL project directories, agent definitions at `.claude/agents/`, skill definitions at `.claude/skills/`. Writes to: `meta/proposals/`, `meta/health-reports/`.
10. `## Task Classification` -- DETERMINISTIC (log parsing, metric extraction, file structure validation) vs HEURISTIC (bottleneck assessment, UX judgment, improvement prioritization, pattern interpretation).

CRITICAL consolidation rules per D-08:
- Present as ONE unified expert, not "meta-lead wearing pipeline-observer hat"
- Merge all three V5 agent bodies, resolving conflicts
- Remove meta-lead coordination procedures (approval workflows, team coordination) -- Phase 4
- Remove pipeline-observer delegation chain references
- Remove ux-improver's narrow focus -- broaden to general output usability
- KEEP: pattern detection logic, usability review criteria, proposal structure

**Create `.claude/agent-memory/meta/MEMORY.md`**

Seed from V5 meta-lead + pipeline-observer + ux-improver bodies (D-09 -- merge all, dedupe):
```markdown
# Meta Memory

## Key Files
- Agent definitions: .claude/agents/*.md
- Skill definitions: .claude/skills/*/SKILL.md
- Project outputs: projects/*/
- Health reports: meta/health-reports/
- Improvement proposals: meta/proposals/
- Pipeline config: .planning/config.json

## Decisions
- [2026-04-10] Improvement proposals follow structured format: problem, impact, proposed solution, effort estimate, priority
- [2026-04-10] Pipeline health checks assess: completion rate, error frequency, time per stage, output quality trends
- [2026-04-10] UX review covers: navigability, completeness, format consistency, actionability of outputs
[Add 2-3 more from V5 bodies -- merge unique decisions from all 3 sources, dedupe]

## Patterns
- [2026-04-10] Cross-stage bottlenecks most commonly appear at research->script and visual-plan->assets transitions
- [2026-04-10] Edit sheet readability degrades when act count exceeds 7 -- visual cognitive load increases
- [2026-04-10] Recurring issues across 3+ pipeline runs warrant a formal improvement proposal
[Add 2-3 more from V5 bodies -- merge unique patterns, dedupe]

## Observations
(none yet)

## Open Questions
(none yet)
```

---

**Create `.claude/agents/code-reviewer.md`** -- from V5 code-reviewer (1:1 per D-04).

Frontmatter:
```yaml
---
name: code-reviewer
description: >-
  Reviews Python scripts and agent definitions for code quality, correctness,
  and adherence to project conventions. Identifies bugs, suggests improvements,
  and can implement fixes. Invoke when code changes need review or quality
  assessment.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
memory: project
color: yellow
skills:
  - agent-protocols
  - autoresearch
---
```

Body structure:
1. `<project_context>` block
2. `# Code Reviewer`
3. `## Identity` -- Rich persona per D-06. Reviews code quality across the pipeline codebase: Python scripts (strategy/, editorial/, media/), agent definitions, skill definitions, test scripts. Identifies bugs, convention violations, performance issues, and security concerns. Can implement fixes directly. Does NOT observe pipeline health (that's meta). Does NOT review editorial content. Does NOT write scripts (documentary scripts). Domain is code quality, correctness, and project convention adherence.
4. `## Channel Context` -- `@channel/channel.md` only (for project context, not voice-relevant)
5. `## Review Procedure` -- Adapt from V5: code review checklist, severity classification (critical/major/minor), review output format.
6. `## Python Script Review` -- Adapt from V5: Python-specific checks (error handling, path safety, conda env usage, Windows compatibility).
7. `## Agent/Skill Definition Review` -- Adapted for V0.6: frontmatter validation, persona completeness, tool scoping correctness, memory setup verification.
8. `## Fix Implementation` -- When and how to implement fixes directly vs flagging for human decision.
9. `## File Conventions` -- Reads from: ALL code files (`strategy/`, `editorial/`, `media/`, `.claude/agents/`, `.claude/skills/`, `tests/`). Writes to: review reports at `meta/reviews/`, direct edits to code files when implementing fixes.
10. `## Task Classification` -- DETERMINISTIC (convention checking, syntax validation, test execution) vs HEURISTIC (code quality judgment, refactoring suggestions, security assessment).

Strip all V5 artifacts. This is a 1:1 mapping (no consolidation needed).

**Create `.claude/agent-memory/code-reviewer/MEMORY.md`**

Seed from V5 code-reviewer body:
```markdown
# Code Reviewer Memory

## Key Files
- Strategy scripts: strategy/*.py
- Editorial scripts: editorial/**/*.py
- Media scripts: media/*.py
- Agent definitions: .claude/agents/*.md
- Skill definitions: .claude/skills/*/SKILL.md
- Test scripts: tests/*.js
- Review output: meta/reviews/

## Decisions
- [2026-04-10] Windows path safety: always use path.resolve() or os.path.join(), never string concatenation
- [2026-04-10] GPU scripts must specify conda env explicitly, never rely on system PATH
- [2026-04-10] Review severity: critical (blocks pipeline), major (incorrect behavior), minor (style/convention)
[Add 1-2 more from V5 body]

## Patterns
- [2026-04-10] Most common Python bugs in pipeline: unhandled None returns, missing file existence checks, hardcoded paths
- [2026-04-10] Agent definition issues typically: missing frontmatter fields, V5 artifacts, incorrect tool scoping
[Add 1-2 more from V5 source]

## Observations
(none yet)

## Open Questions
(none yet)
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve.bind(p,'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');['meta','code-reviewer'].forEach(a=>{const ag=fs.readFileSync(r('.claude/agents/'+a+'.md'),'utf8');const mm=fs.readFileSync(r('.claude/agent-memory/'+a+'/MEMORY.md'),'utf8');console.log(a+'-agent:',ag.includes('memory: project')&&ag.includes('agent-protocols')&&ag.includes('## Identity')&&!ag.includes('.pi/'));console.log(a+'-mem:',mm.includes('## Key Files')&&mm.includes('## Decisions')&&!mm.includes('(none yet)'))})"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/meta.md exists with `name: meta`, `model: sonnet`, `memory: project`, `color: red`
    - meta.md `skills:` includes `agent-protocols`, `autoresearch`, `structured-output`
    - meta.md `## Identity` explicitly states "does NOT review code (that's code-reviewer)"
    - meta.md presents as unified expert (no "meta-lead + pipeline-observer" internal splits per D-08)
    - .claude/agents/code-reviewer.md exists with `name: code-reviewer`, `model: sonnet`, `memory: project`, `color: yellow`
    - code-reviewer.md `skills:` includes `agent-protocols` and `autoresearch`
    - code-reviewer.md `## Identity` explicitly states "does NOT observe pipeline health (that's meta)"
    - All 4 MEMORY.md files have 5 sections with seeded Key Files, Decisions, Patterns
    - meta MEMORY.md merges content from 3 V5 sources (not a copy of just one)
    - No file contains `.pi/multi-team/`, `{{SESSION_DIR}}`, `delegate to`
  </acceptance_criteria>
  <done>Meta (consolidated from 3 V5 agents) and code-reviewer (standalone per D-04) agent definitions exist with clear domain boundaries and seeded MEMORY.md files. All 10 new agents are now created across plans 01-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta broad read access | Meta agent reads all project files for pipeline health assessment |
| Code-reviewer write access | Code-reviewer can edit code files directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Elevation of Privilege | Meta broad access | accept | Meta needs broad read to observe pipeline; tools: field includes Write only for proposals/reports. Phase 4 hooks will add directory enforcement |
| T-03-08 | Tampering | Code-reviewer edits | accept | Code-reviewer legitimately needs Write/Edit to implement fixes; user invokes explicitly. Phase 4 hooks will scope write directories |
</threat_model>

<verification>
After plan completion, verify all 4 agents pass smoke test checks:
```
node tests/smoke-test-agents.js 2>&1 | grep -E "(asset-curator|meta|code-reviewer|compiler)"
```
</verification>

<success_criteria>
- All 4 agent definitions follow established pattern
- Meta presents as unified expert (D-08), not internal domain splits
- Code-reviewer is clearly separate from meta (D-04)
- All 4 MEMORY.md files have rich seeded content
- Zero V5 artifacts in any created file
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-migration-memory/03-03-SUMMARY.md`
</output>
