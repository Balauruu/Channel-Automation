---
phase: 06-integration-end-to-end-validation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .claude/agents/strategy.md
  - .claude/agents/researcher.md
  - .claude/agents/writer.md
  - .claude/agents/asset-processor.md
  - .claude/agents/asset-curator.md
  - .claude/agents/compiler.md
  - .claude/agents/visual-researcher.md
  - .claude/agents/visual-planner.md
  - .claude/skills/documentary-research/SKILL.md
  - .claude/skills/data-analysis/SKILL.md
  - .claude/skills/media-evaluation/SKILL.md
  - .claude/skills/archive-search/SKILL.md
  - .claude/skills/visual-narrative/SKILL.md
autonomous: true
requirements:
  - INTG-01
  - INTG-05

must_haves:
  truths:
    - "All agent bodies reference .claude/scripts/ paths instead of old domain/ paths"
    - "All domain skill script references point to .claude/scripts/ paths"
    - "No agent body contains 'may not yet be fully connected in V0.6' caveats"
    - "Strategy agent uses PYTHONPATH module invocation pattern, not direct cli.py invocation"
    - "Agent bodies use quoted relative PYTHONPATH (no spaces issue)"
    - "Strategy agent does not list 'init' as a valid CLI subcommand"
  artifacts:
    - path: ".claude/agents/strategy.md"
      provides: "Updated strategy agent with correct invocation commands"
      contains: "PYTHONPATH=\".claude/scripts/strategy\""
    - path: ".claude/agents/researcher.md"
      provides: "Updated researcher agent with correct invocation commands"
      contains: "PYTHONPATH=\".claude/scripts/editorial\""
    - path: ".claude/agents/writer.md"
      provides: "Updated writer agent with correct invocation commands"
      contains: "PYTHONPATH=\".claude/scripts/editorial\""
    - path: ".claude/agents/asset-processor.md"
      provides: "Updated asset-processor agent with correct script paths"
      contains: ".claude/scripts/media/embed.py"
    - path: ".claude/skills/documentary-research/SKILL.md"
      provides: "Updated skill with correct editorial script paths"
      contains: ".claude/scripts/editorial"
  key_links:
    - from: ".claude/agents/strategy.md"
      to: ".claude/scripts/strategy/channel_assistant/cli.py"
      via: "PYTHONPATH module invocation"
      pattern: "PYTHONPATH.*\\.claude/scripts/strategy.*python -m channel_assistant"
    - from: ".claude/agents/researcher.md"
      to: ".claude/scripts/editorial/researcher/cli.py"
      via: "PYTHONPATH module invocation"
      pattern: "PYTHONPATH.*\\.claude/scripts/editorial.*python -m researcher"
    - from: ".claude/agents/asset-processor.md"
      to: ".claude/scripts/media/embed.py"
      via: "Direct script invocation with conda python"
      pattern: "perception-models.*\\.claude/scripts/media/embed\\.py"
---

<objective>
Update all agent body script paths and domain skill script references from old V5-era paths to the new `.claude/scripts/` locations. Remove all "not yet connected in V0.6" caveats. Fix the strategy agent's non-existent `init` subcommand reference.

Purpose: Wire agents and skills to the actual script files copied in Plan 01, making them invocable. Per D-04, agent bodies own script invocation. Per D-06, domain skills keep reference-only script docs with updated paths. Per D-07, scripts are primary with no fallback.

Output: 8 updated agent files and 5 updated domain skill files with correct paths, correct invocation patterns, and no stale caveats.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-end-to-end-validation/06-CONTEXT.md
@.planning/phases/06-integration-end-to-end-validation/06-RESEARCH.md
@.planning/phases/06-integration-end-to-end-validation/06-01-SUMMARY.md

<interfaces>
<!-- Agent path update map from RESEARCH.md -->

Strategy agent: `strategy/cli.py add/scrape/analyze/topics/init` -> `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant <cmd>`
Researcher agent: `editorial/researcher/cli.py survey/deepen/synthesize` -> `PYTHONPATH=".claude/scripts/editorial" python -m researcher <cmd>`
Writer agent: `editorial/writer/cli.py load/generate/revise` -> `PYTHONPATH=".claude/scripts/editorial" python -m writer <cmd>`
Asset-processor: `media/embed.py` -> `.claude/scripts/media/embed.py` (with conda python)
Asset-processor: `media/search.py` -> `.claude/scripts/media/search.py` (with conda python)
Asset-processor: `media/download.py` -> `.claude/scripts/media/download.py`
Asset-processor: `media/evaluate.py` -> `.claude/scripts/media/evaluate.py`
Asset-curator: `media/promote.py` -> `.claude/scripts/media/promote.py`
Compiler: `media/organize_assets.py` -> `.claude/scripts/media/organize_assets.py`
Visual-researcher: `media/crawl_images.py` -> `.claude/scripts/media/crawl_images.py`
Visual-researcher: `media/wiki_screenshots.py` -> `.claude/scripts/media/wiki_screenshots.py`
Visual-planner: `media/ia_search.py` -> `.claude/scripts/media/ia_search.py`

Domain skill script references:
documentary-research: `editorial/researcher/cli.py` -> `.claude/scripts/editorial/researcher/cli.py`
data-analysis: `strategy/analyzer.py` etc -> `.claude/scripts/strategy/channel_assistant/analyzer.py`
media-evaluation: `media/evaluate.py` etc -> `.claude/scripts/media/evaluate.py`
archive-search: `media/ia_search.py` etc -> `.claude/scripts/media/ia_search.py`
visual-narrative: `media/discover.py` etc -> `.claude/scripts/media/discover.py`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 8 agent body script paths and remove stale caveats</name>
  <files>
    .claude/agents/strategy.md
    .claude/agents/researcher.md
    .claude/agents/writer.md
    .claude/agents/asset-processor.md
    .claude/agents/asset-curator.md
    .claude/agents/compiler.md
    .claude/agents/visual-researcher.md
    .claude/agents/visual-planner.md
  </files>
  <read_first>
    .claude/agents/strategy.md
    .claude/agents/researcher.md
    .claude/agents/writer.md
    .claude/agents/asset-processor.md
    .claude/agents/asset-curator.md
    .claude/agents/compiler.md
    .claude/agents/visual-researcher.md
    .claude/agents/visual-planner.md
  </read_first>
  <action>
Update each agent file with the exact changes below. Use the Edit tool for targeted modifications. Preserve all other content unchanged.

**1. strategy.md -- 4 changes:**

a) Replace the "Python Scripts Available" section header and caveat. Change:
```
## Python Scripts Available

All strategy scripts are invoked through the strategy CLI. These scripts may not yet be fully connected in V0.6:

- `strategy/cli.py add <url>` -- Register a competitor channel for tracking
- `strategy/cli.py scrape` -- Scrape video metadata from all registered channels
- `strategy/cli.py analyze` -- Run statistical analysis on scraped data
- `strategy/cli.py topics` -- Generate scored topic briefs
- `strategy/cli.py init <project-name>` -- Initialize a new project directory

Run scripts via the Bash tool. Check script help (`--help`) for current capabilities before relying on automated output. Store competitor data in the SQLite database at `data/channel_assistant.db`.
```
To:
```
## Python Scripts

Run strategy commands via module invocation from the Bash tool:

- `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant add <url>` -- Register a competitor channel for tracking
- `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant scrape` -- Scrape video metadata from all registered channels
- `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant analyze` -- Run statistical analysis on scraped data
- `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant topics` -- Generate scored topic briefs

Note: `project_init.py` provides project initialization functions but is not wired as a CLI subcommand. Use the project initialization procedure in the agent body instead.

Store competitor data in the SQLite database at `data/channel_assistant.db`. The competitor channel registry is at `strategy/competitors/competitors.json`.

If a script fails, report the error and stop. Do NOT fall back to Claude-native capabilities.
```

b) Update the inline references elsewhere in the body. Replace:
- `strategy/cli.py add <channel_url>` -> `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant add <channel_url>` (in Channel Discovery section)
- `strategy/cli.py scrape` -> `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant scrape` (in Scraping Pipeline section)
- `strategy/cli.py analyze` -> `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant analyze` (in Analysis Dimensions section)
- `strategy/cli.py topics` -> `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant topics` (in Ranking and Output section)
- `strategy/cli.py init <project-name>` -> remove this line entirely from the Project Initialization section (the `init` subcommand does not exist per Pitfall 5 in research)

**2. researcher.md -- 2 changes:**

a) Replace the "Python Scripts Available" section. Change:
```
## Python Scripts Available

These scripts provide automated research capabilities. They may not yet be fully connected in V0.6:

- `editorial/researcher/cli.py survey <topic>` -- Automated broad survey across configured source types
- `editorial/researcher/cli.py deepen <topic>` -- Deep dive on specific aspects identified during survey
- `editorial/researcher/cli.py synthesize <topic>` -- Compile raw research into structured dossier format

Run scripts via Bash tool. Check script help (`--help`) for current capabilities before relying on automated output.
```
To:
```
## Python Scripts

Run research commands via module invocation from the Bash tool:

- `PYTHONPATH=".claude/scripts/editorial" python -m researcher survey "<topic>"` -- Automated broad survey across configured source types
- `PYTHONPATH=".claude/scripts/editorial" python -m researcher deepen "<topic>"` -- Deep dive on specific aspects identified during survey
- `PYTHONPATH=".claude/scripts/editorial" python -m researcher synthesize "<topic>"` -- Compile raw research into structured dossier format

If a script fails, report the error and stop. Do NOT fall back to Claude-native capabilities (WebSearch, etc.).
```

**3. writer.md -- 2 changes:**

a) Replace the "Python Scripts Available" section. Change:
```
## Python Scripts Available

These scripts provide automated writing capabilities. They may not yet be fully connected in V0.6:

- `editorial/writer/cli.py load <project>` -- Load research dossier for script generation
- `editorial/writer/cli.py generate <project>` -- Generate script draft from loaded research
- `editorial/writer/cli.py revise <project>` -- Revise existing script with feedback

Run scripts via Bash tool. Check script help (`--help`) for current capabilities before relying on automated output.
```
To:
```
## Python Scripts

Run writer commands via module invocation from the Bash tool:

- `PYTHONPATH=".claude/scripts/editorial" python -m writer load "<project>"` -- Load research dossier for script generation
- `PYTHONPATH=".claude/scripts/editorial" python -m writer generate "<project>"` -- Generate script draft from loaded research
- `PYTHONPATH=".claude/scripts/editorial" python -m writer revise "<project>"` -- Revise existing script with feedback

If a script fails, report the error and stop. Do NOT fall back to Claude-native capabilities.
```

**4. asset-processor.md -- 2 changes:**

a) Replace the "Python Scripts Available" section (near end of file). Change:
```
Run GPU scripts with the perception-models conda environment:

```
C:/Users/iorda/miniconda3/envs/perception-models/python.exe media/embed.py
C:/Users/iorda/miniconda3/envs/perception-models/python.exe media/search.py
```

Available scripts:

- `media/embed.py` -- CLIP embedding generation (GPU). Processes video frames through PE-Core model, stores embeddings as `.npy`.
- `media/search.py` -- Semantic search (GPU). Queries embedded pool with natural language, returns ranked results by cosine similarity.
- `media/download.py` -- Asset download from YouTube and archive.org. Handles rate limiting, format selection, re-encoding.
- `media/evaluate.py` -- Automated quality evaluation. Resolution check, artifact detection, blank frame identification.

Non-GPU scripts (`download.py`, `evaluate.py`) can use standard Python if the conda env is unavailable, but using the conda env for all media scripts ensures consistent dependencies.

Scripts may not yet be fully connected in V0.6. Check script help (`--help`) for current capabilities.
```
To:
```
## Python Scripts

Run GPU scripts with the perception-models conda environment:

```
"C:/Users/iorda/miniconda3/envs/perception-models/python.exe" .claude/scripts/media/embed.py <args>
"C:/Users/iorda/miniconda3/envs/perception-models/python.exe" .claude/scripts/media/search.py <args>
```

Available scripts:

- `.claude/scripts/media/embed.py` -- CLIP embedding generation (GPU). Processes video frames through PE-Core model, stores embeddings as `.npy`.
- `.claude/scripts/media/search.py` -- Semantic search (GPU). Queries embedded pool with natural language, returns ranked results by cosine similarity.
- `.claude/scripts/media/download.py` -- Asset download from YouTube and archive.org. Handles rate limiting, format selection, re-encoding.
- `.claude/scripts/media/evaluate.py` -- Automated quality evaluation. Resolution check, artifact detection, blank frame identification.

Non-GPU scripts (`download.py`, `evaluate.py`) can use standard Python. Using the conda env for all media scripts ensures consistent dependencies.

If a script fails, report the error and stop. Do NOT fall back to Claude-native capabilities.
```

**5. asset-curator.md -- 1 change:**

Replace the "Python Scripts Available" section. Change:
```
## Python Scripts Available

These scripts provide automation capabilities. They may not yet be fully connected in V0.6:

- `media/promote.py` -- Asset promotion to global library (copy, catalog insertion, metadata update)

Run scripts via Bash tool. Check script help (`--help`) for current capabilities before relying on automated output.
```
To:
```
## Python Scripts

- `.claude/scripts/media/promote.py` -- Asset promotion to global library (copy, catalog insertion, metadata update)

Run via: `python .claude/scripts/media/promote.py <args>`

If a script fails, report the error and stop. Do NOT fall back to Claude-native capabilities.
```

Also update the File Conventions section: change `media/promote.py` to `.claude/scripts/media/promote.py`.

**6. compiler.md -- 1 change:**

Replace the "Python Scripts Available" section. Change:
```
## Python Scripts Available

These scripts provide automation capabilities. They may not yet be fully connected in V0.6:

- `media/organize_assets.py` -- Asset organization and renaming for editor-ready structure

Run scripts via Bash tool. Check script help (`--help`) for current capabilities before relying on automated output.
```
To:
```
## Python Scripts

- `.claude/scripts/media/organize_assets.py` -- Asset organization and renaming for editor-ready structure

Run via: `python .claude/scripts/media/organize_assets.py <args>`

If a script fails, report the error and stop. Do NOT fall back to Claude-native capabilities.
```

**7. visual-researcher.md -- 1 change:**

Replace the "Python Scripts Available" section. Change:
```
## Python Scripts Available

These scripts provide automated visual resource gathering. They may not yet be fully connected in V0.6:

- `media/crawl_images.py` -- Extract and download images from crawled web pages
- `media/wiki_screenshots.py` -- Playwright-based Wikipedia full-page captures for visual reference

Run scripts via Bash tool. Check script help (`--help`) for current capabilities before relying on automated output.
```
To:
```
## Python Scripts

- `.claude/scripts/media/crawl_images.py` -- Extract and download images from crawled web pages
- `.claude/scripts/media/wiki_screenshots.py` -- Playwright-based Wikipedia full-page captures for visual reference

Run via: `python .claude/scripts/media/crawl_images.py <args>` and `python .claude/scripts/media/wiki_screenshots.py <args>`

If a script fails, report the error and stop. Do NOT fall back to Claude-native capabilities.
```

**8. visual-planner.md -- 1 change:**

Replace the "Python Scripts Available" section. Change:
```
## Python Scripts Available

These scripts provide automated archive search capabilities. They may not yet be fully connected in V0.6:

- `media/ia_search.py` -- Internet Archive search with metadata extraction and filtering

Run scripts via Bash tool. Check script help (`--help`) for current capabilities before relying on automated output.
```
To:
```
## Python Scripts

- `.claude/scripts/media/ia_search.py` -- Internet Archive search with metadata extraction and filtering

Run via: `python .claude/scripts/media/ia_search.py <args>`

If a script fails, report the error and stop. Do NOT fall back to Claude-native capabilities.
```

**CRITICAL across ALL agents:** After updating, verify NO agent file still contains the phrase "may not yet be fully connected in V0.6" or "not yet connected" or "Check script help (`--help`) for current capabilities before relying on automated output."
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path'),r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const agents=['strategy','researcher','writer','asset-processor','asset-curator','compiler','visual-researcher','visual-planner'];let ok=0,total=0;agents.forEach(a=>{const c=fs.readFileSync(p.join(r,'.claude','agents',a+'.md'),'utf8');total++;const noStale=!c.includes('not yet be fully connected')&&!c.includes('not yet connected');console.log(noStale?'PASS':'FAIL',a+'/no_stale_caveat');if(noStale)ok++;total++;const hasNewPath=c.includes('.claude/scripts/');console.log(hasNewPath?'PASS':'FAIL',a+'/has_new_paths');if(hasNewPath)ok++});console.log(ok+'/'+total);process.exit(ok===total?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - strategy.md contains `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant add`
    - strategy.md contains `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant scrape`
    - strategy.md contains `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant analyze`
    - strategy.md contains `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant topics`
    - strategy.md does NOT contain `strategy/cli.py init`
    - strategy.md does NOT contain `may not yet be fully connected`
    - researcher.md contains `PYTHONPATH=".claude/scripts/editorial" python -m researcher survey`
    - researcher.md does NOT contain `may not yet be fully connected`
    - writer.md contains `PYTHONPATH=".claude/scripts/editorial" python -m writer load`
    - writer.md does NOT contain `may not yet be fully connected`
    - asset-processor.md contains `.claude/scripts/media/embed.py`
    - asset-processor.md contains `.claude/scripts/media/search.py`
    - asset-processor.md does NOT contain `may not yet be fully connected`
    - asset-curator.md contains `.claude/scripts/media/promote.py`
    - asset-curator.md does NOT contain `may not yet be fully connected`
    - compiler.md contains `.claude/scripts/media/organize_assets.py`
    - compiler.md does NOT contain `may not yet be fully connected`
    - visual-researcher.md contains `.claude/scripts/media/crawl_images.py`
    - visual-researcher.md does NOT contain `may not yet be fully connected`
    - visual-planner.md contains `.claude/scripts/media/ia_search.py`
    - visual-planner.md does NOT contain `may not yet be fully connected`
    - All 8 agent files contain the no-fallback instruction: `Do NOT fall back to Claude-native capabilities`
  </acceptance_criteria>
  <done>All 8 agent bodies have correct .claude/scripts/ paths, module invocation patterns for strategy/editorial, no stale V0.6 caveats, no-fallback instructions per D-07, and strategy agent no longer references the non-existent `init` subcommand.</done>
</task>

<task type="auto">
  <name>Task 2: Update 5 domain skill script reference paths</name>
  <files>
    .claude/skills/documentary-research/SKILL.md
    .claude/skills/data-analysis/SKILL.md
    .claude/skills/media-evaluation/SKILL.md
    .claude/skills/archive-search/SKILL.md
    .claude/skills/visual-narrative/SKILL.md
  </files>
  <read_first>
    .claude/skills/documentary-research/SKILL.md
    .claude/skills/data-analysis/SKILL.md
    .claude/skills/media-evaluation/SKILL.md
    .claude/skills/archive-search/SKILL.md
    .claude/skills/visual-narrative/SKILL.md
  </read_first>
  <action>
Update script reference paths in each domain skill's SKILL.md file. Per D-06, domain skills keep reference-only script documentation -- these are NOT procedural invocation instructions. Update paths and remove any "Available after Phase 6 integration" or similar notes.

**1. documentary-research/SKILL.md:**
Find the Script References section (around line 105). Replace:
```
- `editorial/researcher/cli.py survey <topic>` -- Automated broad survey across multiple source types
- `editorial/researcher/cli.py deepen <topic>` -- Deep dive on specific aspects identified in survey
- `editorial/researcher/cli.py synthesize <topic>` -- Compile findings into structured dossier format
```
With:
```
- `.claude/scripts/editorial/researcher/cli.py survey <topic>` -- Automated broad survey across multiple source types
- `.claude/scripts/editorial/researcher/cli.py deepen <topic>` -- Deep dive on specific aspects identified in survey
- `.claude/scripts/editorial/researcher/cli.py synthesize <topic>` -- Compile findings into structured dossier format
```

**2. data-analysis/SKILL.md:**
Find the script references (around line 246). Replace:
```
- `strategy/analyzer.py` -- Competitor channel analysis (metrics extraction, benchmarking, trend detection)
- `strategy/topics.py` -- Topic generation and saturation scoring against competitor landscape
- `strategy/scraper.py` -- YouTube data collection (channel metadata, video metrics, comments)
```
With:
```
- `.claude/scripts/strategy/channel_assistant/analyzer.py` -- Competitor channel analysis (metrics extraction, benchmarking, trend detection)
- `.claude/scripts/strategy/channel_assistant/topics.py` -- Topic generation and saturation scoring against competitor landscape
- `.claude/scripts/strategy/channel_assistant/scraper.py` -- YouTube data collection (channel metadata, video metrics, comments)
```

**3. media-evaluation/SKILL.md:**
Find the script references (around line 231). Replace:
```
- `media/evaluate.py` -- Automated technical quality scoring (resolution, artifacts, blank frame detection)
- `media/search.py` -- CLIP semantic search across embedded asset library
- `media/embed.py` -- CLIP embedding generation for visual assets
```
With:
```
- `.claude/scripts/media/evaluate.py` -- Automated technical quality scoring (resolution, artifacts, blank frame detection)
- `.claude/scripts/media/search.py` -- CLIP semantic search across embedded asset library
- `.claude/scripts/media/embed.py` -- CLIP embedding generation for visual assets
```

**4. archive-search/SKILL.md:**
Find the script references (around line 137). Replace:
```
- `media/ia_search.py` -- Internet Archive search with metadata extraction and filtering
- `media/discover.py` -- Multi-source discovery across archives, YouTube, and web sources
```
With:
```
- `.claude/scripts/media/ia_search.py` -- Internet Archive search with metadata extraction and filtering
- `.claude/scripts/media/discover.py` -- Multi-source discovery across archives, YouTube, and web sources
```

**5. visual-narrative/SKILL.md:**
Find the script references (around line 269). Replace:
```
- `media/discover.py` -- Visual source discovery across archive collections
- `media/organize_assets.py` -- Asset organization by visual role and narrative function
```
With:
```
- `.claude/scripts/media/discover.py` -- Visual source discovery across archive collections
- `.claude/scripts/media/organize_assets.py` -- Asset organization by visual role and narrative function
```

Remove any "Available after Phase 6 integration" or similar future-tense notes from all 5 files if present.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path'),r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const skills=[['documentary-research','.claude/scripts/editorial'],['data-analysis','.claude/scripts/strategy'],['media-evaluation','.claude/scripts/media'],['archive-search','.claude/scripts/media'],['visual-narrative','.claude/scripts/media']];let ok=0;skills.forEach(([s,expected])=>{const c=fs.readFileSync(p.join(r,'.claude','skills',s,'SKILL.md'),'utf8');const has=c.includes(expected);console.log(has?'PASS':'FAIL',s+'/has_new_script_path');if(has)ok++});console.log(ok+'/'+skills.length);process.exit(ok===skills.length?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - documentary-research/SKILL.md contains `.claude/scripts/editorial/researcher/cli.py`
    - documentary-research/SKILL.md does NOT contain bare `editorial/researcher/cli.py` without `.claude/scripts/` prefix
    - data-analysis/SKILL.md contains `.claude/scripts/strategy/channel_assistant/analyzer.py`
    - data-analysis/SKILL.md does NOT contain bare `strategy/analyzer.py` without `.claude/scripts/` prefix
    - media-evaluation/SKILL.md contains `.claude/scripts/media/evaluate.py`
    - media-evaluation/SKILL.md does NOT contain bare `media/evaluate.py` without `.claude/scripts/` prefix
    - archive-search/SKILL.md contains `.claude/scripts/media/ia_search.py`
    - archive-search/SKILL.md does NOT contain bare `media/ia_search.py` without `.claude/scripts/` prefix
    - visual-narrative/SKILL.md contains `.claude/scripts/media/discover.py`
    - visual-narrative/SKILL.md does NOT contain bare `media/discover.py` without `.claude/scripts/` prefix
    - No skill file contains "Available after Phase 6" or similar future-tense notes
  </acceptance_criteria>
  <done>All 5 domain skill SKILL.md files have updated script reference paths pointing to .claude/scripts/ locations. No stale path references remain. Reference-only documentation format preserved per D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent body -> script path | Agent instructions reference filesystem paths that must resolve correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Agent body script paths | mitigate | Paths updated to exact `.claude/scripts/` locations verified to exist by Plan 01. Smoke test in Plan 03 validates all paths resolve. |
| T-06-05 | Elevation of Privilege | No-fallback instruction | accept | D-07 mandates no silent fallback. Agent bodies contain explicit "report error and stop" instruction. Risk is user confusion if script fails -- acceptable, clear error is better than hidden degradation. |
</threat_model>

<verification>
1. All 8 agent files contain `.claude/scripts/` path references
2. No agent file contains "may not yet be fully connected" or similar V0.6 caveats
3. Strategy agent uses `PYTHONPATH=".claude/scripts/strategy" python -m channel_assistant` pattern
4. Researcher agent uses `PYTHONPATH=".claude/scripts/editorial" python -m researcher` pattern
5. Writer agent uses `PYTHONPATH=".claude/scripts/editorial" python -m writer` pattern
6. Asset-processor uses conda python path with `.claude/scripts/media/` prefix
7. All 5 domain skills reference `.claude/scripts/` paths
8. All agents include no-fallback instruction per D-07
9. Strategy agent does not reference `init` as a CLI subcommand
</verification>

<success_criteria>
- Every agent body that references Python scripts uses the correct `.claude/scripts/` path
- Module invocation pattern (PYTHONPATH) used for strategy and editorial agents
- Direct script invocation (with conda python) used for media agent scripts
- All stale V0.6 caveats removed from all agents
- All domain skill script references updated to `.claude/scripts/` paths
- No-fallback behavior documented in all script-using agents per D-07
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-end-to-end-validation/06-02-SUMMARY.md`
</output>
