---
phase: 04-agent-consumption
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - .claude/agents/observer.md
  - .claude/scripts/obs-summarize.js
autonomous: true
requirements: [MEML-04]
must_haves:
  truths:
    - "Observer writes cross-agent insights to PLAYBOOK ## Open, routes to target MEMORY.md/insights.md, and marks PLAYBOOK entry as Resolved -- all in one pass"
    - "Observer Step 8 has a PLAYBOOK-specific branch for Q3 scope-test passes"
    - "Observer Protocol Overrides section references ## Open (not ## Pending Review) for PLAYBOOK.md"
    - "obs-summarize.js references .claude/logs/observations/ path instead of .claude/logs/runs/"
  artifacts:
    - path: ".claude/agents/observer.md"
      provides: "Observer agent with PLAYBOOK routing capability"
      contains: "## Open"
    - path: ".claude/scripts/obs-summarize.js"
      provides: "Updated path references to obs.jsonl"
      contains: "logs/observations"
  key_links:
    - from: ".claude/agents/observer.md"
      to: ".claude/PLAYBOOK.md"
      via: "Step 8 PLAYBOOK routing branch"
      pattern: "## Open"
    - from: ".claude/agents/observer.md"
      to: ".claude/agent-memory"
      via: "PLAYBOOK routing target resolution"
      pattern: "route to.*MEMORY.md"
---

<objective>
Update the observer agent prompt to perform PLAYBOOK routing (write to Open, route to target, mark Resolved) and fix obs-summarize.js old path references.

Purpose: The observer must gain the ability to route cross-agent insights through PLAYBOOK.md's new Open/Resolved lifecycle in a single /evolve pass (D-10). obs-summarize.js is the last file referencing the dead `.claude/logs/runs/` path (D-17).

Output: Updated `.claude/agents/observer.md`, fixed `.claude/scripts/obs-summarize.js`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-consumption/04-CONTEXT.md
@.planning/phases/04-agent-consumption/04-PATTERNS.md
@.planning/phases/02-observer-agent/02-03-SUMMARY.md

<interfaces>
<!-- PLAYBOOK.md structure (created by Plan 01) -->
## Open
(entries in format: `- [CONF] source-agent: insight text (timestamp)`)

## Resolved
(entries in format: `- [Resolved] agent: insight text -> routed to {path} (date)`)

<!-- observer.md current Step 8 structure (lines 150-187) -->
### Step 8: Write Entries (per OBSV-04, D-04)
- For MEMORY.md and PLAYBOOK.md targets: `- [CONFIDENCE] source-agent: distilled insight text (YYYY-MM-DDThh:mm)`
- For insights.md targets: `- [YYYY-MM-DD] [CONFIDENCE] distilled insight text (from: source-agent, YYYY-MM-DDThh:mm)`
- Writing procedure: Read target -> find ## Pending Review -> Edit append -> read-back verify

<!-- observer.md Protocol Overrides (lines 30-39) -->
Write targets list includes `.claude/PLAYBOOK.md (## Pending Review section)` -- must change to `## Open section`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update observer.md with PLAYBOOK routing logic and simplified Protocol Overrides</name>
  <files>.claude/agents/observer.md</files>
  <read_first>
    - .claude/agents/observer.md (full file -- 320 lines, current 10-step pipeline)
    - .claude/PLAYBOOK.md (to confirm Plan 01 redesign is in place -- must have ## Open and ## Resolved)
    - .planning/phases/04-agent-consumption/04-CONTEXT.md (D-08, D-09, D-10 define routing behavior)
    - .planning/phases/04-agent-consumption/04-PATTERNS.md (Step 8 update pattern, Protocol Overrides update)
  </read_first>
  <action>
Make three targeted edits to `.claude/agents/observer.md`:

**Edit 1: Simplify Protocol Overrides section (around lines 30-39)**

Replace the current Protocol Overrides section with:

```markdown
## Protocol Overrides

The observer writes ONLY to these targets:
- `.claude/agent-memory/<agent>/MEMORY.md` (## Pending Review section)
- `.claude/skills/<skill>/insights.md` (## Pending Review section)
- `.claude/PLAYBOOK.md` (## Open section -- routing log, not a memory file)
- `.claude/logs/observations/<project>/rejections.jsonl`
- `.claude/logs/observations/<project>/.observer-cursor`
```

Changes from current:
- Removed the paragraph about agent-protocols project-memories instruction (agent-protocols no longer has that instruction after Plan 01)
- Removed "Do NOT write to project-memories/. Do NOT read or reference signals.yaml." (moot after agent-protocols rewrite)
- Changed PLAYBOOK.md line from `## Pending Review section` to `## Open section -- routing log, not a memory file`
- Kept the 5-item write target list as the authoritative reference

**Edit 2: Update YAML frontmatter description (lines 4-7)**

Change the description to mention PLAYBOOK routing:

```yaml
description: >-
  Reads obs.jsonl event logs from completed agent runs, extracts reusable
  learnings, classifies each to the correct memory tier via scope-test
  questions, writes tagged entries to Pending Review sections, and routes
  cross-agent insights through PLAYBOOK.md Open/Resolved lifecycle.
  Do NOT invoke manually -- dispatched by /evolve command only.
```

**Edit 3: Add PLAYBOOK routing branch to Step 8 (after the existing Writing procedure, around line 186)**

After the current Writing procedure (steps 1-5 about Read target -> find ## Pending Review -> Edit append -> read-back verify), add this PLAYBOOK-specific branch:

```markdown
**PLAYBOOK.md routing (Q3 pass targets only):**

When a candidate's scope-test Q3 passes (coordination insight), handle differently from MEMORY.md/insights.md targets:

1. Write the entry to `.claude/PLAYBOOK.md` under `## Open`:
   `- [CONFIDENCE] source-agent: distilled insight text (YYYY-MM-DDThh:mm)`

2. Identify the routing target:
   - If the insight names a specific agent -> route to `.claude/agent-memory/<agent>/MEMORY.md` ## Pending Review
   - If the insight names a specific skill -> route to `.claude/skills/<skill>/insights.md` ## Pending Review
   - If the target is unclear -> leave the entry in ## Open for manual routing via /evolve. Skip steps 3-4.

3. Write the insight to the routing target's ## Pending Review section using the standard format for that target type (MEMORY.md format or insights.md format).

4. Update the PLAYBOOK entry from Open format to Resolved format:
   `- [Resolved] source-agent: insight text -> routed to <target_path> (YYYY-MM-DD)`
   Use the Edit tool to replace the Open entry with the Resolved entry, then move it from ## Open to ## Resolved.

This completes in one observer pass -- no separate /evolve interaction needed for PLAYBOOK entries.
```

**Edit 4: Update Guardrail #5 (target file integrity, around line 293)**

Add PLAYBOOK.md awareness to the existing guardrail. After the current text about checking `## Pending Review` heading, add:

```markdown
For PLAYBOOK.md targets, check for `## Open` heading instead of `## Pending Review`. If `## Open` is absent, skip with "target-file-corrupt".
```

**Edit 5: Update Step 7 dedup reference (line 147)**

The current text says "Consider both ## Pending Review and ## Permanent sections". Change to:

```markdown
Consider both ## Pending Review and ## Permanent sections (or ## Open and ## Resolved for PLAYBOOK.md).
```

Do NOT change the 10-step pipeline structure. The routing logic is an extension of Step 8, not a new step. The step count stays at 10.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.claude/agents/observer.md','utf8'); const checks=[f.includes('## Open section'), f.includes('routing log'), f.includes('PLAYBOOK.md routing'), f.includes('[Resolved]'), f.includes('route to'), !f.includes('Do NOT write to \x60project-memories'), !f.includes('Do NOT read or reference \x60signals.yaml'), f.includes('cross-agent insights through PLAYBOOK.md')]; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: '+checks.map((c,i)=>c?'ok':'FAIL@'+i).join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - `observer.md` contains `## Open section` in Protocol Overrides write target list
    - `observer.md` contains `routing log` describing PLAYBOOK.md purpose
    - `observer.md` contains `PLAYBOOK.md routing` as a subsection or label in Step 8
    - `observer.md` contains `[Resolved]` format string for resolved entries
    - `observer.md` contains `route to` describing the routing action
    - `observer.md` does NOT contain `Do NOT write to \`project-memories` (moot after agent-protocols rewrite)
    - `observer.md` does NOT contain `Do NOT read or reference \`signals.yaml` (moot after agent-protocols rewrite)
    - `observer.md` YAML description contains `cross-agent insights through PLAYBOOK.md`
    - `observer.md` still contains all 10 `### Step` headings (pipeline structure preserved)
    - `observer.md` Guardrail #5 mentions `## Open` for PLAYBOOK targets
  </acceptance_criteria>
  <done>Observer.md updated with PLAYBOOK routing branch in Step 8, simplified Protocol Overrides, updated description, and PLAYBOOK-aware guardrail. 10-step pipeline structure preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Fix obs-summarize.js old path references</name>
  <files>.claude/scripts/obs-summarize.js</files>
  <read_first>
    - .claude/scripts/obs-summarize.js (221 lines -- old .claude/logs/runs/ references at line 9 comment and line 39 resolveRunFile function)
    - .planning/phases/04-agent-consumption/04-CONTEXT.md (D-17: verify and clean up old path references)
    - .planning/phases/04-agent-consumption/04-RESEARCH.md (obs-summarize.js Disposition section -- update paths, don't delete)
  </read_first>
  <action>
Update obs-summarize.js to reference the new obs.jsonl path instead of the old .claude/logs/runs/ path. Per RESEARCH.md assumption A1, this is a path update -- not a rewrite or deletion.

**Edit 1: Update header comment (around line 8-9)**

Change:
```javascript
// matches uniquely one file in .claude/logs/runs/, summarizes that. When
```
To:
```javascript
// matches uniquely one file in .claude/logs/observations/<project>/obs.jsonl, summarizes that. When
```

**Edit 2: Update resolveRunFile function (around line 38-49)**

Change the `runsDir` path resolution from:
```javascript
const runsDir = path.resolve(process.env.CLAUDE_PROJECT_DIR || '.', '.claude', 'logs', 'runs');
```
To:
```javascript
const obsDir = path.resolve(process.env.CLAUDE_PROJECT_DIR || '.', '.claude', 'logs', 'observations');
```

Update the function to work with the observations directory structure:
- When no arg: find the most recent project subdirectory in observations/ and use its obs.jsonl
- When given an arg that is a file path: use it directly (unchanged)
- When given a project name prefix: look for matching subdirectory in observations/

The variable name changes from `runsDir` to `obsDir` throughout this function. The file matching changes from `*.jsonl` files in a flat directory to `*/obs.jsonl` in project subdirectories.

Rename the function from `resolveRunFile` to `resolveObsFile` and update the call site (around line 55-60 where it's invoked).

**Edit 3: Update any remaining `.claude/logs/runs/` string references**

Search the entire file for `logs/runs` and replace all occurrences with the appropriate `logs/observations` reference. This includes error messages, comments, and variable names containing "run" that specifically reference the old path.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.claude/scripts/obs-summarize.js','utf8'); const checks=[!f.includes('logs/runs'), f.includes('logs/observations'), !f.includes('resolveRunFile'), f.includes('resolveObsFile') || f.includes('obsDir')]; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: '+checks.map((c,i)=>c?'ok':'FAIL@'+i).join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - `obs-summarize.js` does NOT contain `logs/runs` (old path fully removed)
    - `obs-summarize.js` contains `logs/observations` (new path in use)
    - `obs-summarize.js` does NOT contain `resolveRunFile` (renamed to `resolveObsFile` or equivalent)
    - `node -c .claude/scripts/obs-summarize.js` exits 0 (syntax valid)
  </acceptance_criteria>
  <done>obs-summarize.js updated from .claude/logs/runs/ to .claude/logs/observations/ path. Function renamed. All old path references eliminated. File syntax valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Observer -> PLAYBOOK.md | Observer gains write access to Open/Resolved sections |
| Observer -> target MEMORY.md/insights.md | Observer routes PLAYBOOK entries to target files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Observer routing | accept | Observer is dispatched by /evolve only, no untrusted input. Routing targets are filesystem paths within .claude/ directory. Misrouting is a quality issue, not a security issue. |
| T-04-04 | Denial of Service | Observer prompt bloat | mitigate | PLAYBOOK routing is a compact branch within Step 8, not a new step. Core 10-step pipeline preserved. Routing logic adds ~20 lines to a 320-line prompt. |
</threat_model>

<verification>
1. observer.md contains PLAYBOOK routing branch in Step 8 with Open/Resolved lifecycle
2. observer.md Protocol Overrides references `## Open section` not `## Pending Review section`
3. observer.md still has all 10 Step headings (pipeline structure preserved)
4. obs-summarize.js has zero `logs/runs` references, uses `logs/observations` path
5. obs-summarize.js passes syntax check (`node -c`)
</verification>

<success_criteria>
- Observer can route Q3 scope-test passes through PLAYBOOK Open -> target -> Resolved in one pass
- Observer prompt remains at 10 steps with routing as Step 8 extension
- Protocol Overrides simplified (no references to dead agent-protocols behaviors)
- obs-summarize.js references only the new observation path
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-consumption/04-02-SUMMARY.md`
</output>
