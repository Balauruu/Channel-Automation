---
phase: 04-agent-consumption
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - .claude/skills/agent-observability/SKILL.md
autonomous: true
requirements: [MEML-05, MEML-06]
must_haves:
  truths:
    - "agent-observability SKILL.md comprehensively documents the full observation pipeline: capture, observer, evolve, PLAYBOOK routing, scope tests, and debug recipes"
    - "agent-observability references only obs.jsonl path (.claude/logs/observations/<project>/obs.jsonl), with zero references to old .claude/logs/runs/ or obs.js"
    - "agent-observability includes the 3-layer scope-test questions verbatim"
    - "agent-observability triggers on both debugging queries AND system understanding queries"
    - "No file in .claude/ contains stale references to obs.js, logs/runs, signals.yaml, or project-memories"
  artifacts:
    - path: ".claude/skills/agent-observability/SKILL.md"
      provides: "Comprehensive observation pipeline reference (merged pipeline-learning scope)"
      min_lines: 200
      contains: "logs/observations"
  key_links:
    - from: ".claude/skills/agent-observability/SKILL.md"
      to: ".claude/hooks/pipeline-observe.js"
      via: "documents event schema from pipeline-observe.js"
      pattern: "pipeline-observe.js"
    - from: ".claude/skills/agent-observability/SKILL.md"
      to: ".claude/agents/observer.md"
      via: "documents observer 10-step pipeline"
      pattern: "scope-test"
    - from: ".claude/skills/agent-observability/SKILL.md"
      to: ".claude/skills/evolve/SKILL.md"
      via: "documents /evolve command flow"
      pattern: "/evolve"
---

<objective>
Comprehensively rewrite agent-observability SKILL.md to cover the full observation pipeline (merging pipeline-learning scope per D-11), then verify no stale references remain anywhere in .claude/.

Purpose: The current 305-line agent-observability skill documents the dead obs.js system. It must be completely replaced with documentation of the live pipeline: pipeline-observe.js capture, observer classification, /evolve flow, PLAYBOOK routing, scope tests, and debug recipes. This single skill satisfies both MEML-05 (agent-observability rewrite) and MEML-06 (pipeline-learning content -- merged per D-11).

Output: Rewritten `.claude/skills/agent-observability/SKILL.md` (~250-300 lines), clean stale-reference audit
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-consumption/04-CONTEXT.md
@.planning/phases/04-agent-consumption/04-PATTERNS.md
@.planning/phases/04-agent-consumption/04-RESEARCH.md
@.planning/phases/04-agent-consumption/04-01-SUMMARY.md
@.planning/phases/04-agent-consumption/04-02-SUMMARY.md

<interfaces>
<!-- pipeline-observe.js event schema (source of truth) -->
Base fields on every event:
- ts: ISO timestamp with colons replaced (Windows filename safe)
- epoch_ms: Unix milliseconds for duration computation
- event: event type string (tool_pre, tool_post, tool_fail, permission_denied, dispatch, assistant_message, complete)
- session_id: Claude Code session ID
- agent_id: empty string for main conversation, populated for subagents
- project: project slug from path detection

Truncation caps:
- Read/Grep/Glob: 1KB input, 1KB output
- Bash/Write/Edit: 5KB input, 5KB output
- Agent: 2KB input, 5KB output
- Thinking: 10KB per turn
- Text: 10KB per turn
- Dispatch prompt: 2KB

<!-- Scope-test questions (from observer.md) -->
Q1: "Does this change how a specific skill or method runs?" -> .claude/skills/<skill>/insights.md
Q2: "Would a fresh instance of this agent need this to do its job?" -> .claude/agent-memory/<agent>/MEMORY.md
Q3: "Does this change how agents hand off or coordinate?" -> .claude/PLAYBOOK.md

<!-- /evolve flow (from evolve SKILL.md) -->
1. Dispatch @observer to process new events
2. Scan all memory files for Pending Review entries (evolve.js scan)
3. Auto-promote all to Permanent (evolve.js promote)
4. Display grouped numbered summary
5. Offer revert by number

<!-- PLAYBOOK routing (from updated observer.md) -->
Q3 pass -> write to PLAYBOOK ## Open -> route to target MEMORY.md/insights.md -> mark Resolved
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite agent-observability SKILL.md with comprehensive pipeline documentation</name>
  <files>.claude/skills/agent-observability/SKILL.md</files>
  <read_first>
    - .claude/skills/agent-observability/SKILL.md (current 305-line file documenting dead obs.js system -- complete replacement)
    - .claude/hooks/pipeline-observe.js (event schema source of truth -- read lines 1-50 for constants, truncation caps, base fields)
    - .claude/agents/observer.md (10-step pipeline, scope-test questions -- read lines 100-230 for processing steps and scope tests)
    - .claude/skills/evolve/SKILL.md (evolve command flow -- read lines 1-50 for step overview)
    - .claude/PLAYBOOK.md (to confirm Open/Resolved structure from Plan 01)
    - .planning/phases/04-agent-consumption/04-CONTEXT.md (D-11 through D-16 define the rewrite scope)
    - .planning/phases/04-agent-consumption/04-PATTERNS.md (pipeline-design SKILL.md as structural analog, event schema details, debug recipe pattern)
  </read_first>
  <action>
Replace the entire contents of `.claude/skills/agent-observability/SKILL.md` with a comprehensive ~250-300 line document covering the full observation pipeline. The file must have 8 sections per D-12, with a broadened trigger per D-13.

**YAML Frontmatter:**

```yaml
---
name: agent-observability
description: >-
  Observation pipeline reference. Use when debugging agent runs ("why did
  agent X produce wrong output", "which tool call was slow", "what was
  denied"), understanding the memory system ("how does the observer work",
  "what does /evolve do", "how are learnings classified"), or querying
  obs.jsonl event logs. Covers capture, observer, evolve, PLAYBOOK routing,
  scope tests, and debug recipes.
user-invocable: true
---
```

**Section 1: Overview**

```markdown
# Agent Observability

The observation pipeline captures every tool call, agent dispatch, and reasoning turn to `.claude/logs/observations/<project>/obs.jsonl`, then provides tools to analyze, extract learnings, and route insights to the correct memory tier.

Components:
1. **pipeline-observe.js** -- PostToolUse/SubagentStop hook that writes events to obs.jsonl
2. **@observer agent** -- Sonnet-class subagent that reads obs.jsonl, extracts learnings, classifies via scope tests
3. **/evolve command** -- User-invoked skill that dispatches observer and manages memory promotion
4. **PLAYBOOK.md** -- Observer-managed routing log for cross-agent coordination insights
```

**Section 2: When to Use**

```markdown
## When to Use

**Debugging (post-mortem on agent runs):**
- "Why did @researcher return X instead of Y?"
- "Which tool call took the most time in that run?"
- "What permission was denied and why did the agent stop?"
- "What was the agent reasoning about before calling that tool?"

**System understanding (how the memory pipeline works):**
- "How does the observer classify learnings?"
- "What does /evolve do?"
- "How are insights routed through PLAYBOOK.md?"
- "What are the scope-test questions?"

**Not for:** Direct memory file edits (use /evolve), pipeline-observe.js code changes (use pipeline-design skill).
```

**Section 3: Event Schema**

Document the obs.jsonl event schema from pipeline-observe.js. Include:
- File path: `.claude/logs/observations/<project>/obs.jsonl`
- Format: one JSON object per line (JSONL)
- Rotation: 10MB with timestamped archive, 30-day purge
- Base fields table: `ts`, `epoch_ms`, `event`, `session_id`, `agent_id`, `project`
- Event types table: `tool_pre`, `tool_post`, `tool_fail`, `permission_denied`, `dispatch`, `assistant_message`, `complete` -- with "When emitted" column
- Truncation caps table: tool name -> input cap -> output cap (Read/Grep/Glob: 1KB/1KB, Bash/Write/Edit: 5KB/5KB, Agent: 2KB/5KB, Thinking: 10KB/turn, Text: 10KB/turn, Dispatch prompt: 2KB)
- `agent_id` semantics: empty string = main conversation, populated = subagent dispatch

Use markdown tables for all three sub-sections (base fields, event types, truncation caps).

**Section 4: Observer System**

```markdown
## Observer System

The `@observer` agent (Sonnet, dispatched by /evolve only) processes obs.jsonl incrementally via a 10-step pipeline:

1. **Load cursor** -- reads byte offset from `.observer-cursor`
2. **Read events** -- reads obs.jsonl from cursor position
3. **Group by run** -- segments events by agent_id
4. **Filter self** -- skips runs where agent_id contains "observer"
5. **Classify candidates** -- applies scope-test questions (see below)
6. **Score confidence** -- assigns [HIGH], [MED], or [LOW]
7. **Deduplicate** -- checks target files for existing entries
8. **Write entries** -- appends to target file's Pending Review (or PLAYBOOK Open for Q3)
9. **Log rejections** -- writes rejected candidates to rejections.jsonl
10. **Update cursor** -- advances byte offset after successful writes

Maximum 3 candidates per run. Exactly one scope-test question must pass per candidate.
```

**Section 5: /evolve Command**

```markdown
## /evolve Command

User-invoked skill that orchestrates the learning cycle:

1. Dispatch @observer to process new obs.jsonl events
2. Scan all memory files for ## Pending Review entries (`evolve.js scan`)
3. Auto-promote all entries to ## Permanent (`evolve.js promote`)
4. Display grouped numbered summary (insights files first, then MEMORY files, then PLAYBOOK)
5. Offer revert by number -- user can undo specific promotions
6. Commit changes

PLAYBOOK.md is excluded from evolve.js scan/promote (its Open/Resolved lifecycle is managed by the observer, not /evolve).

Helper script: `.claude/scripts/memory/evolve.js` with subcommands: `scan`, `promote <file> <entry>`, `revert <file> <entry>`
```

**Section 6: PLAYBOOK Routing**

```markdown
## PLAYBOOK Routing

`.claude/PLAYBOOK.md` is an observer-managed routing log for cross-agent coordination insights (scope-test Q3 passes). Agents do not read or write it directly.

**Lifecycle:**
1. Observer writes entry to `## Open`: `- [CONF] agent: insight (timestamp)`
2. Observer identifies routing target (specific agent MEMORY.md or skill insights.md)
3. Observer writes insight to target file's `## Pending Review`
4. Observer updates PLAYBOOK entry to `## Resolved`: `- [Resolved] agent: insight -> routed to <path> (date)`

If the routing target is unclear, the entry remains in ## Open for manual review during /evolve.
```

**Section 7: 3-Layer Scope Tests**

```markdown
## 3-Layer Scope Tests

Every candidate must pass exactly one of these three questions:

| # | Question | YES means | Target |
|---|----------|-----------|--------|
| Q1 | Does this change how a specific skill or method runs? | Tool technique, library pattern, script convention, procedural step | `.claude/skills/<skill>/insights.md` |
| Q2 | Would a fresh instance of this agent need this to do its job? | Agent behavior, decision, cross-task pattern | `.claude/agent-memory/<agent>/MEMORY.md` |
| Q3 | Does this change how agents hand off or coordinate? | Inter-agent protocol, handoff, resource conflict, workflow sequencing | `.claude/PLAYBOOK.md` (routed to target) |

Zero passes = reject ("no-scope-match"). Multiple passes = reject ("ambiguous-scope").
```

**Section 8: Debug Recipes**

Raw JSONL one-liners per D-16. No obs-summarize.js dependency. All recipes use the obs.jsonl path. Include these recipes:

```markdown
## Debug Recipes

Direct obs.jsonl queries. Replace `<project>` with project slug.

**List all runs (unique agent_ids):**
```bash
node -e "
const lines = require('fs').readFileSync('.claude/logs/observations/<project>/obs.jsonl','utf8')
  .trim().split('\n').map(JSON.parse);
const ids = [...new Set(lines.map(e => e.agent_id || '(main)'))];
ids.forEach(id => console.log(id));
"
```

**Find slowest tool calls in a run:**
```bash
node -e "
const lines = require('fs').readFileSync('.claude/logs/observations/<project>/obs.jsonl','utf8')
  .trim().split('\n').map(JSON.parse);
lines.filter(e => e.event === 'tool_post' && e.agent_id === '<agent-id>')
  .sort((a,b) => (b.duration_ms||0) - (a.duration_ms||0))
  .slice(0, 5)
  .forEach(e => console.log(e.duration_ms+'ms', e.tool_name, (e.input||'').slice(0,80)));
"
```

**Find all failures:**
```bash
node -e "
const lines = require('fs').readFileSync('.claude/logs/observations/<project>/obs.jsonl','utf8')
  .trim().split('\n').map(JSON.parse);
lines.filter(e => e.event === 'tool_fail')
  .forEach(e => console.log(e.ts, e.agent_id||'(main)', e.tool_name, (e.error||'').slice(0,120)));
"
```

**Find permission denials:**
```bash
node -e "
const lines = require('fs').readFileSync('.claude/logs/observations/<project>/obs.jsonl','utf8')
  .trim().split('\n').map(JSON.parse);
lines.filter(e => e.event === 'permission_denied')
  .forEach(e => console.log(e.ts, e.agent_id||'(main)', e.tool_name));
"
```

**Filter events by agent:**
```bash
node -e "
const lines = require('fs').readFileSync('.claude/logs/observations/<project>/obs.jsonl','utf8')
  .trim().split('\n').map(JSON.parse);
lines.filter(e => e.agent_id === '<agent-id>')
  .forEach(e => console.log(e.ts, e.event, e.tool_name || ''));
"
```

**Get agent reasoning timeline:**
```bash
node -e "
const lines = require('fs').readFileSync('.claude/logs/observations/<project>/obs.jsonl','utf8')
  .trim().split('\n').map(JSON.parse);
lines.filter(e => e.event === 'assistant_message' && e.agent_id === '<agent-id>')
  .forEach(e => console.log(e.ts, (e.text||'').slice(0,120)));
"
```
```

That completes the 8 sections. The file should be ~250-300 lines. Do NOT include any references to:
- `.claude/logs/runs/` (old path -- D-14)
- `obs.js` (old hook -- D-14)
- `obs-summarize.js` (not referenced per D-16 -- raw recipes only)
- `signals.yaml` (dead system)
- `project-memories/` (dead system)
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.claude/skills/agent-observability/SKILL.md','utf8'); const lines=f.trim().split('\n').length; const checks=[lines>=200, lines<=350, f.includes('logs/observations'), !f.includes('logs/runs'), !f.includes('obs.js'), !f.includes('obs-summarize'), !f.includes('signals.yaml'), !f.includes('project-memories'), f.includes('## Event Schema') || f.includes('## Event') || f.includes('event schema'), f.includes('## Observer System') || f.includes('observer system'), f.includes('## Debug Recipes') || f.includes('## Debug'), f.includes('scope-test') || f.includes('Scope Test'), f.includes('/evolve'), f.includes('PLAYBOOK'), f.includes('Q1') && f.includes('Q2') && f.includes('Q3')]; console.log(checks.every(Boolean) ? 'PASS ('+lines+' lines)' : 'FAIL: '+checks.map((c,i)=>c?'ok':'FAIL@'+i).join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/agent-observability/SKILL.md` is between 200 and 350 lines
    - File contains `logs/observations` (new obs.jsonl path)
    - File does NOT contain `logs/runs` (old path fully removed)
    - File does NOT contain `obs.js` (old hook removed)
    - File does NOT contain `obs-summarize` (no summarizer dependency per D-16)
    - File does NOT contain `signals.yaml` (dead system)
    - File does NOT contain `project-memories` (dead system)
    - File contains sections covering: event schema, observer system, debug recipes, scope tests, /evolve, PLAYBOOK routing
    - File contains all three scope-test questions (Q1, Q2, Q3)
    - File YAML frontmatter contains broadened trigger phrases covering both debugging AND system understanding (per D-13)
    - File contains `user-invocable: true`
  </acceptance_criteria>
  <done>agent-observability SKILL.md comprehensively rewritten with 8 sections covering the full observation pipeline. Merges pipeline-learning scope (MEML-06). Zero stale references.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no stale references remain across .claude/ directory</name>
  <files></files>
  <read_first>
    - .claude/skills/agent-protocols/SKILL.md (confirm Plan 01 rewrite is clean)
    - .claude/PLAYBOOK.md (confirm Plan 01 redesign is clean)
    - .claude/agents/observer.md (confirm Plan 02 update is clean)
    - .claude/scripts/obs-summarize.js (confirm Plan 02 path fix is clean)
    - .claude/skills/agent-observability/SKILL.md (confirm Task 1 rewrite is clean)
  </read_first>
  <action>
Run a comprehensive grep audit for stale references across all .claude/ files (per D-17). Check for each of these patterns:

1. `logs/runs` -- old observation path (should only appear in git history, not working tree)
2. `obs.js` -- old hook filename (should not appear anywhere; pipeline-observe.js is the replacement)
3. `signals.yaml` -- dead feedback system
4. `project-memories` -- dead project-scoped notes system
5. `check-definition-signals` -- old hook that was deleted

Run these grep commands:

```bash
grep -r "logs/runs" .claude/ --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" -l
grep -r "obs\.js" .claude/ --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" -l
grep -r "signals\.yaml" .claude/ --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" -l
grep -r "project-memories" .claude/ --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" -l
grep -r "check-definition-signals" .claude/ --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" -l
```

**Expected result:** All five grep commands return no matches (exit code 1, no output).

**If any matches found:** Evaluate each match:
- If it's in a file modified by Phase 4 plans (agent-protocols, agent-observability, PLAYBOOK, observer.md, obs-summarize.js) -- this is a bug in the earlier task. Report it.
- If it's in a file NOT in Phase 4 scope (e.g., agent MEMORY.md files containing promoted insights that reference old paths) -- document but do not modify. These are historical entries that will age out via memory lifecycle (Phase 5).
- If it's in `.claude/agent-memory/*/MEMORY.md` or `.claude/skills/*/insights.md` as a historical entry -- acceptable, not a stale reference.

Document the audit results. If all five patterns return clean, the phase cleanup is complete.

Also verify evolve.js scan excludes PLAYBOOK:
```bash
node .claude/scripts/memory/evolve.js scan
```
The output should list memory files with Pending Review entries but NOT include PLAYBOOK.md.
  </action>
  <verify>
    <automated>node -e "const {execSync}=require('child_process'); const patterns=['logs/runs','obs[.]js','signals[.]yaml','project-memories','check-definition-signals']; const results=patterns.map(p=>{try{const out=execSync('grep -r \"'+p+'\" .claude/ --include=\"*.md\" --include=\"*.js\" --include=\"*.json\" --include=\"*.yaml\" -l 2>/dev/null',{encoding:'utf8'});return{p,files:out.trim().split('\\n').filter(f=>!f.includes('agent-memory')&&!f.includes('insights.md')&&!f.includes('MEMORY.md')&&!f.includes('project-memories/'))}}catch{return{p,files:[]}}}); const stale=results.filter(r=>r.files.length>0); console.log(stale.length===0?'PASS: no stale references':'FAIL: '+JSON.stringify(stale))"</automated>
  </verify>
  <acceptance_criteria>
    - Grep for `logs/runs` across `.claude/` returns no matches in Phase 4 modified files
    - Grep for `obs.js` across `.claude/` returns no matches (excluding git-tracked deletions)
    - Grep for `signals.yaml` across `.claude/` returns no matches in Phase 4 modified files
    - Grep for `project-memories` across `.claude/` returns no matches in Phase 4 modified files
    - Grep for `check-definition-signals` across `.claude/` returns no matches
    - `node .claude/scripts/memory/evolve.js scan` output does not contain "PLAYBOOK"
    - Any matches in historical memory entries (agent MEMORY.md, insights.md) are documented as acceptable historical data
  </acceptance_criteria>
  <done>Comprehensive stale-reference audit passes. All Phase 4 modified files are clean. evolve.js scan excludes PLAYBOOK. Phase 4 cleanup complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill documentation -> agent behavior | agent-observability is a reference doc; incorrect documentation could mislead debugging but has no runtime effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | Debug recipes in skill | accept | Debug recipes operate on local obs.jsonl files. No network access, no secrets exposed. Recipes are read-only queries. |
| T-04-06 | Tampering | Stale path references | mitigate | Task 2 comprehensive grep audit catches any surviving old-system references across all .claude/ files. |
</threat_model>

<verification>
1. agent-observability SKILL.md is 200-350 lines with 8 sections covering the full pipeline
2. Zero references to old system (logs/runs, obs.js, obs-summarize, signals.yaml, project-memories)
3. Scope-test questions Q1/Q2/Q3 present verbatim
4. Debug recipes use obs.jsonl path exclusively
5. Comprehensive grep audit finds no stale references in Phase 4 modified files
6. evolve.js scan excludes PLAYBOOK.md
</verification>

<success_criteria>
- agent-observability SKILL.md is a comprehensive single reference for the entire observation pipeline
- MEML-05 (observability rewrite) and MEML-06 (pipeline-learning content, merged) both satisfied
- D-17 cleanup verified: no stale old-system references survive in .claude/ directory
- Phase 4 is complete with all 4 requirements (MEML-03, MEML-04, MEML-05, MEML-06) delivered
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-consumption/04-03-SUMMARY.md`
</output>
