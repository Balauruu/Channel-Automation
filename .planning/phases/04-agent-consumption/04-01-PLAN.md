---
phase: 04-agent-consumption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/PLAYBOOK.md
  - .claude/skills/agent-protocols/SKILL.md
autonomous: true
requirements: [MEML-03, MEML-04]
must_haves:
  truths:
    - "PLAYBOOK.md has ## Open and ## Resolved sections with no ## Pending Review or ## Permanent headings"
    - "agent-protocols SKILL.md is ~20 lines with no signals.yaml, project-memories, scratchpad, or section guide references"
    - "agent-protocols instructs agents to treat MEMORY.md as read-only context"
    - "PLAYBOOK.md is excluded from evolve.js scan output (Open/Resolved headings do not match Pending Review/Permanent)"
  artifacts:
    - path: ".claude/PLAYBOOK.md"
      provides: "Cross-agent routing log with Open/Resolved lifecycle"
      contains: "## Open"
    - path: ".claude/skills/agent-protocols/SKILL.md"
      provides: "Ultra-thin behavioral protocol for passive memory consumers"
      min_lines: 15
  key_links:
    - from: ".claude/PLAYBOOK.md"
      to: ".claude/scripts/memory/evolve.js"
      via: "section heading mismatch (intentional exclusion)"
      pattern: "## Open"
---

<objective>
Redesign PLAYBOOK.md as an observer-managed routing log with Open/Resolved lifecycle, and rewrite agent-protocols to an ultra-thin (~20 line) read-only consumption protocol.

Purpose: PLAYBOOK.md must transition from a memory file (Pending Review/Permanent) to a routing log (Open/Resolved) so the observer can manage cross-agent insight staging and routing. Agent-protocols must be stripped of all dead references (signals.yaml, project-memories, scratchpad, section guide) and rewritten to reflect the new observer-only-writes model where agents are pure memory consumers.

Output: Redesigned `.claude/PLAYBOOK.md`, rewritten `.claude/skills/agent-protocols/SKILL.md`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-consumption/04-CONTEXT.md
@.planning/phases/04-agent-consumption/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign PLAYBOOK.md as Open/Resolved routing log</name>
  <files>.claude/PLAYBOOK.md</files>
  <read_first>
    - .claude/PLAYBOOK.md (current 10-line file with empty Pending Review/Permanent sections)
    - .planning/phases/04-agent-consumption/04-CONTEXT.md (D-06 through D-09 define the lifecycle)
    - .planning/phases/04-agent-consumption/04-PATTERNS.md (entry format conventions section)
  </read_first>
  <action>
Replace the entire contents of `.claude/PLAYBOOK.md` with the new routing log structure per D-06, D-07, D-08, D-09.

The new file must contain exactly this content:

```markdown
# Playbook

Cross-agent coordination routing log. The observer writes new entries to Open
when a scope-test Q3 passes, routes each to the target agent's MEMORY.md or
skill's insights.md, then marks it Resolved. This file is managed exclusively
by the observer -- agents do not read or write it directly.

## Open

## Resolved

```

Key requirements:
- Title stays `# Playbook` (matches existing convention)
- Boilerplate describes the routing log model: observer writes to Open, routes to target, marks Resolved (per D-07)
- States "managed exclusively by the observer -- agents do not read or write it directly" (per D-02)
- `## Open` replaces `## Pending Review` (per D-06)
- `## Resolved` replaces `## Permanent` (per D-06)
- Both sections are empty (current PLAYBOOK has no entries to migrate -- verified in RESEARCH.md)
- No trailing content after `## Resolved` except a blank line

The Open entry format (documented here for observer reference, not written in the file) is:
`- [CONF] source-agent: insight text (YYYY-MM-DDThh:mm)`

The Resolved entry format (per D-09) is:
`- [Resolved] agent: insight text -> routed to .claude/agent-memory/agent/MEMORY.md (YYYY-MM-DD)`
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.claude/PLAYBOOK.md','utf8'); const checks=[f.includes('## Open'), f.includes('## Resolved'), !f.includes('## Pending Review'), !f.includes('## Permanent'), f.includes('managed exclusively by the observer'), f.includes('agents do not read or write it directly')]; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: '+checks.map((c,i)=>c?'ok':'FAIL@'+i).join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/PLAYBOOK.md` contains `## Open` heading
    - `.claude/PLAYBOOK.md` contains `## Resolved` heading
    - `.claude/PLAYBOOK.md` does NOT contain `## Pending Review`
    - `.claude/PLAYBOOK.md` does NOT contain `## Permanent`
    - `.claude/PLAYBOOK.md` contains `managed exclusively by the observer`
    - `.claude/PLAYBOOK.md` contains `agents do not read or write it directly`
    - `node .claude/scripts/memory/evolve.js scan` output does NOT contain "PLAYBOOK" (intentional exclusion via heading mismatch)
  </acceptance_criteria>
  <done>PLAYBOOK.md redesigned with Open/Resolved sections. evolve.js scan confirms PLAYBOOK is excluded from promote cycle.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite agent-protocols SKILL.md to ultra-thin read-only protocol</name>
  <files>.claude/skills/agent-protocols/SKILL.md</files>
  <read_first>
    - .claude/skills/agent-protocols/SKILL.md (current 114-line file with dead references)
    - .planning/phases/04-agent-consumption/04-CONTEXT.md (D-01 through D-05 define the rewrite scope)
    - .planning/phases/04-agent-consumption/04-PATTERNS.md (YAML frontmatter pattern, section conventions)
  </read_first>
  <action>
Replace the entire contents of `.claude/skills/agent-protocols/SKILL.md` with the ultra-thin protocol per D-03. The complete new file content:

```markdown
---
name: agent-protocols
description: >-
  Shared behavioral protocols for all pipeline agents. Memory is auto-injected
  and read-only. Observer handles all memory writes. Not user-invocable.
user-invocable: false
---

# Agent Protocols

## Memory

Your MEMORY.md is auto-injected into your context at task start (first
200 lines). Treat it as read-only reference -- you do not write to memory
files. The observer system handles all memory updates after your runs.

Entries tagged [HIGH], [MED], or [LOW] are confidence levels from the
observer. Untagged entries are legacy. Both permanent and pending entries
are visible to you.

## Project Context

Read ./CLAUDE.md at the start of every task for project-wide rules,
agent reference table, folder map, and architecture constraints.
```

Key requirements per locked decisions:
- D-01: No writing instructions. Agents NEVER write to MEMORY.md or insights.md.
- D-02: No PLAYBOOK.md reading instruction. Agents are unaware of PLAYBOOK.
- D-03: Two sections only: Memory and Project Context. ~20 lines.
- D-04: No section guide (agents don't need to know MEMORY.md section structure).
- D-05: Both permanent and pending entries are visible. No hiding mechanism.
- YAML frontmatter: `name: agent-protocols`, `user-invocable: false`, updated description.
- Completely removed: Memory Lifecycle (At Task Start/During Work/At Task End), Section Guide, Project-Scoped Notes, Feedback Signal Protocol.
- Zero references to: `signals.yaml`, `project-memories/`, `scratchpad`, `append`, `write`.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.claude/skills/agent-protocols/SKILL.md','utf8'); const lines=f.trim().split('\n').length; const checks=[lines<=30, lines>=15, !f.includes('signals'), !f.includes('project-memories'), !f.includes('scratchpad'), !f.includes('Section Guide'), !f.includes('Feedback Signal'), f.includes('read-only'), f.includes('observer'), f.includes('## Memory'), f.includes('## Project Context'), f.includes('user-invocable: false')]; console.log(checks.every(Boolean) ? 'PASS ('+lines+' lines)' : 'FAIL: '+checks.map((c,i)=>c?'ok':'FAIL@'+i).join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/agent-protocols/SKILL.md` is between 15 and 30 lines
    - File contains `## Memory` and `## Project Context` headings
    - File contains `read-only` describing memory access
    - File contains `observer` referencing who handles writes
    - File contains `user-invocable: false` in YAML frontmatter
    - File does NOT contain `signals` (no signals.yaml reference)
    - File does NOT contain `project-memories` (dead system removed)
    - File does NOT contain `scratchpad` (dead concept removed)
    - File does NOT contain `Section Guide` (dropped per D-04)
    - File does NOT contain `Feedback Signal` (dead system removed)
    - File does NOT contain `Append` or `append` in write-instruction context
  </acceptance_criteria>
  <done>agent-protocols rewritten to ~20-line ultra-thin protocol. All dead references (signals.yaml, project-memories, scratchpad, section guide, feedback signals) eliminated. Agents are pure read-only memory consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Observer -> PLAYBOOK.md | Observer is sole writer; section headings are the contract |
| agent-protocols -> all agents | Injected into 12 agents; any write instruction would violate D-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | PLAYBOOK.md | accept | File is markdown in git; observer is sole writer; no untrusted input. Tampering requires git commit access which is trusted. |
| T-04-02 | Information Disclosure | agent-protocols | accept | No secrets in skill file. Read-only protocol contains no sensitive data. |
</threat_model>

<verification>
1. `node -e "..."` checks for PLAYBOOK.md section headings (Open/Resolved present, Pending Review/Permanent absent)
2. `node -e "..."` checks for agent-protocols line count, dead reference absence, read-only language
3. `node .claude/scripts/memory/evolve.js scan` confirms PLAYBOOK excluded from results
</verification>

<success_criteria>
- PLAYBOOK.md has Open/Resolved sections with routing log boilerplate
- agent-protocols is ~20 lines with read-only consumption model
- Both files have zero references to dead systems (signals.yaml, project-memories, obs.js, logs/runs)
- evolve.js scan excludes PLAYBOOK.md from results
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-consumption/04-01-SUMMARY.md`
</output>
