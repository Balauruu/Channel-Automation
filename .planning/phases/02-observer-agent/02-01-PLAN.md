---
phase: 02-observer-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/PLAYBOOK.md
  - .claude/agent-memory/observer/MEMORY.md
  - .claude/agent-memory/asset-curator/MEMORY.md
  - .claude/agent-memory/asset-processor/MEMORY.md
  - .claude/agent-memory/code-reviewer/MEMORY.md
  - .claude/agent-memory/compiler/MEMORY.md
  - .claude/agent-memory/editorial-lead/MEMORY.md
  - .claude/agent-memory/researcher/MEMORY.md
  - .claude/agent-memory/strategy/MEMORY.md
  - .claude/agent-memory/style-extractor/MEMORY.md
  - .claude/agent-memory/visual-planner/MEMORY.md
  - .claude/agent-memory/visual-researcher/MEMORY.md
  - .claude/agent-memory/writer/MEMORY.md
  - .claude/skills/archive-search/insights.md
  - .claude/skills/autoresearch/insights.md
  - .claude/skills/crawl4ai-scraping/insights.md
  - .claude/skills/data-analysis/insights.md
  - .claude/skills/media-evaluation/insights.md
  - .claude/skills/pipeline-design/insights.md
  - .claude/skills/structured-output/insights.md
  - .claude/skills/visual-narrative/insights.md
autonomous: true
requirements:
  - OBSV-04
  - MEML-01

must_haves:
  truths:
    - "PLAYBOOK.md exists at .claude/PLAYBOOK.md with Pending Review and Permanent sections"
    - "All 11 agent MEMORY.md files have a ## Pending Review section at the end"
    - "All 8 skills insights.md files have a ## Pending Review section after existing entries"
    - "Observer has its own MEMORY.md with key file paths"
  artifacts:
    - path: ".claude/PLAYBOOK.md"
      provides: "Orchestration-layer memory target for observer"
      contains: "## Pending Review"
    - path: ".claude/agent-memory/observer/MEMORY.md"
      provides: "Observer agent memory with key file references"
      contains: "## Key Files"
  key_links:
    - from: ".claude/PLAYBOOK.md"
      to: "observer.md (Plan 03)"
      via: "Observer writes orchestration insights to PLAYBOOK.md ## Pending Review"
      pattern: "## Pending Review"
    - from: ".claude/agent-memory/*/MEMORY.md"
      to: "observer.md (Plan 03)"
      via: "Observer writes agent-layer insights to MEMORY.md ## Pending Review"
      pattern: "## Pending Review"
---

<objective>
Bootstrap all write targets for the observer agent: create PLAYBOOK.md, add ## Pending Review sections to all 19 existing memory/insights files, and create the observer's own MEMORY.md.

Purpose: The observer needs these sections to exist BEFORE it runs. Without them, Edit calls targeting "## Pending Review" will fail or corrupt files (Pitfall 2 from RESEARCH.md). This plan creates the infrastructure the observer writes to.

Output: 20 modified/created files with ## Pending Review sections ready for observer writes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observer-agent/02-CONTEXT.md
@.planning/phases/02-observer-agent/02-PATTERNS.md

<interfaces>
<!-- Target file structures the executor needs to understand -->

MEMORY.md 5-section structure (from .claude/agent-memory/researcher/MEMORY.md):
```markdown
# <Agent> Memory

## Key Files
- ...

## Decisions

## Patterns

## Observations

## Open Questions

```
New ## Pending Review section appended AFTER ## Open Questions.

insights.md structure (from .claude/skills/autoresearch/insights.md):
```markdown
# Insights

Accumulated learnings from skill runs. Read at the start of every run.

## Lifecycle
- **Append:** One insight per run (never skip the reflection phase)
- **Merge:** Consolidate duplicates at 20+ entries
- **Promote:** Extract to SKILL.md when 3+ entries converge on same pattern

<!-- Append new insights below this line -->
- [YYYY-MM-DD] existing insight text...
```
New ## Pending Review section appended AFTER the last existing insight entry.

Entry format for MEMORY.md / PLAYBOOK.md (per D-04, MEML-01):
```
- [HIGH] agent-name: distilled insight text (YYYY-MM-DDThh:mm)
```

Entry format for insights.md (per D-04, MEML-01):
```
- [YYYY-MM-DD] [HIGH] distilled insight text (from: agent-name, YYYY-MM-DDThh:mm)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PLAYBOOK.md and observer MEMORY.md</name>
  <files>.claude/PLAYBOOK.md, .claude/agent-memory/observer/MEMORY.md</files>
  <read_first>
    - .claude/agent-memory/researcher/MEMORY.md (reference 5-section structure)
    - .planning/phases/02-observer-agent/02-CONTEXT.md (D-06: minimal bootstrap, D-07: location)
  </read_first>
  <action>
Create two new files:

**`.claude/PLAYBOOK.md`** (per D-06, D-07 -- minimal bootstrap with only Pending Review and Permanent sections):
```markdown
# Playbook

Cross-agent coordination insights. Observer writes new entries to Pending Review.
Promoted entries move to Permanent during /evolve review.

## Pending Review


## Permanent

```

**`.claude/agent-memory/observer/MEMORY.md`** (observer's own memory -- minimal, per RESEARCH.md recommendation):
```markdown
# Observer Memory

## Key Files
- Input: .claude/logs/observations/<project>/obs.jsonl
- Cursor: .claude/logs/observations/<project>/.observer-cursor
- Rejections: .claude/logs/observations/<project>/rejections.jsonl
- Write targets (agent): .claude/agent-memory/<agent>/MEMORY.md (## Pending Review)
- Write targets (skill): .claude/skills/<skill>/insights.md (## Pending Review)
- Write targets (orchestration): .claude/PLAYBOOK.md (## Pending Review)

## Decisions


## Patterns


## Observations


## Open Questions

```

Create the `observer` directory if it does not exist: `.claude/agent-memory/observer/`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p1=fs.readFileSync('.claude/PLAYBOOK.md','utf8'); const p2=fs.readFileSync('.claude/agent-memory/observer/MEMORY.md','utf8'); const ok1=p1.includes('## Pending Review') && p1.includes('## Permanent'); const ok2=p2.includes('## Key Files') && p2.includes('obs.jsonl'); console.log('PLAYBOOK:', ok1 ? 'PASS' : 'FAIL'); console.log('ObserverMem:', ok2 ? 'PASS' : 'FAIL'); if(!ok1||!ok2) process.exit(1);"</automated>
  </verify>
  <done>
    - .claude/PLAYBOOK.md exists and contains exactly two section headings: ## Pending Review and ## Permanent (NOT a copy of MEMORY.md 5-section structure per D-06)
    - .claude/agent-memory/observer/MEMORY.md exists with ## Key Files listing paths to obs.jsonl, .observer-cursor, rejections.jsonl, and all write targets
    - Observer MEMORY.md uses the standard 5-section structure (Key Files, Decisions, Patterns, Observations, Open Questions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ## Pending Review sections to all 19 target memory files</name>
  <files>
    .claude/agent-memory/asset-curator/MEMORY.md,
    .claude/agent-memory/asset-processor/MEMORY.md,
    .claude/agent-memory/code-reviewer/MEMORY.md,
    .claude/agent-memory/compiler/MEMORY.md,
    .claude/agent-memory/editorial-lead/MEMORY.md,
    .claude/agent-memory/researcher/MEMORY.md,
    .claude/agent-memory/strategy/MEMORY.md,
    .claude/agent-memory/style-extractor/MEMORY.md,
    .claude/agent-memory/visual-planner/MEMORY.md,
    .claude/agent-memory/visual-researcher/MEMORY.md,
    .claude/agent-memory/writer/MEMORY.md,
    .claude/skills/archive-search/insights.md,
    .claude/skills/autoresearch/insights.md,
    .claude/skills/crawl4ai-scraping/insights.md,
    .claude/skills/data-analysis/insights.md,
    .claude/skills/media-evaluation/insights.md,
    .claude/skills/pipeline-design/insights.md,
    .claude/skills/structured-output/insights.md,
    .claude/skills/visual-narrative/insights.md
  </files>
  <read_first>
    - .claude/agent-memory/researcher/MEMORY.md (see where ## Open Questions ends)
    - .claude/agent-memory/writer/MEMORY.md (populated MEMORY.md -- see content after last section)
    - .claude/skills/autoresearch/insights.md (insights.md with existing entries after marker comment)
    - .claude/skills/archive-search/insights.md (insights.md possibly empty after marker)
  </read_first>
  <action>
Add `## Pending Review` section to all 19 target files. Read each file first, then append the section.

**For 11 MEMORY.md files** (asset-curator, asset-processor, code-reviewer, compiler, editorial-lead, researcher, strategy, style-extractor, visual-planner, visual-researcher, writer):

Append after the last line of content (after `## Open Questions` section and any entries within it):
```markdown

## Pending Review

```
(blank line before heading, blank line after heading, trailing newline)

**For 8 insights.md files** (archive-search, autoresearch, crawl4ai-scraping, data-analysis, media-evaluation, pipeline-design, structured-output, visual-narrative):

Append after the last existing insight entry (below the `<!-- Append new insights below this line -->` marker and any existing `- [YYYY-MM-DD]` entries):
```markdown

## Pending Review

```
(blank line before heading, blank line after heading, trailing newline)

IMPORTANT: Do NOT modify any existing content in these files. Read each file fully, then append the new section at the end. Use Edit tool to append, not Write tool, to preserve existing content safely.

Process all 11 MEMORY.md files first, then all 8 insights.md files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const path=require('path'); const agents=['asset-curator','asset-processor','code-reviewer','compiler','editorial-lead','researcher','strategy','style-extractor','visual-planner','visual-researcher','writer']; const skills=['archive-search','autoresearch','crawl4ai-scraping','data-analysis','media-evaluation','pipeline-design','structured-output','visual-narrative']; let pass=0; let fail=0; for(const a of agents){const f=fs.readFileSync(path.join('.claude/agent-memory',a,'MEMORY.md'),'utf8'); if(f.includes('## Pending Review')){pass++}else{console.log('FAIL:',a);fail++}} for(const s of skills){const f=fs.readFileSync(path.join('.claude/skills',s,'insights.md'),'utf8'); if(f.includes('## Pending Review')){pass++}else{console.log('FAIL:',s);fail++}} console.log(pass+'/19 have Pending Review'); if(fail>0) process.exit(1);"</automated>
  </verify>
  <done>
    - All 11 MEMORY.md files under .claude/agent-memory/ contain ## Pending Review as a section heading
    - All 8 insights.md files under .claude/skills/ contain ## Pending Review as a section heading
    - No existing content in any file is deleted or modified (only append)
    - writer/MEMORY.md still contains all its existing Decisions, Patterns, and Observations entries
    - autoresearch/insights.md still contains the existing `- [2026-04-17] For well-documented cold cases` entry
    - Each file's ## Pending Review section appears AFTER all existing content
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system writes | Appending sections to 19 existing memory files -- risk of content loss on bad Edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-01 | Tampering | MEMORY.md / insights.md files | mitigate | Read each file before Edit; verify existing content preserved via done criteria checks |
| T-02-02 | Denial of Service | Large MEMORY.md injection | accept | Observer MEMORY.md is minimal (~15 lines); auto-injection cap is 200 lines -- well within budget |
</threat_model>

<verification>
- All 19 target files have `## Pending Review` section: `node -e` script above exits 0
- PLAYBOOK.md exists at `.claude/PLAYBOOK.md` with correct structure
- Observer MEMORY.md exists with key file references
- No existing file content lost (spot-check writer/MEMORY.md and autoresearch/insights.md)
</verification>

<success_criteria>
20 files created or modified. Every observer write target has a ## Pending Review section. PLAYBOOK.md bootstrapped per D-06/D-07.
</success_criteria>

<output>
After completion, create `.planning/phases/02-observer-agent/02-01-SUMMARY.md`
</output>
