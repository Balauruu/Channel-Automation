---
phase: 04-pipeline-triggers-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/strategy/SKILL.md
  - .claude/skills/strategy-scrape/SKILL.md
  - .claude/skills/strategy-analyze/SKILL.md
  - .claude/skills/strategy-topics/SKILL.md
  - .claude/skills/research/SKILL.md
  - .claude/skills/write-script/SKILL.md
  - .claude/skills/visual-plan/SKILL.md
  - .claude/skills/process-assets/SKILL.md
  - .claude/skills/assets-download/SKILL.md
  - .claude/skills/assets-embed/SKILL.md
  - .claude/skills/assets-search/SKILL.md
  - .claude/skills/assets-score/SKILL.md
  - .claude/skills/compile/SKILL.md
  - tests/smoke-test-pipeline.js
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-06
  - PIPE-07
  - PIPE-08
must_haves:
  truths:
    - "User can type /strategy and it dispatches @strategy agent for competitor analysis, topic generation, and project initialization"
    - "User can type /research <project> and it dispatches @researcher for 3-pass research"
    - "User can type /write-script <project> and it dispatches @writer for script generation from research"
    - "User can type /visual-plan <project> and it dispatches @visual-researcher then @visual-planner sequentially"
    - "User can type /process-assets <project> and it dispatches asset pipeline agents"
    - "User can type /compile <project> and it dispatches @compiler for edit sheet compilation"
    - "User can type granular sub-commands (/strategy-scrape, /strategy-analyze, /strategy-topics, /assets-download, /assets-embed, /assets-search, /assets-score) for fine-grained control"
    - "/strategy presents topics and STOPs for user selection (human checkpoint)"
    - "/process-assets presents assets and STOPs for user review (human checkpoint)"
    - "No pipeline skill references Python scripts (pre-Phase 6 mode)"
  artifacts:
    - path: ".claude/skills/strategy/SKILL.md"
      provides: "Full strategy pipeline command"
      contains: "disable-model-invocation: true"
    - path: ".claude/skills/research/SKILL.md"
      provides: "Research pipeline command"
      contains: "disable-model-invocation: true"
    - path: ".claude/skills/write-script/SKILL.md"
      provides: "Script generation command"
      contains: "disable-model-invocation: true"
    - path: ".claude/skills/visual-plan/SKILL.md"
      provides: "Visual planning command with two-agent chaining"
      contains: "visual-researcher"
    - path: ".claude/skills/process-assets/SKILL.md"
      provides: "Full asset processing command"
      contains: "disable-model-invocation: true"
    - path: ".claude/skills/compile/SKILL.md"
      provides: "Compile command"
      contains: "disable-model-invocation: true"
    - path: "tests/smoke-test-pipeline.js"
      provides: "Pipeline validation smoke test"
      contains: "testCases"
  key_links:
    - from: ".claude/skills/strategy/SKILL.md"
      to: "@strategy agent"
      via: "natural language dispatch instruction"
      pattern: "@strategy"
    - from: ".claude/skills/visual-plan/SKILL.md"
      to: "@visual-researcher and @visual-planner agents"
      via: "sequential dispatch instructions"
      pattern: "@visual-researcher.*@visual-planner"
    - from: ".claude/skills/strategy/SKILL.md"
      to: "checkpoint behavior"
      via: "STOP/WAIT instructions"
      pattern: "STOP|WAIT"
---

<objective>
Create all 13 pipeline trigger slash-command skills and the Wave 0 smoke test covering PIPE-01 through PIPE-08.

Purpose: Users need to run every pipeline stage via slash commands. This plan delivers all 13 skills: 6 primary pipeline commands (/strategy, /research, /write-script, /visual-plan, /process-assets, /compile) and 7 granular sub-commands (/strategy-scrape, /strategy-analyze, /strategy-topics, /assets-download, /assets-embed, /assets-search, /assets-score). Each skill dispatches the correct agent(s) with context handoff, and checkpoint skills (/strategy, /process-assets) present results and STOP for user approval.

Output: 13 SKILL.md files in .claude/skills/, 1 smoke test at tests/smoke-test-pipeline.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pipeline-triggers-hooks/04-CONTEXT.md
@.planning/phases/04-pipeline-triggers-hooks/04-RESEARCH.md

<interfaces>
<!-- Existing skill frontmatter pattern (from documentary-research) -->
```yaml
# ---
name: documentary-research
description: >-
  Documentary research domain expertise...
user-invocable: true
# ---
```

<!-- Pipeline skill frontmatter pattern (from 04-RESEARCH.md) -->
```yaml
# ---
name: research
description: >-
  Run the documentary research pipeline for a project...
disable-model-invocation: true
# ---
```

<!-- Note: pipeline skills use `disable-model-invocation: true` instead of `user-invocable: true`.
     The `disable-model-invocation` field prevents Claude from auto-invoking the skill.
     Per research: these are user-invocable slash commands that coordinate agent dispatch. -->

<!-- Agent names (from .claude/agents/) -->
12 agents: researcher, writer, strategy, style-extractor, editorial-lead,
visual-researcher, visual-planner, asset-processor, asset-curator,
meta, code-reviewer, compiler

<!-- Existing smoke test pattern (from tests/smoke-test-skills.js) -->
```javascript
const testCases = [];
testCases.push({
  name: `${skill}/directory_exists`,
  check: () => fs.existsSync(skillDir)
});
// ... test runner at bottom
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave 0 smoke test + strategy tier + research + write-script skills</name>
  <files>
    tests/smoke-test-pipeline.js,
    .claude/skills/strategy/SKILL.md,
    .claude/skills/strategy-scrape/SKILL.md,
    .claude/skills/strategy-analyze/SKILL.md,
    .claude/skills/strategy-topics/SKILL.md,
    .claude/skills/research/SKILL.md,
    .claude/skills/write-script/SKILL.md
  </files>
  <read_first>
    tests/smoke-test-skills.js,
    tests/smoke-test-agents.js,
    .claude/skills/documentary-research/SKILL.md,
    .claude/agents/strategy.md,
    .claude/agents/researcher.md,
    .claude/agents/writer.md,
    .planning/phases/04-pipeline-triggers-hooks/04-RESEARCH.md,
    .planning/phases/04-pipeline-triggers-hooks/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Each of 13 pipeline skill directories exists under .claude/skills/
    - Test: Each pipeline SKILL.md contains `disable-model-invocation: true` in frontmatter
    - Test: Each pipeline SKILL.md contains `name:` matching directory name in frontmatter
    - Test: `/strategy` SKILL.md body contains STOP or WAIT (checkpoint behavior)
    - Test: `/process-assets` SKILL.md body contains STOP or WAIT (checkpoint behavior)
    - Test: `/visual-plan` SKILL.md body references both @visual-researcher and @visual-planner
    - Test: No pipeline skill body contains `.py` or `python` (pre-Phase 6 mode per D-03)
    - Test: `/strategy` SKILL.md body references @strategy agent
    - Test: `/research` SKILL.md body references @researcher agent
    - Test: `/write-script` SKILL.md body references @writer agent
    - Test: `/compile` SKILL.md body references @compiler agent
    - Test: `/process-assets` SKILL.md body references @asset-processor
    - Test: Each skill body references `projects/` for output path convention (per D-04)
  </behavior>
  <action>
**Step 1 -- Create `tests/smoke-test-pipeline.js`:**

Create the Wave 0 smoke test following the same `testCases` array + runner pattern from `tests/smoke-test-skills.js` and `tests/smoke-test-agents.js`. The test validates all PIPE-01 through PIPE-08 requirements plus HOOK-03 and HOOK-04 structurally.

Pipeline skill directories to validate (13 total):
```
strategy, strategy-scrape, strategy-analyze, strategy-topics,
research, write-script, visual-plan,
process-assets, assets-download, assets-embed, assets-search, assets-score,
compile
```

Also include stubs for HOOK-03 checks (hook scripts exist, settings.json has registrations) and HOOK-04 checks (audit-agents skill + script exist). These will fail initially (RED) and be satisfied by Plans 02 and 03.

Per-skill test cases:
1. `${skill}/directory_exists` -- `fs.existsSync(skillDir)`
2. `${skill}/SKILL.md_exists` -- `fs.existsSync(skillMd)`
3. `${skill}/has_disable_model_invocation` -- content includes `disable-model-invocation: true`
4. `${skill}/has_name_field` -- content includes `name: ${skill}`

Agent dispatch checks (using regex matching):
5. `strategy/dispatches_strategy_agent` -- body includes `@strategy`
6. `research/dispatches_researcher_agent` -- body includes `@researcher`
7. `write-script/dispatches_writer_agent` -- body includes `@writer`
8. `visual-plan/dispatches_visual_researcher` -- body includes `@visual-researcher`
9. `visual-plan/dispatches_visual_planner` -- body includes `@visual-planner`
10. `process-assets/dispatches_asset_processor` -- body includes `@asset-processor`
11. `compile/dispatches_compiler_agent` -- body includes `@compiler`

Checkpoint checks:
12. `strategy/has_checkpoint_stop` -- body matches `/STOP|WAIT/i`
13. `process-assets/has_checkpoint_stop` -- body matches `/STOP|WAIT/i`

Pre-Phase 6 check (all 13 skills):
14. `${skill}/no_python_references` -- body does NOT match `/\.py\b|python/i`

Output path convention (all 13 skills -- check primary commands at minimum):
15. `${skill}/references_projects_dir` -- body includes `projects/`

Hook checks (stubs for Plan 02):
16. `hooks/dispatch_script_exists` -- `fs.existsSync('.claude/hooks/log-agent-dispatch.js')`
17. `hooks/complete_script_exists` -- `fs.existsSync('.claude/hooks/log-agent-complete.js')`
18. `hooks/settings_has_pretooluse` -- settings.json content includes `PreToolUse`
19. `hooks/settings_has_subagentstop` -- settings.json content includes `SubagentStop`

Audit checks (stubs for Plan 03):
20. `audit/skill_exists` -- `fs.existsSync('.claude/skills/audit-agents/SKILL.md')`
21. `audit/script_exists` -- `fs.existsSync('.claude/scripts/audit-agents.js')`

Test runner at bottom: same pattern as existing smoke tests -- iterate testCases, count pass/fail, exit(failCount > 0 ? 1 : 0). Use `path.resolve(__dirname, '..')` for projectRoot.

**Step 2 -- Create strategy tier skills (4 skills):**

Create `.claude/skills/strategy/SKILL.md`:
```yaml
# ---
name: strategy
description: >-
  Run the full strategy pipeline: competitor scraping, trend analysis,
  and topic generation. For individual operations, use /strategy-scrape,
  /strategy-analyze, or /strategy-topics.
disable-model-invocation: true
# ---
```
Body instructions:
1. If `$ARGUMENTS` is provided and looks like a project name, set PROJECT = $ARGUMENTS. Otherwise proceed without a project context.
2. Dispatch @strategy with task: "Run the complete strategy pipeline for the documentary channel. a) Use WebSearch to research competitor channels for latest video data on dark mysteries/true crime documentaries. b) Analyze competitor data for trends, gaps, and opportunities. c) Generate 5 scored topic candidates with scores for obscurity, complexity, shock_factor, verifiability, pillar_fit."
3. **CHECKPOINT**: Present the generated topics to the user. Display each topic with scores and total score.
4. STOP HERE. Do not continue. Wait for the user to select a topic.
5. After user selects: "Create project directory `projects/<selected-topic-slug>/` with subdirectories: `research/`, `script/`, `visuals/`, `assets/`, `compilation/`. Write a `metadata.md` with topic name, selection date, scores."
6. Guide: "Project initialized. Run `/research <project-name>` to start the documentary research pipeline."

Create `.claude/skills/strategy-scrape/SKILL.md`:
```yaml
# ---
name: strategy-scrape
description: >-
  Scrape competitor channel data for the documentary channel. Granular
  sub-command of /strategy.
disable-model-invocation: true
# ---
```
Body: Dispatch @strategy with task: "Scrape competitor channels using WebSearch. Focus on dark mysteries/true crime/unsolved documentary channels. Gather video titles, view counts, publish dates, topics. Write results to `projects/strategy/competitor_data.md`."

Create `.claude/skills/strategy-analyze/SKILL.md`:
```yaml
# ---
name: strategy-analyze
description: >-
  Analyze competitor data for trends, gaps, and opportunities. Granular
  sub-command of /strategy.
disable-model-invocation: true
# ---
```
Body: Dispatch @strategy with task: "Analyze competitor data at `projects/strategy/competitor_data.md`. Identify trend patterns, content gaps, and opportunities. Write analysis to `projects/strategy/analysis.md`."

Create `.claude/skills/strategy-topics/SKILL.md`:
```yaml
# ---
name: strategy-topics
description: >-
  Generate scored topic candidates for the documentary channel. Granular
  sub-command of /strategy.
disable-model-invocation: true
# ---
```
Body: Dispatch @strategy with task: "Generate 5 scored topic candidates based on analysis at `projects/strategy/analysis.md`. Score each on obscurity, complexity, shock_factor, verifiability, pillar_fit. Write topics to `projects/strategy/topics.md`." Then present topics to user and STOP.

**Step 3 -- Create research skill:**

Create `.claude/skills/research/SKILL.md`:
```yaml
# ---
name: research
description: >-
  Run the documentary research pipeline for a project. Dispatches the
  researcher agent to conduct 3-pass research (survey, deepen, synthesize).
disable-model-invocation: true
# ---
```
Body:
1. Verify `projects/$ARGUMENTS/` exists. If not, tell user: "Project not found. Run `/strategy` first to initialize a project."
2. Dispatch @researcher with task: "Research the documentary topic for project '$ARGUMENTS'. Read the project metadata at `projects/$ARGUMENTS/metadata.md`. Conduct your 3-pass research process (survey, deepen, synthesize). Write the research dossier to `projects/$ARGUMENTS/research/Research.md`. Write the entity index to `projects/$ARGUMENTS/research/entity_index.json`."
3. Present a summary of key findings (topic overview, source count, key entities).
4. Guide: "Research complete. Run `/write-script $ARGUMENTS` to generate a documentary script from this research."

**Step 4 -- Create write-script skill:**

Create `.claude/skills/write-script/SKILL.md`:
```yaml
# ---
name: write-script
description: >-
  Generate a documentary script from research. Dispatches the writer
  agent with the research dossier and voice profile context.
disable-model-invocation: true
# ---
```
Body:
1. Verify `projects/$ARGUMENTS/research/Research.md` exists. If not, tell user: "Research not found. Run `/research $ARGUMENTS` first."
2. Dispatch @writer with task: "Generate a documentary script for project '$ARGUMENTS'. Read the research dossier at `projects/$ARGUMENTS/research/Research.md`. Read the entity index at `projects/$ARGUMENTS/research/entity_index.json`. Write the script to `projects/$ARGUMENTS/script/Script.md`. Write script metadata to `projects/$ARGUMENTS/script/metadata.json`."
3. Present script summary (word count, act count, estimated duration).
4. Guide: "Script complete. Run `/visual-plan $ARGUMENTS` to generate the visual plan and shotlist."
  </action>
  <verify>
    <automated>node tests/smoke-test-pipeline.js 2>&1 | findstr /C:"strategy/" /C:"research/" /C:"write-script/" /C:"PASS" /C:"FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tests/smoke-test-pipeline.js exists and runs without syntax errors
    - tests/smoke-test-pipeline.js contains `const testCases = []`
    - tests/smoke-test-pipeline.js contains checks for all 13 pipeline skill names: strategy, strategy-scrape, strategy-analyze, strategy-topics, research, write-script, visual-plan, process-assets, assets-download, assets-embed, assets-search, assets-score, compile
    - .claude/skills/strategy/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/strategy/SKILL.md contains `@strategy`
    - .claude/skills/strategy/SKILL.md contains `STOP` (checkpoint)
    - .claude/skills/strategy/SKILL.md contains `projects/`
    - .claude/skills/strategy-scrape/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/strategy-scrape/SKILL.md contains `@strategy`
    - .claude/skills/strategy-analyze/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/strategy-topics/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/research/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/research/SKILL.md contains `@researcher`
    - .claude/skills/research/SKILL.md contains `projects/$ARGUMENTS/`
    - .claude/skills/write-script/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/write-script/SKILL.md contains `@writer`
    - .claude/skills/write-script/SKILL.md contains `projects/$ARGUMENTS/`
    - No .py or python references in any of the 6 skill bodies
  </acceptance_criteria>
  <done>
    Wave 0 smoke test created and runs (some tests fail for Plan 02/03 stubs -- expected).
    Strategy tier (4 skills), research skill, and write-script skill all created with correct frontmatter,
    agent dispatch instructions, output path conventions, and checkpoint behavior where required.
    All 6 skills pass their smoke test checks.
  </done>
</task>

<task type="auto">
  <name>Task 1: Visual-plan, process-assets tier, and compile skills</name>
  <files>
    .claude/skills/visual-plan/SKILL.md,
    .claude/skills/process-assets/SKILL.md,
    .claude/skills/assets-download/SKILL.md,
    .claude/skills/assets-embed/SKILL.md,
    .claude/skills/assets-search/SKILL.md,
    .claude/skills/assets-score/SKILL.md,
    .claude/skills/compile/SKILL.md
  </files>
  <read_first>
    .claude/agents/visual-researcher.md,
    .claude/agents/visual-planner.md,
    .claude/agents/asset-processor.md,
    .claude/agents/asset-curator.md,
    .claude/agents/compiler.md,
    .planning/phases/04-pipeline-triggers-hooks/04-RESEARCH.md,
    .planning/phases/04-pipeline-triggers-hooks/04-CONTEXT.md,
    .claude/skills/strategy/SKILL.md
  </read_first>
  <action>
**Create `.claude/skills/visual-plan/SKILL.md` (multi-agent chaining per D-08):**
```yaml
# ---
name: visual-plan
description: >-
  Run the full visual planning pipeline: visual research then shotlist
  generation. Dispatches @visual-researcher followed by @visual-planner.
disable-model-invocation: true
# ---
```
Body (sequential two-agent chain per D-08):
1. Verify `projects/$ARGUMENTS/script/Script.md` exists. If not, tell user: "Script not found. Run `/write-script $ARGUMENTS` first."
2. Dispatch @visual-researcher with task: "Define visual intent and gather primary resources for project '$ARGUMENTS'. Read the script at `projects/$ARGUMENTS/script/Script.md`. Read the entity index at `projects/$ARGUMENTS/research/entity_index.json`. Write the visual brief to `projects/$ARGUMENTS/visuals/visual_brief.json`. Write media leads to `projects/$ARGUMENTS/visuals/media_leads.json`."
3. After @visual-researcher completes, dispatch @visual-planner with task: "Generate shotlist from visual brief for project '$ARGUMENTS'. Read the visual brief at `projects/$ARGUMENTS/visuals/visual_brief.json`. Read the media leads at `projects/$ARGUMENTS/visuals/media_leads.json`. Read the script at `projects/$ARGUMENTS/script/Script.md`. Write the shotlist to `projects/$ARGUMENTS/visuals/shotlist.json`."
4. Present a summary (shot count, asset categories, estimated b-roll needs).
5. Guide: "Visual planning complete. Run `/process-assets $ARGUMENTS` to download and process assets for these shots."

**Create `.claude/skills/process-assets/SKILL.md` (two-tier + checkpoint per D-06, D-09):**
```yaml
# ---
name: process-assets
description: >-
  Run the full asset processing pipeline: download, embed, search, and score
  assets. For individual operations, use /assets-download, /assets-embed,
  /assets-search, or /assets-score.
disable-model-invocation: true
# ---
```
Body:
1. Verify `projects/$ARGUMENTS/visuals/shotlist.json` exists. If not, tell user: "Shotlist not found. Run `/visual-plan $ARGUMENTS` first."
2. Dispatch @asset-processor with task: "Process assets for project '$ARGUMENTS'. Read the shotlist at `projects/$ARGUMENTS/visuals/shotlist.json`. For each shot: a) Download candidate assets to `projects/$ARGUMENTS/assets/raw/`. b) Generate CLIP embeddings for downloaded assets. c) Run semantic search against the shotlist requirements. d) Score each asset for relevance and quality. Write the asset manifest to `projects/$ARGUMENTS/assets/asset_manifest.json`."
3. **CHECKPOINT**: Present the processed assets to the user. Show asset count, quality distribution, coverage gaps.
4. STOP HERE. Do not continue. Wait for the user to review and approve the assets.
5. Guide: "Asset review complete. Run `/compile $ARGUMENTS` to generate the DaVinci Resolve edit sheet."

**Create `.claude/skills/assets-download/SKILL.md`:**
```yaml
# ---
name: assets-download
description: >-
  Download candidate assets for a project's shotlist. Granular sub-command
  of /process-assets.
disable-model-invocation: true
# ---
```
Body: Dispatch @asset-processor with task: "Download candidate assets for project '$ARGUMENTS'. Read the shotlist at `projects/$ARGUMENTS/visuals/shotlist.json`. Download candidates to `projects/$ARGUMENTS/assets/raw/`. Write download log to `projects/$ARGUMENTS/assets/download_log.json`."

**Create `.claude/skills/assets-embed/SKILL.md`:**
```yaml
# ---
name: assets-embed
description: >-
  Generate CLIP embeddings for downloaded assets. Granular sub-command
  of /process-assets.
disable-model-invocation: true
# ---
```
Body: Dispatch @asset-processor with task: "Generate CLIP embeddings for assets in `projects/$ARGUMENTS/assets/raw/`. Write embeddings index to `projects/$ARGUMENTS/assets/embeddings.json`."

**Create `.claude/skills/assets-search/SKILL.md`:**
```yaml
# ---
name: assets-search
description: >-
  Run semantic search against shotlist requirements. Granular sub-command
  of /process-assets.
disable-model-invocation: true
# ---
```
Body: Dispatch @asset-processor with task: "Run semantic search for project '$ARGUMENTS'. Read the shotlist at `projects/$ARGUMENTS/visuals/shotlist.json`. Read embeddings at `projects/$ARGUMENTS/assets/embeddings.json`. Match assets to shots by semantic similarity. Write search results to `projects/$ARGUMENTS/assets/search_results.json`."

**Create `.claude/skills/assets-score/SKILL.md`:**
```yaml
# ---
name: assets-score
description: >-
  Score assets for relevance and quality. Granular sub-command
  of /process-assets.
disable-model-invocation: true
# ---
```
Body: Dispatch @asset-processor with task: "Score assets for project '$ARGUMENTS'. Read search results at `projects/$ARGUMENTS/assets/search_results.json`. Evaluate each asset on topical relevance, temporal relevance, and visual quality. Write scored manifest to `projects/$ARGUMENTS/assets/asset_manifest.json`."

**Create `.claude/skills/compile/SKILL.md`:**
```yaml
# ---
name: compile
description: >-
  Compile the DaVinci Resolve edit sheet from script, shotlist, and scored
  assets. Dispatches the compiler agent.
disable-model-invocation: true
# ---
```
Body:
1. Verify `projects/$ARGUMENTS/assets/asset_manifest.json` exists. If not, tell user: "Asset manifest not found. Run `/process-assets $ARGUMENTS` first."
2. Dispatch @compiler with task: "Compile the edit sheet for project '$ARGUMENTS'. Read the script at `projects/$ARGUMENTS/script/Script.md`. Read the shotlist at `projects/$ARGUMENTS/visuals/shotlist.json`. Read the asset manifest at `projects/$ARGUMENTS/assets/asset_manifest.json`. Generate the DaVinci Resolve edit sheet at `projects/$ARGUMENTS/compilation/edit_sheet.json`. Generate the asset organization report at `projects/$ARGUMENTS/compilation/asset_report.md`."
3. Present compilation summary (total shots, asset coverage, timeline duration estimate).
4. Guide: "Compilation complete. The edit sheet is ready for DaVinci Resolve import at `projects/$ARGUMENTS/compilation/edit_sheet.json`."

**Run the smoke test after creating all skills to verify all 13 pass their structural checks.**
  </action>
  <verify>
    <automated>node tests/smoke-test-pipeline.js 2>&1 | findstr /C:"PASS" /C:"FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/visual-plan/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/visual-plan/SKILL.md contains `@visual-researcher`
    - .claude/skills/visual-plan/SKILL.md contains `@visual-planner`
    - .claude/skills/visual-plan/SKILL.md contains `projects/$ARGUMENTS/`
    - .claude/skills/process-assets/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/process-assets/SKILL.md contains `@asset-processor`
    - .claude/skills/process-assets/SKILL.md contains `STOP` (checkpoint)
    - .claude/skills/process-assets/SKILL.md contains `projects/$ARGUMENTS/`
    - .claude/skills/assets-download/SKILL.md contains `disable-model-invocation: true` and `@asset-processor`
    - .claude/skills/assets-embed/SKILL.md contains `disable-model-invocation: true` and `@asset-processor`
    - .claude/skills/assets-search/SKILL.md contains `disable-model-invocation: true` and `@asset-processor`
    - .claude/skills/assets-score/SKILL.md contains `disable-model-invocation: true` and `@asset-processor`
    - .claude/skills/compile/SKILL.md contains `disable-model-invocation: true`
    - .claude/skills/compile/SKILL.md contains `@compiler`
    - .claude/skills/compile/SKILL.md contains `projects/$ARGUMENTS/`
    - No .py or python references in any of the 7 skill bodies
    - All 13 pipeline skill smoke test checks pass (strategy tier + research + write-script from Task 0 + these 7)
  </acceptance_criteria>
  <done>
    All 13 pipeline trigger skills created. Visual-plan chains two agents sequentially.
    Process-assets has checkpoint behavior. All skills reference projects/ output paths.
    Smoke test passes for all PIPE-01 through PIPE-08 structural checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> $ARGUMENTS | User-provided project name used as path component in skill body instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | $ARGUMENTS in skill body used as path | mitigate | Each skill verifies `projects/$ARGUMENTS/` directory exists before dispatching agent. Skills instruct Claude to check directory existence first. Path traversal is limited because Claude's file tools resolve paths relative to project root. |
| T-04-04 | Information Disclosure | Skill bodies contain agent dispatch patterns | accept | Skills are version-controlled project files. No secrets in skill bodies. Agent names are already documented in CLAUDE.md. |
</threat_model>

<verification>
1. Run `node tests/smoke-test-pipeline.js` -- all pipeline skill checks pass (hook/audit stubs expected to fail)
2. Run `node tests/smoke-test-skills.js` -- existing domain skills unaffected
3. Verify each of 13 skill directories has SKILL.md with `disable-model-invocation: true`
4. Verify /strategy and /process-assets skills contain STOP/WAIT checkpoint behavior
5. Verify /visual-plan references both @visual-researcher and @visual-planner
6. Verify no skill references .py or python (pre-Phase 6 mode)
</verification>

<success_criteria>
All 13 pipeline trigger skills exist as SKILL.md files with correct frontmatter, agent dispatch instructions, output path conventions to projects/<name>/, and checkpoint behavior where required. Smoke test validates all PIPE-01 through PIPE-08 requirements structurally.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pipeline-triggers-hooks/04-01-SUMMARY.md`
</output>
