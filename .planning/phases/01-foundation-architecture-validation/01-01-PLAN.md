---
phase: 01-foundation-architecture-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/settings.json
  - channel/channel.md
  - channel/voice-profile.md
  - channel/VISUAL_STYLE_GUIDE.md
  - .claude/references/skill-crafting-guide.md
  - .claude/skills/agent-protocols/SKILL.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-06
  - AGNT-01
  - AGNT-13

must_haves:
  truths:
    - "CLAUDE.md exists at project root with agent reference table listing all ~10 agents"
    - "Directory structure matches the Phase 1 architecture (agents, skills, rules, hooks, scripts, references, agent-memory, channel, tests)"
    - "Channel identity docs exist at channel/ with content migrated from V5"
    - "Skill crafting guide exists at .claude/references/skill-crafting-guide.md"
    - "agent-protocols skill exists with memory lifecycle and feedback signal protocols"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project entry point with agent reference table, folder map, architecture rules"
      contains: "Agent Reference"
    - path: ".claude/settings.json"
      provides: "Claude Code project settings placeholder"
      contains: "hooks"
    - path: "channel/channel.md"
      provides: "Channel DNA migrated from V5"
      contains: "Channel DNA"
    - path: "channel/voice-profile.md"
      provides: "Writing style profile migrated from V5"
      contains: "Channel Style Profile"
    - path: "channel/VISUAL_STYLE_GUIDE.md"
      provides: "Visual style definitions migrated from V5"
      contains: "Visual Style Guide"
    - path: ".claude/references/skill-crafting-guide.md"
      provides: "Reference for future skill creation"
      contains: "Skill Crafting Guide"
    - path: ".claude/skills/agent-protocols/SKILL.md"
      provides: "Shared memory + feedback protocol for all agents"
      contains: "Memory Lifecycle"
  key_links:
    - from: "CLAUDE.md"
      to: "channel/"
      via: "Folder map reference"
      pattern: "channel.*Channel identity"
    - from: ".claude/skills/agent-protocols/SKILL.md"
      to: ".claude/agent-memory/*/MEMORY.md"
      via: "Memory lifecycle protocol instructions"
      pattern: "Read your complete MEMORY.md"
---

<objective>
Create the foundational directory structure, CLAUDE.md project entry point, channel identity docs (migrated from V5), skill crafting reference guide, and the shared agent-protocols behavioral skill.

Purpose: Everything in Plans 02 and 03 depends on these files existing. The directory scaffold, CLAUDE.md agent reference table, channel docs, and agent-protocols skill must exist before any agent definition can be written.

Output: CLAUDE.md, .claude/ directory tree, channel/ docs, skill-crafting-guide.md, agent-protocols SKILL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-architecture-validation/01-CONTEXT.md
@.planning/phases/01-foundation-architecture-validation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure, CLAUDE.md, and settings.json</name>
  <files>
    CLAUDE.md
    .claude/settings.json
    .claude/agents/.gitkeep
    .claude/skills/.gitkeep
    .claude/rules/.gitkeep
    .claude/hooks/.gitkeep
    .claude/scripts/.gitkeep
    .claude/references/.gitkeep
    .claude/agent-memory/researcher/.gitkeep
    .claude/agent-memory/writer/.gitkeep
    channel/.gitkeep
    tests/.gitkeep
  </files>
  <read_first>
    .planning/phases/01-foundation-architecture-validation/01-RESEARCH.md (lines 109-147 for directory tree, lines 365-411 for CLAUDE.md example)
    .planning/phases/01-foundation-architecture-validation/01-CONTEXT.md (D-01, D-04 for agent reference table decisions)
  </read_first>
  <action>
1. Create the full directory scaffold with .gitkeep files in empty directories:
   - `.claude/agents/`
   - `.claude/skills/`
   - `.claude/rules/`
   - `.claude/hooks/`
   - `.claude/scripts/`
   - `.claude/references/`
   - `.claude/agent-memory/researcher/`
   - `.claude/agent-memory/writer/`
   - `channel/`
   - `tests/`

2. Create `.claude/settings.json` with this exact content:

    {"hooks": {}}

3. Create `CLAUDE.md` at project root (target under 200 lines per RESEARCH.md Pattern 4). The file must contain these exact sections and content:

**Section: # Channel-Automation V0.6**

**Section: ## Project**
Content: "Dark mysteries documentary video production pipeline. Phase 1 vertical slice: researcher + writer agents."

**Section: ## Folder Map**
A bullet list mapping each directory to its purpose:
- `channel/` -- Channel identity (voice, style, visual guide)
- `.claude/agents/` -- Agent definitions
- `.claude/skills/` -- Shared skills (agent-protocols)
- `.claude/agent-memory/` -- Per-agent persistent memory
- `.claude/references/` -- Reference guides (skill crafting)
- `.claude/rules/` -- Path-scoped rules (Phase 4)
- `.claude/hooks/` -- Pre/PostToolUse hooks (Phase 4)
- `.claude/scripts/` -- Utility scripts (Phase 4)
- `projects/` -- Per-documentary outputs
- `strategy/` -- Strategy Python scripts
- `editorial/` -- Editorial Python scripts
- `media/` -- Media Python scripts
- `tests/` -- Smoke tests and validation scripts

**Section: ## Agent Reference**
A markdown table with columns: Agent, Domain, When to Use. Rows:

| Agent | Domain | When to Use |
|-------|--------|-------------|
| @researcher | Editorial | Research a documentary topic |
| @writer | Editorial | Generate a script from research |
| @strategy-lead | Strategy | Competitor analysis, topics (Phase 3) |
| @editorial-lead | Editorial | Complex editorial coordination (Phase 3) |
| @style-extractor | Editorial | Extract narrator voice (Phase 3) |
| @visual-researcher | Media | Visual intent and reference gathering (Phase 3) |
| @visual-planner | Media | Shotlist generation (Phase 3) |
| @asset-processor | Media | CLIP embeddings, downloads (Phase 3) |
| @asset-curator | Media | Asset evaluation and library management (Phase 3) |
| @meta | Meta | Pipeline health and code quality (Phase 3) |
| @compiler | Media | Edit sheet compilation (Phase 3) |

Below the table: "Agents marked '(Phase 3)' are not yet created. They appear here for reference."

**Section: ## Architecture Rules**
Bullet list:
- Agents are user-invoked only. Type @agent-name to delegate.
- No auto-routing. No auto-dispatch. User decides what to delegate.
- Human checkpoints: after topic generation (present and WAIT), after asset processing (present and WAIT).
- Subagents do NOT inherit CLAUDE.md -- each agent has a project_context block instructing it to Read ./CLAUDE.md.
- Shared behavioral protocols are injected via the agent-protocols skill in each agent's skills: field.

**Section: ## Platform**
Bullet list:
- Windows 11, RTX 4070 8GB VRAM
- Project path has spaces and periods -- use path.resolve(), never hardcode paths
- GPU scripts: use `C:/Users/iorda/miniconda3/envs/perception-models/python.exe`
- File operations: use Node.js `path` module. Never use `test -d`/`test -f` (bash builtins, not available on Windows)
- Filenames: colons are illegal on Windows -- timestamps must replace colons with dashes

Per D-01: The agent reference table is documentation only, NOT routing enforcement. No orchestrator agent.
Per D-04: Table includes all ~10 future agents from day one. Agents not yet created are marked "(Phase 3)".
Per D-03: Human checkpoints are instruction-based text in Architecture Rules section.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const dirs=['.claude/agents','.claude/skills','.claude/rules','.claude/hooks','.claude/scripts','.claude/references','.claude/agent-memory/researcher','.claude/agent-memory/writer','channel','tests'];let ok=true;dirs.forEach(d=>{if(!fs.existsSync(p.resolve(d))){console.log('MISSING:',d);ok=false}});if(!fs.existsSync('CLAUDE.md')){console.log('MISSING: CLAUDE.md');ok=false}if(!fs.existsSync('.claude/settings.json')){console.log('MISSING: settings.json');ok=false}const c=fs.readFileSync('CLAUDE.md','utf8');if(!c.includes('Agent Reference')){console.log('MISSING: Agent Reference table');ok=false}if(!c.includes('@researcher')){console.log('MISSING: @researcher in table');ok=false}if(!c.includes('@writer')){console.log('MISSING: @writer in table');ok=false}if(!c.includes('Phase 3')){console.log('MISSING: Phase 3 markers');ok=false}console.log(ok?'PASS':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md exists at project root
    - CLAUDE.md contains "## Agent Reference" section
    - CLAUDE.md contains "@researcher" and "@writer" in agent table
    - CLAUDE.md contains "(Phase 3)" markers for future agents
    - CLAUDE.md contains "## Architecture Rules" section with "user-invoked only"
    - CLAUDE.md contains "## Platform" section with Windows 11 and path.resolve
    - CLAUDE.md contains "## Folder Map" section
    - CLAUDE.md is under 200 lines
    - .claude/settings.json exists with {"hooks": {}}
    - All 10 directories exist: .claude/agents, .claude/skills, .claude/rules, .claude/hooks, .claude/scripts, .claude/references, .claude/agent-memory/researcher, .claude/agent-memory/writer, channel, tests
  </acceptance_criteria>
  <done>CLAUDE.md under 200 lines with complete agent reference table, folder map, architecture rules, and platform section. All directories scaffolded. settings.json placeholder created.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate channel identity docs from V5 and create skill crafting guide</name>
  <files>
    channel/channel.md
    channel/voice-profile.md
    channel/VISUAL_STYLE_GUIDE.md
    .claude/references/skill-crafting-guide.md
  </files>
  <read_first>
    D:\Youtube\D. Mysteries Channel\1.2 Agents\Channel-Automation V5\channel\channel.md (V5 source -- 68 lines, direct copy)
    D:\Youtube\D. Mysteries Channel\1.2 Agents\Channel-Automation V5\channel\voice\WRITTING_STYLE_PROFILE.md (V5 source -- 372 lines, rename to voice-profile.md)
    D:\Youtube\D. Mysteries Channel\1.2 Agents\Channel-Automation V5\channel\visuals\VISUAL_STYLE_GUIDE.md (V5 source -- 197 lines, flatten path)
    .planning/phases/01-foundation-architecture-validation/01-RESEARCH.md (lines 489-507 for V5 inventory)
  </read_first>
  <action>
1. **Channel docs migration (per D-10, D-12):**
   - Copy `D:\Youtube\D. Mysteries Channel\1.2 Agents\Channel-Automation V5\channel\channel.md` to `channel/channel.md` -- preserve content exactly (68 lines, Channel DNA).
   - Copy `D:\Youtube\D. Mysteries Channel\1.2 Agents\Channel-Automation V5\channel\voice\WRITTING_STYLE_PROFILE.md` to `channel/voice-profile.md` -- preserve all content, rename to fix the V5 typo ("WRITTING") and flatten the subdirectory path.
   - Copy `D:\Youtube\D. Mysteries Channel\1.2 Agents\Channel-Automation V5\channel\visuals\VISUAL_STYLE_GUIDE.md` to `channel/VISUAL_STYLE_GUIDE.md` -- preserve content, flatten from subdirectory.

   IMPORTANT: Read each V5 source file fully, then Write the content to the V0.6 target path. Do NOT modify content -- preserve as-is. The only changes are filename (voice profile) and directory structure (flatten subdirectories).

2. **Skill crafting guide (FOUND-06):**
   Create `.claude/references/skill-crafting-guide.md` as a reference document for creating new skills in later phases. The file must contain:

   **Title:** `# Skill Crafting Guide`
   **Subtitle:** Reference for creating Claude Code skills in the Channel-Automation pipeline.

   **Section: ## Skill Directory Structure**
   Show the layout: .claude/skills/skill-name/ containing SKILL.md (under 200 lines), prompts/, scripts/, references/ (with exemplar_01.md etc.), and insights.md.

   **Section: ## SKILL.md Frontmatter**
   Document the required YAML frontmatter fields: `name` (skill name), `description` (one-paragraph), `user-invocable` (true for user-triggered, false for agent-injected). Show as a YAML block WITHOUT triple-dash delimiters -- use indented text or a labeled block instead.

   **Section: ## Skill Body Sections**
   Subsections for:
   - Phase 0 Context Loading: Read insights.md + exemplars before starting
   - Procedure: Tag steps as [HEURISTIC] or [DETERMINISTIC]
   - Reflection Phase: Re-read output, evaluate, append one-line insight to insights.md with format `- [YYYY-MM-DD] insight`

   **Section: ## Insight Lifecycle**
   - Append one insight per run
   - At 20+ entries: merge duplicates
   - When 3+ insights converge: promote to SKILL.md

   **Section: ## Exemplar Curation**
   - 2-3 exemplar outputs in references/
   - Named exemplar_01.md, exemplar_02.md

   **Section: ## Anti-Patterns**
   - Do NOT exceed 200 lines in SKILL.md
   - Do NOT skip reflection phase
   - Do NOT put channel-specific content in skills -- reference via @channel/file.md
   - Do NOT duplicate agent-protocols content
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['channel/channel.md','channel/voice-profile.md','channel/VISUAL_STYLE_GUIDE.md','.claude/references/skill-crafting-guide.md'];let ok=true;files.forEach(f=>{if(!fs.existsSync(f)){console.log('MISSING:',f);ok=false}});const ch=fs.readFileSync('channel/channel.md','utf8');if(!ch.includes('Channel DNA')){console.log('FAIL: channel.md missing Channel DNA');ok=false}const vp=fs.readFileSync('channel/voice-profile.md','utf8');if(!vp.includes('Channel Style Profile')){console.log('FAIL: voice-profile.md missing header');ok=false}const vs=fs.readFileSync('channel/VISUAL_STYLE_GUIDE.md','utf8');if(!vs.includes('Visual Style Guide')){console.log('FAIL: VISUAL_STYLE_GUIDE.md missing header');ok=false}const sg=fs.readFileSync('.claude/references/skill-crafting-guide.md','utf8');if(!sg.includes('Skill Crafting Guide')){console.log('FAIL: skill-crafting-guide.md missing header');ok=false}if(!sg.includes('insights.md')){console.log('FAIL: skill-crafting-guide.md missing insights lifecycle');ok=false}console.log(ok?'PASS':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - channel/channel.md exists and contains "# Channel DNA"
    - channel/voice-profile.md exists and contains "# Channel Style Profile" (migrated from V5 WRITTING_STYLE_PROFILE.md)
    - channel/VISUAL_STYLE_GUIDE.md exists and contains "# Visual Style Guide"
    - channel/channel.md content matches V5 source (68 lines)
    - channel/voice-profile.md content matches V5 source (372 lines, no content changes)
    - .claude/references/skill-crafting-guide.md exists and contains "# Skill Crafting Guide"
    - skill-crafting-guide.md contains sections: "Skill Directory Structure", "SKILL.md Frontmatter", "Skill Body Sections", "Insight Lifecycle", "Exemplar Curation", "Anti-Patterns"
  </acceptance_criteria>
  <done>Three channel identity docs migrated from V5 with content preserved. Skill crafting guide reference created with complete structure documentation.</done>
</task>

<task type="auto">
  <name>Task 3: Create agent-protocols shared behavioral skill</name>
  <files>
    .claude/skills/agent-protocols/SKILL.md
  </files>
  <read_first>
    .planning/phases/01-foundation-architecture-validation/01-RESEARCH.md (lines 204-363 for Pattern 2 and Example 2)
    .planning/phases/01-foundation-architecture-validation/01-CONTEXT.md (D-06, D-14, D-15 for protocol decisions)
  </read_first>
  <action>
Create `.claude/skills/agent-protocols/SKILL.md` -- the shared behavioral skill injected into every agent via the `skills:` frontmatter field (per D-06).

This skill has `user-invocable: false` (hidden from / menu, only injected into agents). It provides two protocols.

**YAML Frontmatter fields (between triple-dash delimiters at top of file):**
- name: agent-protocols
- description: Shared behavioral protocols for all agents. Memory lifecycle (read at start, update after work) and feedback signal handling. Injected into agent context at startup via the skills: frontmatter field. Not user-invocable.
- user-invocable: false

**Body section: # Agent Protocols**
Opening line: "Behavioral protocols shared by all agents in the Channel-Automation pipeline. This skill is injected into your context at startup. Follow these protocols for every task."

**Body section: ## Memory Lifecycle**

Subsection "### At Task Start":
1. Read your complete MEMORY.md file using the Read tool
   - Path: `.claude/agent-memory/your-agent-name/MEMORY.md`
   - Read the FULL file, not just the auto-injected first 200 lines
   - The auto-injected content is a preview -- always Read the complete file
2. Review all sections: key_files, decisions, patterns, observations, open_questions
3. Identify patterns and decisions relevant to the current task
4. Note any open questions that this task might answer

Subsection "### During Work":
- Notice new patterns as you work
- Track decisions you make and why
- Record key files you create or discover

Subsection "### At Task End":
1. Read your current MEMORY.md again (it may have been updated during the session)
2. Append new entries to the appropriate section with timestamp
3. Entry format: `- [YYYY-MM-DD] observation or decision`
4. Preserve ALL existing entries -- this is append-only, never delete
5. Sections to update: key_files, decisions, patterns, observations, open_questions

Include a "### Memory Format Example" showing 2-3 example entries under a ## Patterns section with timestamps like [2026-04-10].

**Body section: ## Feedback Signal Protocol**

Subsection "### At Task Start":
1. Check if `feedback/SIGNALS.md` exists in the project root
2. If it exists, read it and look for entries where `to_agent` matches your agent name
3. Apply high-severity signals to your current work
4. Note medium-severity signals for awareness

Subsection "### At Task End":
1. If you noticed quality issues or insights that would help OTHER agents, write a signal
2. Append to `feedback/SIGNALS.md` (create file and directory if they do not exist)
3. Signal entry format: `### [YYYY-MM-DD] from:your-name to:target-agent severity:high|medium|low` followed by a one-line insight with evidence
4. Only write signals that are actionable by the target agent
5. Do NOT write signals to yourself -- use your MEMORY.md instead

**Body section: ## Project Context**
"Read ./CLAUDE.md at the start of every task for project-wide rules and conventions. This file contains the agent reference table, folder map, architecture rules, and platform constraints."

The skill must be under 200 lines (target ~60-80 lines). Per D-14, the memory protocol uses structured append with timestamps. Per D-15, agents must explicitly Read the full MEMORY.md, not rely on auto-injection.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f='.claude/skills/agent-protocols/SKILL.md';if(!fs.existsSync(f)){console.log('MISSING:',f);process.exit(1)}const c=fs.readFileSync(f,'utf8');const checks=[['user-invocable: false','frontmatter'],['Memory Lifecycle','memory section'],['At Task Start','start protocol'],['At Task End','end protocol'],['Feedback Signal Protocol','feedback section'],['SIGNALS.md','feedback file ref'],['Read your complete MEMORY.md','explicit read instruction'],['append-only','append-only rule'],['YYYY-MM-DD','timestamp format'],['Project Context','project context block']];let ok=true;checks.forEach(([pat,label])=>{if(!c.includes(pat)){console.log('MISSING:',label,'(pattern:',pat,')');ok=false}});const lines=c.split('\\n').length;if(lines>200){console.log('FAIL: skill is',lines,'lines (max 200)');ok=false}console.log(ok?'PASS':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/agent-protocols/SKILL.md exists
    - Frontmatter contains `name: agent-protocols`
    - Frontmatter contains `user-invocable: false`
    - Body contains "## Memory Lifecycle" section
    - Body contains "### At Task Start" with instruction to "Read your complete MEMORY.md"
    - Body contains "### At Task End" with append-only rule and `[YYYY-MM-DD]` timestamp format
    - Body contains "## Feedback Signal Protocol" section
    - Body contains reference to `feedback/SIGNALS.md`
    - Body contains "## Project Context" section with instruction to Read ./CLAUDE.md
    - File is under 200 lines total
  </acceptance_criteria>
  <done>agent-protocols skill created with memory lifecycle protocol (read full MEMORY.md at start, structured append at end) and feedback signal protocol (read/write SIGNALS.md). Under 200 lines, user-invocable: false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MEMORY.md content | Agent reads MEMORY.md which could contain adversarial content if git history is tampered |
| SIGNALS.md content | Cross-agent signals could contain misleading instructions |
| @file imports | Agent body references channel docs which are user-authored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-01-01 | Tampering | MEMORY.md | accept | Memory files are git-tracked for auditability. Agent-protocols instructs agents to treat memory as advisory context, not executable commands. Low risk: single-user project, no external contributors. |
| T-01-02 | Tampering | SIGNALS.md | accept | Feedback signals are git-tracked. Single-user project -- no untrusted signal sources. Agents treat signals as suggestions. |
| T-01-03 | Information Disclosure | @file references | accept | @file syntax is handled by Claude Code natively -- no custom path resolution code. Channel docs are non-sensitive project assets. |
| T-01-04 | Elevation of Privilege | agent-protocols skill | accept | Skill is injected via skills: frontmatter (Claude Code native mechanism). No custom code execution. Skill content is advisory markdown, not executable. |
</threat_model>

<verification>
1. All 10 directories exist in the scaffold
2. CLAUDE.md exists under 200 lines with agent reference table, folder map, architecture rules, platform section
3. Three channel docs exist at channel/ with content matching V5 sources
4. Skill crafting guide exists at .claude/references/skill-crafting-guide.md
5. agent-protocols skill exists under 200 lines with memory and feedback protocols
6. settings.json exists with minimal valid content
</verification>

<success_criteria>
- `node -e "..."` verification commands for all three tasks exit with code 0
- CLAUDE.md agent reference table lists all ~10 agents with Phase 3 markers
- Channel docs preserve V5 content without modification
- agent-protocols skill contains explicit "Read your complete MEMORY.md" instruction (per D-15)
- agent-protocols skill contains append-only memory update protocol with timestamps (per D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-architecture-validation/01-01-SUMMARY.md`
</output>
