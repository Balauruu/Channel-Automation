---
phase: 02-skills-library
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - tests/smoke-test-skills.js
  - .claude/references/skill-crafting-guide.md
  - .planning/REQUIREMENTS.md
  - .planning/config.json
autonomous: true
requirements:
  - SKIL-09
  - SKIL-10
  - SKIL-11
  - SKIL-12

must_haves:
  truths:
    - "Running `node tests/smoke-test-skills.js` validates all 8 skills for correct structure (SKILL.md, insights.md, frontmatter, Phase 0, Reflection, H/D tags)"
    - "Skill crafting guide reflects current decisions: no line cap, optional exemplars"
    - "REQUIREMENTS.md reflects D-12 and D-13 updates (SKIL-09 no line cap, SKIL-07 exemplars optional)"
    - "config.json contains agent_skills mapping defining which skills each agent receives"
  artifacts:
    - path: "tests/smoke-test-skills.js"
      provides: "Automated validation of all 8 domain skills"
      contains: "smoke-test-skills"
    - path: ".claude/references/skill-crafting-guide.md"
      provides: "Updated skill creation reference"
      contains: "no line cap"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements with D-12/D-13 changes"
      contains: "SKIL-09"
    - path: ".planning/config.json"
      provides: "Agent-to-skill mapping for Phase 3"
      contains: "agent_skills"
  key_links:
    - from: "tests/smoke-test-skills.js"
      to: ".claude/skills/*/SKILL.md"
      via: "fs.readFileSync validation"
      pattern: "readFileSync.*SKILL\\.md"
    - from: ".planning/config.json"
      to: ".claude/skills/*/SKILL.md"
      via: "agent_skills mapping references skill names"
      pattern: "agent_skills"
---

<objective>
Validate and finalize the skills library: create a comprehensive smoke test that verifies all 8 skills meet SKIL-09 through SKIL-12 structural requirements, update the skill crafting guide to reflect current decisions (D-08 no line cap, D-10 optional exemplars), update REQUIREMENTS.md per D-12/D-13, and add the agent_skills config mapping per D-02.

Purpose: This plan ensures all cross-cutting quality requirements are met and the skills library is ready for Phase 3 agent migration. The smoke test becomes a permanent validation tool. The config mapping tells Phase 3 which skills each agent needs.

Output: Smoke test script, updated crafting guide, updated REQUIREMENTS.md, updated config.json with agent_skills.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-skills-library/02-CONTEXT.md
@.planning/phases/02-skills-library/02-RESEARCH.md

<interfaces>
<!-- All 8 skill directories that must exist (created by Plans 01-03) -->
Skills to validate:
- .claude/skills/documentary-research/SKILL.md + insights.md
- .claude/skills/archive-search/SKILL.md + insights.md
- .claude/skills/crawl4ai-scraping/SKILL.md + insights.md
- .claude/skills/visual-narrative/SKILL.md + insights.md
- .claude/skills/media-evaluation/SKILL.md + insights.md
- .claude/skills/data-analysis/SKILL.md + insights.md
- .claude/skills/autoresearch/SKILL.md + insights.md
- .claude/skills/structured-output/SKILL.md + insights.md

<!-- Existing test pattern from Phase 1 -->
From tests/smoke-test-paths.js:
```javascript
const fs = require('fs');
const path = require('path');
const projectRoot = path.resolve(__dirname, '..');
// ... test cases with { name, check: () => boolean } pattern
let passed = 0;
const total = testCases.length;
for (const tc of testCases) { /* ... */ }
console.log(`\n${passed}/${total} passed`);
process.exit(passed === total ? 0 : 1);
```

<!-- agent_skills mapping from research -->
From 02-RESEARCH.md:
```json
{
  "agent_skills": {
    "researcher": ["agent-protocols", "documentary-research", "archive-search", "crawl4ai-scraping"],
    "writer": ["agent-protocols", "documentary-research", "structured-output"],
    "visual-researcher": ["agent-protocols", "visual-narrative", "archive-search", "crawl4ai-scraping"],
    "visual-planner": ["agent-protocols", "visual-narrative", "archive-search", "media-evaluation"],
    "asset-processor": ["agent-protocols", "media-evaluation"],
    "asset-curator": ["agent-protocols", "media-evaluation"],
    "strategy": ["agent-protocols", "data-analysis", "structured-output"],
    "meta": ["agent-protocols", "autoresearch", "structured-output"]
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke-test-skills.js validation script</name>
  <files>tests/smoke-test-skills.js</files>
  <read_first>
    - tests/smoke-test-paths.js (existing test pattern to follow for consistent test infrastructure)
    - .planning/phases/02-skills-library/02-RESEARCH.md (lines 544-574 contain the test architecture specification)
  </read_first>
  <action>
Create `tests/smoke-test-skills.js` following the same pattern as `tests/smoke-test-paths.js` (Node.js fs assertions, testCases array with name/check pairs, exit code 0/1).

The script must validate ALL of the following for each of the 8 domain skills (documentary-research, archive-search, crawl4ai-scraping, visual-narrative, media-evaluation, data-analysis, autoresearch, structured-output):

**Per-skill checks (8 skills x 6 checks = 48 test cases):**
1. Skill directory exists: `fs.existsSync(path.join(projectRoot, '.claude/skills/{name}/'))`
2. SKILL.md exists: `fs.existsSync(path.join(projectRoot, '.claude/skills/{name}/SKILL.md'))`
3. insights.md exists: `fs.existsSync(path.join(projectRoot, '.claude/skills/{name}/insights.md'))`
4. SKILL.md has valid frontmatter with `name:` and `user-invocable: true`: Read file, check `content.includes('name: {name}')` and `content.includes('user-invocable: true')`
5. SKILL.md has `description:` in frontmatter: `content.includes('description:')`
6. SKILL.md name in frontmatter matches directory name: Parse the `name:` line and compare

**Cross-skill structural checks (4 checks per skill = 32 test cases):**
7. SKILL.md contains `Phase 0` section (SKIL-11): `content.includes('Phase 0')`
8. SKILL.md contains `Reflection Phase` section (SKIL-10): `content.includes('Reflection Phase')`
9. SKILL.md contains `[HEURISTIC]` tag (SKIL-12): `content.includes('[HEURISTIC]')`
10. SKILL.md contains `[DETERMINISTIC]` tag (SKIL-12): `content.includes('[DETERMINISTIC]')`

**Global checks (2 test cases):**
11. No SKILL.md contains V5 paths (`.pi/multi-team/`): Read all 8 files, check none include `.pi/multi-team/`
12. insights.md files all contain the lifecycle template marker (`Append new insights below this line`)

Use this array of skill names to iterate:
```javascript
const skills = [
  'documentary-research', 'archive-search', 'crawl4ai-scraping',
  'visual-narrative', 'media-evaluation', 'data-analysis',
  'autoresearch', 'structured-output'
];
```

Total: ~82 test cases. Output format matches smoke-test-paths.js: `PASS` or `FAIL` per test, summary at end, exit code 0 only if all pass.

Header comment:
```javascript
// tests/smoke-test-skills.js
// Phase 2 Skills Library validation -- checks SKIL-09 through SKIL-12
// Run: node tests/smoke-test-skills.js
// Full suite: node tests/smoke-test-paths.js && node tests/smoke-test-skills.js
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const f=p.join(r,'tests/smoke-test-skills.js');const c=fs.readFileSync(f,'utf8');console.log('file exists:',fs.existsSync(f));console.log('has skill list:',c.includes('documentary-research'));console.log('has Phase 0 check:',c.includes('Phase 0'));console.log('has Reflection check:',c.includes('Reflection Phase'));console.log('has HEURISTIC check:',c.includes('[HEURISTIC]'));console.log('has DETERMINISTIC check:',c.includes('[DETERMINISTIC]'));console.log('has exit code:',c.includes('process.exit'));const ok=fs.existsSync(f)&&c.includes('documentary-research')&&c.includes('Phase 0')&&c.includes('process.exit');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/smoke-test-skills.js exists
    - Script checks all 8 skill names: documentary-research, archive-search, crawl4ai-scraping, visual-narrative, media-evaluation, data-analysis, autoresearch, structured-output
    - Script validates directory existence, SKILL.md existence, insights.md existence per skill
    - Script validates frontmatter fields (name, description, user-invocable: true) per skill
    - Script validates Phase 0, Reflection Phase, [HEURISTIC], [DETERMINISTIC] presence per skill
    - Script checks no SKILL.md contains `.pi/multi-team/` (V5 path contamination)
    - Script uses `process.exit(passed === total ? 0 : 1)` pattern
    - Script runs successfully with `node tests/smoke-test-skills.js` and all tests pass (assumes Plans 01-03 completed)
  </acceptance_criteria>
  <done>Smoke test script exists at tests/smoke-test-skills.js and validates all 8 skills for structural compliance with SKIL-09, SKIL-10, SKIL-11, SKIL-12</done>
</task>

<task type="auto">
  <name>Task 2: Update skill-crafting-guide.md and REQUIREMENTS.md</name>
  <files>.claude/references/skill-crafting-guide.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .claude/references/skill-crafting-guide.md (current state -- contains outdated 200-line cap and required exemplars)
    - .planning/REQUIREMENTS.md (current state -- SKIL-09 and SKIL-07 need updates per D-12 and D-13)
    - .planning/phases/02-skills-library/02-CONTEXT.md (D-08, D-10, D-12, D-13 decision details)
  </read_first>
  <action>
**Update `.claude/references/skill-crafting-guide.md`** to reflect Phase 2 decisions. Specific changes:

1. **Remove the 200-line cap.** Change `SKILL.md              # Skill definition (under 200 lines)` to `SKILL.md              # Skill definition (no line cap)`. Change the anti-pattern `Do NOT exceed 200 lines in SKILL.md -- split into referenced files if needed` to `Do NOT pad skills with filler content -- every section should earn its place. Skills can be as long as needed for thorough domain coverage.`

2. **Make exemplars optional.** Change the directory structure section: `references/` should say `# OPTIONAL exemplar outputs for quality calibration` (not required). Change `exemplar_01.md` and `exemplar_02.md` to show them as optional with a comment.

3. **Update Phase 0: Context Loading.** Step 2 currently says "Read 2-3 exemplar outputs from `references/` to calibrate quality expectations". Change to: "If `references/` directory exists with exemplar files, read them to calibrate quality expectations (exemplars are optional)".

4. **Update Exemplar Curation section.** Add "(OPTIONAL)" to the heading. Change "Maintain 2-3 exemplar outputs" to "Optionally maintain 2-3 exemplar outputs". Change "Each exemplar represents..." paragraph to indicate this is guidance for skills that benefit from exemplars, not a requirement.

5. **Update Reflection Phase.** Step 2 currently says "Evaluate against the exemplars loaded in Phase 0". Change to: "Evaluate against quality standards defined in the skill and any exemplars loaded in Phase 0".

6. **Add note about `insights.md` being the primary learning mechanism.** After the Insight Lifecycle section, add: "`insights.md` is the primary and sufficient learning mechanism for skills. Exemplars are supplementary -- not all skills need them."

**Update `.planning/REQUIREMENTS.md`** per D-12 and D-13. Specific changes:

1. **SKIL-09** (line ~48): Change from `All skills follow crafting guide structure: SKILL.md < 200 lines, prompts/, scripts/, references/exemplar_*.md, insights.md` to `All skills follow crafting guide structure: SKILL.md (no line cap), prompts/, scripts/ (where applicable), insights.md, references/ (optional)`

2. **SKIL-07** (currently at line ~47): The requirement says `Autoresearch skill (Karpathy-style iterative research loop)`. This is fine as-is. However, the MEMO-07 requirement (line ~72) references exemplar curation as "2-3 max per skill" -- add a note that this is guidance, not a mandate, per D-13. Actually, MEMO-07 is in Phase 3 scope, not Phase 2. The D-13 change is specifically about SKIL-07 (previously had exemplar requirement). Since SKIL-07 in REQUIREMENTS.md does not mention exemplars directly, the update is to SKIL-09 only. Add a note to MEMO-07: `(exemplar curation is optional per D-13; 2-3 max is guidance when exemplars are used)`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const g=fs.readFileSync(p.join(r,'.claude/references/skill-crafting-guide.md'),'utf8');const q=fs.readFileSync(p.join(r,'.planning/REQUIREMENTS.md'),'utf8');console.log('guide no 200 cap:',!g.includes('under 200 lines'));console.log('guide optional exemplars:',g.includes('OPTIONAL') || g.includes('optional'));console.log('guide no exceed 200:',!g.includes('Do NOT exceed 200 lines'));console.log('req SKIL-09 updated:',q.includes('no line cap'));console.log('req has optional:',q.includes('optional'));const ok=!g.includes('under 200 lines')&&(g.includes('OPTIONAL')||g.includes('optional'))&&q.includes('no line cap');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/references/skill-crafting-guide.md does NOT contain "under 200 lines"
    - .claude/references/skill-crafting-guide.md does NOT contain "Do NOT exceed 200 lines"
    - .claude/references/skill-crafting-guide.md contains "no line cap" in directory structure comment
    - .claude/references/skill-crafting-guide.md contains "OPTIONAL" or "optional" in exemplar/references section
    - .claude/references/skill-crafting-guide.md Phase 0 section treats exemplar reading as conditional
    - .planning/REQUIREMENTS.md SKIL-09 line contains "no line cap" and "optional" for references
    - .planning/REQUIREMENTS.md MEMO-07 has a note about exemplar curation being optional guidance
  </acceptance_criteria>
  <done>Skill crafting guide updated to remove 200-line cap and make exemplars optional. REQUIREMENTS.md updated with SKIL-09 (no line cap, optional exemplars) and MEMO-07 (optional guidance note) per D-12 and D-13.</done>
</task>

<task type="auto">
  <name>Task 3: Add agent_skills mapping to config.json</name>
  <files>.planning/config.json</files>
  <read_first>
    - .planning/config.json (current state -- has empty agent_skills: {})
    - .planning/phases/02-skills-library/02-RESEARCH.md (lines 476-498 contain the recommended agent_skills mapping)
  </read_first>
  <action>
Update `.planning/config.json` to populate the `agent_skills` field with the mapping from 02-RESEARCH.md. This serves as a planning reference for Phase 3 (which agents get which skills). The actual skill injection happens via each agent's `skills:` frontmatter field (the Claude Code native mechanism).

Change the `"agent_skills": {}` line to:

```json
"agent_skills": {
  "researcher": ["agent-protocols", "documentary-research", "archive-search", "crawl4ai-scraping"],
  "writer": ["agent-protocols", "documentary-research", "structured-output"],
  "visual-researcher": ["agent-protocols", "visual-narrative", "archive-search", "crawl4ai-scraping"],
  "visual-planner": ["agent-protocols", "visual-narrative", "archive-search", "media-evaluation"],
  "asset-processor": ["agent-protocols", "media-evaluation"],
  "asset-curator": ["agent-protocols", "media-evaluation"],
  "strategy": ["agent-protocols", "data-analysis", "structured-output"],
  "meta": ["agent-protocols", "autoresearch", "structured-output"]
}
```

Preserve all other fields in config.json exactly as they are. Only modify the `agent_skills` value.

Note: This is a planning artifact, not runtime config. Phase 3 will read this mapping to know which `skills:` entries to add to each agent's frontmatter. Per D-02, the config-based mechanism serves as the centralized reference for agent-to-skill assignments.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const c=JSON.parse(fs.readFileSync(p.join(r,'.planning/config.json'),'utf8'));const a=c.agent_skills;console.log('has agent_skills:',!!a);console.log('researcher skills:',JSON.stringify(a.researcher));console.log('writer skills:',JSON.stringify(a.writer));console.log('strategy skills:',JSON.stringify(a.strategy));console.log('meta skills:',JSON.stringify(a.meta));const ok=a&&a.researcher&&a.researcher.includes('documentary-research')&&a.writer&&a.writer.includes('structured-output')&&a.strategy&&a.strategy.includes('data-analysis')&&a.meta&&a.meta.includes('autoresearch');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/config.json is valid JSON (parses without error)
    - .planning/config.json `agent_skills.researcher` contains `["agent-protocols", "documentary-research", "archive-search", "crawl4ai-scraping"]`
    - .planning/config.json `agent_skills.writer` contains `["agent-protocols", "documentary-research", "structured-output"]`
    - .planning/config.json `agent_skills.visual-researcher` contains `["agent-protocols", "visual-narrative", "archive-search", "crawl4ai-scraping"]`
    - .planning/config.json `agent_skills.visual-planner` contains `["agent-protocols", "visual-narrative", "archive-search", "media-evaluation"]`
    - .planning/config.json `agent_skills.asset-processor` contains `["agent-protocols", "media-evaluation"]`
    - .planning/config.json `agent_skills.asset-curator` contains `["agent-protocols", "media-evaluation"]`
    - .planning/config.json `agent_skills.strategy` contains `["agent-protocols", "data-analysis", "structured-output"]`
    - .planning/config.json `agent_skills.meta` contains `["agent-protocols", "autoresearch", "structured-output"]`
    - All other config.json fields (model_profile, commit_docs, workflow, etc.) are preserved unchanged
  </acceptance_criteria>
  <done>config.json agent_skills mapping populated with all 8 agent-to-skill assignments per D-02 and 02-RESEARCH.md specification, serving as the Phase 3 planning reference for skill injection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test script execution | smoke-test-skills.js runs via `node` and reads local files only. No network, no secrets, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | config.json agent_skills mapping | accept | config.json is a planning artifact in a git repo. Changes are tracked. No runtime enforcement -- Phase 4 `/audit-agents` validates actual agent-skill pairings. |
| T-02-02 | Denial of Service | smoke-test-skills.js | accept | Test script reads ~16 small markdown files. Negligible resource usage. No external dependencies. |
</threat_model>

<verification>
After all three tasks complete:
1. `node tests/smoke-test-skills.js` exits with code 0 (all tests pass)
2. `node tests/smoke-test-paths.js` still exits with code 0 (Phase 1 tests unaffected)
3. `node tests/smoke-test-paths.js && node tests/smoke-test-skills.js` runs the full validation suite
4. skill-crafting-guide.md no longer references 200-line cap or required exemplars
5. REQUIREMENTS.md SKIL-09 reflects "no line cap" and "optional" exemplars
6. config.json agent_skills maps 8 agents to their skill sets
</verification>

<success_criteria>
- Smoke test validates all 8 skills and passes (82+ test cases)
- Crafting guide updated to reflect D-08 and D-10 decisions
- REQUIREMENTS.md updated per D-12 and D-13
- config.json agent_skills provides complete Phase 3 reference mapping
- Full test suite (Phase 1 + Phase 2) passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-skills-library/02-04-SUMMARY.md`
</output>
