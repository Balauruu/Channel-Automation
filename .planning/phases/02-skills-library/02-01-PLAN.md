---
phase: 02-skills-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/documentary-research/SKILL.md
  - .claude/skills/documentary-research/insights.md
  - .claude/skills/archive-search/SKILL.md
  - .claude/skills/archive-search/insights.md
  - .claude/skills/crawl4ai-scraping/SKILL.md
  - .claude/skills/crawl4ai-scraping/insights.md
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
  - SKIL-05
  - SKIL-09
  - SKIL-10
  - SKIL-11
  - SKIL-12
must_haves:
  truths:
    - "User can invoke /documentary-research and receive domain expertise on source evaluation, triangulation, and narrative hooks"
    - "User can invoke /archive-search and receive Internet Archive, Prelinger, and YouTube navigation guidance"
    - "User can invoke /crawl4ai-scraping and receive web scraping patterns for JS-heavy research sites"
    - "All three skills load insights.md at Phase 0 and append insights after completion"
  artifacts:
    - path: ".claude/skills/documentary-research/SKILL.md"
      provides: "Documentary research domain expertise"
      contains: "user-invocable: true"
    - path: ".claude/skills/documentary-research/insights.md"
      provides: "Learning accumulation for documentary research"
      contains: "Append new insights below this line"
    - path: ".claude/skills/archive-search/SKILL.md"
      provides: "Archive navigation domain expertise"
      contains: "user-invocable: true"
    - path: ".claude/skills/archive-search/insights.md"
      provides: "Learning accumulation for archive search"
      contains: "Append new insights below this line"
    - path: ".claude/skills/crawl4ai-scraping/SKILL.md"
      provides: "Web scraping domain expertise"
      contains: "user-invocable: true"
    - path: ".claude/skills/crawl4ai-scraping/insights.md"
      provides: "Learning accumulation for crawl4ai scraping"
      contains: "Append new insights below this line"
  key_links:
    - from: ".claude/skills/documentary-research/SKILL.md"
      to: ".claude/skills/documentary-research/insights.md"
      via: "Phase 0 context loading instruction"
      pattern: "Read.*insights\\.md"
    - from: ".claude/skills/archive-search/SKILL.md"
      to: ".claude/skills/archive-search/insights.md"
      via: "Phase 0 context loading instruction"
      pattern: "Read.*insights\\.md"
    - from: ".claude/skills/crawl4ai-scraping/SKILL.md"
      to: ".claude/skills/crawl4ai-scraping/insights.md"
      via: "Phase 0 context loading instruction"
      pattern: "Read.*insights\\.md"
---

<objective>
Build three editorial/web research domain skills: documentary-research (SKIL-01), archive-search (SKIL-02), and crawl4ai-scraping (SKIL-05). Each skill adapts battle-tested V5 domain knowledge into the Claude Code skill format with proper frontmatter, Phase 0 context loading, heuristic/deterministic tagging, and reflection phases.

Purpose: These three skills provide the domain expertise that the researcher agent (and future visual-researcher agent) needs for documentary source evaluation, archive navigation, and web scraping. They are the editorial research foundation of the skills library.

Output: 3 skill directories under `.claude/skills/`, each with `SKILL.md` and `insights.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-skills-library/02-CONTEXT.md
@.planning/phases/02-skills-library/02-RESEARCH.md

<interfaces>
<!-- Existing skill pattern to follow -->
From .claude/skills/agent-protocols/SKILL.md:
```yaml
# ---
name: agent-protocols
description: >-
  Shared behavioral protocols for all agents. Memory lifecycle (read at start,
  update after work) and feedback signal handling. Injected into agent context
  at startup via the skills: frontmatter field. Not user-invocable.
user-invocable: false
# ---
```

<!-- Skill structure pattern from research -->
Required SKILL.md sections (in order):
1. YAML frontmatter (name, description, user-invocable: true)
2. Phase 0: Context Loading (read insights.md + relevant channel docs)
3. Domain Knowledge sections tagged [HEURISTIC] or [DETERMINISTIC]
4. Script References (marked "available after Phase 6 integration")
5. Reflection Phase (re-read output, append one-line insight to insights.md)

<!-- insights.md template -->
```markdown
# Insights

Accumulated learnings from skill runs. Read at the start of every run.

## Lifecycle
- **Append:** One insight per run (never skip the reflection phase)
- **Merge:** Consolidate duplicates at 20+ entries
- **Promote:** Extract to SKILL.md when 3+ entries converge on same pattern

<!-- Append new insights below this line -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create documentary-research skill</name>
  <files>.claude/skills/documentary-research/SKILL.md, .claude/skills/documentary-research/insights.md</files>
  <read_first>
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/skills/documentary-research.md (V5 source domain knowledge to adapt)
    - .claude/skills/agent-protocols/SKILL.md (pattern reference for skill structure)
    - .planning/phases/02-skills-library/02-RESEARCH.md (lines 309-376 contain the exact example for this skill)
    - .claude/references/skill-crafting-guide.md (supplementary structure reference)
  </read_first>
  <action>
Create the directory `.claude/skills/documentary-research/` and two files inside it.

**SKILL.md:** Adapt the V5 documentary-research.md domain knowledge into Claude Code skill format. The SKILL.md in 02-RESEARCH.md lines 309-376 is a near-complete example -- use it as the base and enrich it with any additional V5 domain knowledge not already captured. Per D-01, this skill provides SUPPLEMENTARY domain expertise (source evaluation tiers, triangulation rules, entity indexing standards, narrative hook assessment) -- it does NOT contain the 3-pass research pipeline (that lives in the researcher agent body).

Frontmatter:
```yaml
# ---
name: documentary-research
description: >-
  Documentary research domain expertise: source evaluation tiers, claim
  triangulation rules, entity indexing standards, and narrative hook
  assessment criteria. Use when researching a documentary topic, evaluating
  sources, or building research dossiers.
user-invocable: true
# ---
```

Required sections (in this order):
1. `# Documentary Research Expertise`
2. `## Phase 0: Context Loading` -- Instructions to read `insights.md` from `.claude/skills/documentary-research/insights.md` (even if empty) and channel identity via `@channel/channel.md`
3. `## Source Evaluation Tiers [DETERMINISTIC]` -- 5-tier table from V5 (court docs = Tier 1 through social media = Tier 5)
4. `## Source Triangulation Rules [DETERMINISTIC]` -- Sourced (2+ independent Tier 1-3), attributed (single source), unverified (Tier 4-5 only)
5. `## Entity Indexing Standards [DETERMINISTIC]` -- From V5: unique IDs, no duplicate entries, alias tracking, relationship mapping
6. `## Output Quality Standards [HEURISTIC]` -- Timeline consistency, entity uniqueness, coverage completeness, gap documentation
7. `## Narrative Hook Assessment [HEURISTIC]` -- Three axes: dramatic tension, factual grounding, visual potential
8. `## Script References` -- Mark as "available after Phase 6 integration": `editorial/researcher/cli.py survey`, `deepen`, `synthesize`
9. `## Reflection Phase` -- Re-read output, identify one insight, append to insights.md with `- [YYYY-MM-DD] insight text`

Per D-08, no line cap. Per D-04, script references are documented but marked future. Per D-10, no exemplars required.

**insights.md:** Create with the standard template:
```markdown
# Insights

Accumulated learnings from skill runs. Read at the start of every run.

## Lifecycle
- **Append:** One insight per run (never skip the reflection phase)
- **Merge:** Consolidate duplicates at 20+ entries
- **Promote:** Extract to SKILL.md when 3+ entries converge on same pattern

<!-- Append new insights below this line -->
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const s=fs.readFileSync(p.join(r,'.claude/skills/documentary-research/SKILL.md'),'utf8');const i=fs.existsSync(p.join(r,'.claude/skills/documentary-research/insights.md'));console.log('SKILL exists:',s.length>0);console.log('insights exists:',i);console.log('has frontmatter:',s.includes('user-invocable: true'));console.log('has Phase 0:',s.includes('Phase 0'));console.log('has Reflection:',s.includes('Reflection Phase'));console.log('has HEURISTIC:',s.includes('[HEURISTIC]'));console.log('has DETERMINISTIC:',s.includes('[DETERMINISTIC]'));console.log('has Phase 6 marker:',s.includes('Phase 6'));const ok=s.length>0&&i&&s.includes('user-invocable: true')&&s.includes('Phase 0')&&s.includes('Reflection Phase')&&s.includes('[HEURISTIC]')&&s.includes('[DETERMINISTIC]');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/documentary-research/SKILL.md exists and contains `name: documentary-research`
    - .claude/skills/documentary-research/SKILL.md contains `user-invocable: true`
    - .claude/skills/documentary-research/SKILL.md contains `## Phase 0: Context Loading`
    - .claude/skills/documentary-research/SKILL.md contains `[HEURISTIC]` tag on at least one section
    - .claude/skills/documentary-research/SKILL.md contains `[DETERMINISTIC]` tag on at least one section
    - .claude/skills/documentary-research/SKILL.md contains `## Reflection Phase`
    - .claude/skills/documentary-research/SKILL.md contains `Phase 6` in script references
    - .claude/skills/documentary-research/SKILL.md contains `Source Evaluation Tiers`
    - .claude/skills/documentary-research/SKILL.md contains `Source Triangulation Rules`
    - .claude/skills/documentary-research/insights.md exists and contains `Append new insights below this line`
  </acceptance_criteria>
  <done>documentary-research skill directory exists with SKILL.md containing adapted V5 domain knowledge (source tiers, triangulation, entity indexing, hooks) plus Claude Code structure (frontmatter, Phase 0, reflection, H/D tags) and insights.md with standard template</done>
</task>

<task type="auto">
  <name>Task 2: Create archive-search skill</name>
  <files>.claude/skills/archive-search/SKILL.md, .claude/skills/archive-search/insights.md</files>
  <read_first>
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/skills/archive-search.md (V5 source domain knowledge to adapt)
    - .claude/skills/agent-protocols/SKILL.md (pattern reference)
    - .claude/skills/documentary-research/SKILL.md (just-created skill as structure reference)
  </read_first>
  <action>
Create the directory `.claude/skills/archive-search/` and two files inside it.

**SKILL.md:** Adapt V5 archive-search.md domain knowledge into Claude Code skill format. Per D-01, this provides archive navigation expertise (Internet Archive patterns, Prelinger Collection rules, YouTube search strategies, rate limiting) -- NOT a search procedure.

Frontmatter:
```yaml
# ---
name: archive-search
description: >-
  Internet Archive, Prelinger Collection, and YouTube archive navigation
  expertise. Source discovery patterns, rate limiting rules, relevance
  scoring for archival footage and documents. Use when searching for
  historical footage, public domain media, or archival documents.
user-invocable: true
# ---
```

Required sections:
1. `# Archive Search Expertise`
2. `## Phase 0: Context Loading` -- Read insights.md from `.claude/skills/archive-search/insights.md`, read channel visual style: `@channel/VISUAL_STYLE_GUIDE.md`
3. `## Internet Archive Navigation [DETERMINISTIC]` -- From V5: search operators, collection targeting (prelinger, newsfilms, etc.), date range filtering, media type filtering, rate limiting (respect robots.txt, 1 req/sec)
4. `## Prelinger Collection Rules [DETERMINISTIC]` -- From V5: public domain verification, usage rights checking, quality tiers for archival footage
5. `## YouTube Search Strategies [HEURISTIC]` -- From V5: search term construction for historical topics, channel credibility assessment, Creative Commons filtering
6. `## Relevance Scoring [DETERMINISTIC]` -- From V5: criteria for evaluating search results (topic match, era accuracy, visual quality, rights status)
7. `## Script References` -- Mark as "available after Phase 6 integration": `media/ia_search.py` (Internet Archive search), `media/discover.py` (multi-source discovery)
8. `## Reflection Phase` -- Standard reflection template

**insights.md:** Standard template (same as documentary-research).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const s=fs.readFileSync(p.join(r,'.claude/skills/archive-search/SKILL.md'),'utf8');const i=fs.existsSync(p.join(r,'.claude/skills/archive-search/insights.md'));console.log('SKILL exists:',s.length>0);console.log('insights exists:',i);console.log('has frontmatter:',s.includes('user-invocable: true'));console.log('has Phase 0:',s.includes('Phase 0'));console.log('has Reflection:',s.includes('Reflection Phase'));console.log('has HEURISTIC:',s.includes('[HEURISTIC]'));console.log('has DETERMINISTIC:',s.includes('[DETERMINISTIC]'));const ok=s.length>0&&i&&s.includes('user-invocable: true')&&s.includes('Phase 0')&&s.includes('Reflection Phase')&&s.includes('[HEURISTIC]')&&s.includes('[DETERMINISTIC]');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/archive-search/SKILL.md exists and contains `name: archive-search`
    - .claude/skills/archive-search/SKILL.md contains `user-invocable: true`
    - .claude/skills/archive-search/SKILL.md contains `## Phase 0: Context Loading`
    - .claude/skills/archive-search/SKILL.md contains `Internet Archive` navigation content
    - .claude/skills/archive-search/SKILL.md contains `Prelinger` collection content
    - .claude/skills/archive-search/SKILL.md contains `[HEURISTIC]` and `[DETERMINISTIC]` tags
    - .claude/skills/archive-search/SKILL.md contains `## Reflection Phase`
    - .claude/skills/archive-search/SKILL.md contains `Phase 6` in script references
    - .claude/skills/archive-search/insights.md exists and contains `Append new insights below this line`
  </acceptance_criteria>
  <done>archive-search skill directory exists with SKILL.md containing adapted V5 archive navigation knowledge (IA patterns, Prelinger rules, YouTube strategies, scoring) plus Claude Code structure and insights.md</done>
</task>

<task type="auto">
  <name>Task 3: Create crawl4ai-scraping skill</name>
  <files>.claude/skills/crawl4ai-scraping/SKILL.md, .claude/skills/crawl4ai-scraping/insights.md</files>
  <read_first>
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/skills/crawl4ai-scraping.md (V5 source - 137 lines, most complex of the short skills)
    - .claude/skills/documentary-research/SKILL.md (structure reference)
  </read_first>
  <action>
Create the directory `.claude/skills/crawl4ai-scraping/` and two files inside it.

**SKILL.md:** Adapt V5 crawl4ai-scraping.md (137 lines) into Claude Code skill format. Per D-01, this provides web scraping domain expertise (extraction strategies, anti-bot handling, content selection) -- NOT a scraping procedure. Per D-04, no Python script dependencies required; the skill documents patterns for agents using Claude's native WebFetch/Bash capabilities and references crawl4ai scripts as future Phase 6 integration.

Frontmatter:
```yaml
# ---
name: crawl4ai-scraping
description: >-
  Web scraping and content extraction expertise for JS-heavy research
  sites. Extraction strategies, anti-bot handling, content selection
  patterns, and rate limiting rules. Use when gathering information from
  websites that require browser automation or structured data extraction.
user-invocable: true
# ---
```

Required sections:
1. `# Web Scraping Expertise`
2. `## Phase 0: Context Loading` -- Read insights.md from `.claude/skills/crawl4ai-scraping/insights.md`
3. `## Extraction Strategy Selection [DETERMINISTIC]` -- From V5: when to use simple fetch vs browser automation, CSS selector patterns, JSON-LD extraction, metadata harvesting
4. `## Anti-Bot Handling [DETERMINISTIC]` -- From V5: rate limiting (respect robots.txt), user-agent rotation, request delays, CAPTCHA detection and abort
5. `## Content Selection Patterns [HEURISTIC]` -- From V5: identifying main content vs boilerplate, table extraction, list extraction, date/attribution extraction
6. `## Output Formatting [DETERMINISTIC]` -- From V5: clean markdown output, source URL preservation, timestamp recording, deduplication
7. `## Platform Notes` -- Windows 11 + conda environment. Crawl4ai scripts use Python. Reference `C:/Users/iorda/miniconda3/envs/perception-models/python.exe` for GPU-dependent operations, standard Python for scraping.
8. `## Script References` -- Mark as "available after Phase 6 integration": `media/crawl_images.py`, `media/wiki_screenshots.py`, reference to crawl4ai library patterns
9. `## Reflection Phase` -- Standard reflection template

**insights.md:** Standard template.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const s=fs.readFileSync(p.join(r,'.claude/skills/crawl4ai-scraping/SKILL.md'),'utf8');const i=fs.existsSync(p.join(r,'.claude/skills/crawl4ai-scraping/insights.md'));console.log('SKILL exists:',s.length>0);console.log('insights exists:',i);console.log('has frontmatter:',s.includes('user-invocable: true'));console.log('has Phase 0:',s.includes('Phase 0'));console.log('has Reflection:',s.includes('Reflection Phase'));console.log('has HEURISTIC:',s.includes('[HEURISTIC]'));console.log('has DETERMINISTIC:',s.includes('[DETERMINISTIC]'));const ok=s.length>0&&i&&s.includes('user-invocable: true')&&s.includes('Phase 0')&&s.includes('Reflection Phase')&&s.includes('[HEURISTIC]')&&s.includes('[DETERMINISTIC]');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/crawl4ai-scraping/SKILL.md exists and contains `name: crawl4ai-scraping`
    - .claude/skills/crawl4ai-scraping/SKILL.md contains `user-invocable: true`
    - .claude/skills/crawl4ai-scraping/SKILL.md contains `## Phase 0: Context Loading`
    - .claude/skills/crawl4ai-scraping/SKILL.md contains extraction strategy content
    - .claude/skills/crawl4ai-scraping/SKILL.md contains anti-bot handling content
    - .claude/skills/crawl4ai-scraping/SKILL.md contains `[HEURISTIC]` and `[DETERMINISTIC]` tags
    - .claude/skills/crawl4ai-scraping/SKILL.md contains `## Reflection Phase`
    - .claude/skills/crawl4ai-scraping/SKILL.md contains `Phase 6`
    - .claude/skills/crawl4ai-scraping/insights.md exists and contains `Append new insights below this line`
  </acceptance_criteria>
  <done>crawl4ai-scraping skill directory exists with SKILL.md containing adapted V5 web scraping expertise (extraction strategies, anti-bot patterns, content selection, output formatting) plus Claude Code structure and insights.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates static markdown files only. No user input processing, no network access, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Script path references in SKILL.md | accept | Skills document future script paths (editorial/, media/) but scripts do not exist yet. No sensitive paths exposed. V5 paths are NOT included in skills per D-05. |
| T-02-02 | Tampering | insights.md append-only lifecycle | accept | insights.md is a local markdown file in a git repo. Tampering risk is equivalent to any tracked file. No mitigation needed beyond git history. |
</threat_model>

<verification>
After all three tasks complete:
1. Three directories exist under `.claude/skills/`: documentary-research, archive-search, crawl4ai-scraping
2. Each directory contains SKILL.md with valid frontmatter (`name`, `description`, `user-invocable: true`)
3. Each SKILL.md has Phase 0, at least one [HEURISTIC] section, at least one [DETERMINISTIC] section, Reflection Phase, and Script References with Phase 6 marker
4. Each directory contains insights.md with standard template
5. No V5 file paths (`.pi/multi-team/`) appear in any SKILL.md -- only V0.6 target paths
</verification>

<success_criteria>
- Three skill directories created with correct structure
- All three skills are user-invocable via their slash commands
- Domain knowledge adapted from V5 without duplicating agent procedures
- Phase 0 and Reflection Phase present in all skills
- [HEURISTIC] and [DETERMINISTIC] tags present in all skills
</success_criteria>

<output>
After completion, create `.planning/phases/02-skills-library/02-01-SUMMARY.md`
</output>
