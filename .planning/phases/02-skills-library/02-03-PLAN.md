---
phase: 02-skills-library
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/autoresearch/SKILL.md
  - .claude/skills/autoresearch/insights.md
  - .claude/skills/structured-output/SKILL.md
  - .claude/skills/structured-output/insights.md
autonomous: true
requirements:
  - SKIL-07
  - SKIL-08
  - SKIL-09
  - SKIL-10
  - SKIL-11
  - SKIL-12
must_haves:
  truths:
    - "User can invoke /autoresearch and receive Karpathy-style iterative research loop expertise"
    - "User can invoke /structured-output and receive formatting rules for reports and JSON outputs"
    - "Both skills load insights.md at Phase 0 and append insights after completion"
  artifacts:
    - path: ".claude/skills/autoresearch/SKILL.md"
      provides: "Iterative research loop domain expertise"
      contains: "user-invocable: true"
    - path: ".claude/skills/autoresearch/insights.md"
      provides: "Learning accumulation for autoresearch"
      contains: "Append new insights below this line"
    - path: ".claude/skills/structured-output/SKILL.md"
      provides: "Structured output formatting domain expertise"
      contains: "user-invocable: true"
    - path: ".claude/skills/structured-output/insights.md"
      provides: "Learning accumulation for structured output"
      contains: "Append new insights below this line"
  key_links:
    - from: ".claude/skills/autoresearch/SKILL.md"
      to: ".claude/skills/autoresearch/insights.md"
      via: "Phase 0 context loading instruction"
      pattern: "Read.*insights\\.md"
    - from: ".claude/skills/structured-output/SKILL.md"
      to: ".claude/skills/structured-output/insights.md"
      via: "Phase 0 context loading instruction"
      pattern: "Read.*insights\\.md"
---

<objective>
Build two process domain skills: autoresearch (SKIL-07) and structured-output (SKIL-08). Autoresearch is the most complex skill adaptation (V5 source is 560 lines) providing Karpathy-style iterative improvement loop expertise. Structured-output provides formatting rules for reports and data outputs.

Purpose: Autoresearch is a cross-domain skill used by the meta agent and any agent performing deep iterative research. Structured-output is used by strategy, editorial, and meta agents for consistent report formatting. Both serve multiple agents per D-03.

Output: 2 skill directories under `.claude/skills/`, each with `SKILL.md` and `insights.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-skills-library/02-CONTEXT.md
@.planning/phases/02-skills-library/02-RESEARCH.md

<interfaces>
<!-- Skill structure pattern -->
Required SKILL.md sections (in order):
1. YAML frontmatter (name, description, user-invocable: true)
2. Phase 0: Context Loading (read insights.md + relevant channel docs)
3. Domain Knowledge sections tagged [HEURISTIC] or [DETERMINISTIC]
4. Script References (marked "available after Phase 6 integration")
5. Reflection Phase (re-read output, append one-line insight to insights.md)

<!-- insights.md template -->
```markdown
# Insights

Accumulated learnings from skill runs. Read at the start of every run.

## Lifecycle
- **Append:** One insight per run (never skip the reflection phase)
- **Merge:** Consolidate duplicates at 20+ entries
- **Promote:** Extract to SKILL.md when 3+ entries converge on same pattern

<!-- Append new insights below this line -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create autoresearch skill</name>
  <files>.claude/skills/autoresearch/SKILL.md, .claude/skills/autoresearch/insights.md</files>
  <read_first>
    - superpowers:writing-skills (invoke via Skill tool -- PRIMARY methodology reference for skill creation per D-09)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/skills/karpathy-autoresearch.md (V5 source - 560 lines, the largest V5 skill file)
    - .claude/skills/agent-protocols/SKILL.md (pattern reference)
    - .planning/phases/02-skills-library/02-RESEARCH.md (V5 to V0.6 mapping notes)
  </read_first>
  <action>
Create the directory `.claude/skills/autoresearch/` and two files inside it.

**SKILL.md:** Adapt V5 karpathy-autoresearch.md (560 lines) into Claude Code skill format. This is the most complex adaptation -- the V5 file contains a full iterative research loop methodology. Per D-01, the skill provides the DOMAIN EXPERTISE about iterative research (loop design patterns, convergence criteria, quality gate definitions, diminishing returns detection) -- NOT a step-by-step procedure that duplicates what an agent body would contain.

CRITICAL: The V5 file is 560 lines. Extract the core domain knowledge (loop patterns, quality gates, convergence criteria, research depth calibration) and reframe as expertise guidance. DO NOT copy the entire 560-line procedure verbatim. Per D-08 there is no line cap, but the skill should be expertise-focused, not procedure-focused.

Frontmatter:
```yaml
# ---
name: autoresearch
description: >-
  Karpathy-style iterative research loop expertise. Convergence criteria,
  quality gate definitions, diminishing returns detection, and research
  depth calibration patterns. Use when conducting deep iterative research
  that requires multiple refinement passes or when evaluating research
  completeness.
user-invocable: true
# ---
```

Required sections:
1. `# Autoresearch Expertise`
2. `## Phase 0: Context Loading` -- Read insights.md from `.claude/skills/autoresearch/insights.md`
3. `## Iterative Loop Design [DETERMINISTIC]` -- From V5: the core loop structure (research -> evaluate -> identify gaps -> refine -> repeat). Define loop entry conditions, iteration limits (prevent infinite loops), and state tracking between iterations.
4. `## Convergence Criteria [DETERMINISTIC]` -- From V5: when to stop iterating. Metrics: source coverage saturation (new searches yield <10% new information), claim verification completeness (all claims at Tier 1-3 sourcing), entity coverage (no unresolved aliases), timeline consistency (no contradictions remain).
5. `## Quality Gates [DETERMINISTIC]` -- From V5: checkpoints between iterations. Gate 1: minimum source diversity (at least 3 independent source types). Gate 2: factual density threshold (claims per section). Gate 3: gap coverage (identified gaps have been addressed or explicitly documented as unresolvable).
6. `## Diminishing Returns Detection [HEURISTIC]` -- From V5: signals that further iteration is wasteful. Indicators: search result overlap >80%, new sources confirm existing claims without adding detail, time-per-new-fact increasing exponentially.
7. `## Research Depth Calibration [HEURISTIC]` -- From V5: how to calibrate depth based on topic type. Well-documented topics (major historical events) need fewer passes. Obscure topics (local mysteries, cold cases) need more passes with creative source strategies. Controversial topics need extra triangulation passes.
8. `## Breadth-First vs Depth-First Strategy [HEURISTIC]` -- From V5: when to go wide (survey phase, topic scoping) vs deep (specific claim verification, source chain following). Switching criteria between modes.
9. `## Script References` -- Mark as "available after Phase 6 integration": No direct V5 script maps to autoresearch (it is a meta-methodology), but reference `editorial/researcher/cli.py` as the tool that executes research passes.
10. `## Reflection Phase` -- Standard reflection template

**insights.md:** Standard template.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const s=fs.readFileSync(p.join(r,'.claude/skills/autoresearch/SKILL.md'),'utf8');const i=fs.existsSync(p.join(r,'.claude/skills/autoresearch/insights.md'));console.log('SKILL exists:',s.length>0);console.log('insights exists:',i);console.log('has frontmatter:',s.includes('user-invocable: true'));console.log('has Phase 0:',s.includes('Phase 0'));console.log('has Reflection:',s.includes('Reflection Phase'));console.log('has HEURISTIC:',s.includes('[HEURISTIC]'));console.log('has DETERMINISTIC:',s.includes('[DETERMINISTIC]'));console.log('has convergence:',s.includes('Convergence'));const ok=s.length>0&&i&&s.includes('user-invocable: true')&&s.includes('Phase 0')&&s.includes('Reflection Phase')&&s.includes('[HEURISTIC]')&&s.includes('[DETERMINISTIC]');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/autoresearch/SKILL.md exists and contains `name: autoresearch`
    - .claude/skills/autoresearch/SKILL.md contains `user-invocable: true`
    - .claude/skills/autoresearch/SKILL.md contains `## Phase 0: Context Loading`
    - .claude/skills/autoresearch/SKILL.md contains `Iterative Loop Design`
    - .claude/skills/autoresearch/SKILL.md contains `Convergence Criteria`
    - .claude/skills/autoresearch/SKILL.md contains `Quality Gates`
    - .claude/skills/autoresearch/SKILL.md contains `Diminishing Returns`
    - .claude/skills/autoresearch/SKILL.md contains `[HEURISTIC]` and `[DETERMINISTIC]` tags
    - .claude/skills/autoresearch/SKILL.md contains `## Reflection Phase`
    - .claude/skills/autoresearch/SKILL.md does NOT contain step-by-step numbered agent procedures (domain expertise only per D-01)
    - .claude/skills/autoresearch/insights.md exists and contains `Append new insights below this line`
  </acceptance_criteria>
  <done>autoresearch skill directory exists with SKILL.md containing adapted V5 iterative research loop expertise (loop design, convergence, quality gates, diminishing returns, depth calibration) reframed as domain knowledge rather than procedure, plus insights.md</done>
</task>

<task type="auto">
  <name>Task 2: Create structured-output skill</name>
  <files>.claude/skills/structured-output/SKILL.md, .claude/skills/structured-output/insights.md</files>
  <read_first>
    - superpowers:writing-skills (invoke via Skill tool -- PRIMARY methodology reference for skill creation per D-09)
    - D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V5/.pi/multi-team/skills/structured-output.md (V5 source - 60 lines, originally a behavioral skill, needs reframing as domain skill)
    - .claude/skills/agent-protocols/SKILL.md (pattern reference -- note that structured-output must NOT overlap with agent-protocols behavioral content)
  </read_first>
  <action>
Create the directory `.claude/skills/structured-output/` and two files inside it.

**SKILL.md:** Adapt V5 structured-output.md into Claude Code skill format. The V5 version was a behavioral skill (formatting rules). Per D-01, reframe as domain expertise about output structuring -- when to use which format, how to structure reports for the documentary pipeline, JSON schema patterns for machine-readable outputs. Per the research document: "frame as domain skill" (not behavioral).

Frontmatter:
```yaml
# ---
name: structured-output
description: >-
  Structured output formatting expertise for documentary pipeline reports
  and data files. Report structure templates, JSON schema patterns, file
  vs chat output rules, and formatting conventions. Use when generating
  research dossiers, analysis reports, or machine-readable data outputs.
user-invocable: true
# ---
```

Required sections:
1. `# Structured Output Expertise`
2. `## Phase 0: Context Loading` -- Read insights.md from `.claude/skills/structured-output/insights.md`
3. `## Output Format Selection [DETERMINISTIC]` -- From V5: when to write to file vs respond in chat. Files for: research dossiers, scripts, analysis reports, edit sheets, any output >50 lines. Chat for: summaries, status updates, quick answers, confirmations.
4. `## Report Structure Templates [DETERMINISTIC]` -- From V5: standard templates for the documentary pipeline outputs. Research dossier template (title, executive summary, timeline, entity index, source bibliography). Analysis report template (objective, methodology, findings, recommendations). Script template (title card, cold open, act structure, credits).
5. `## JSON Schema Patterns [DETERMINISTIC]` -- Rules for machine-readable outputs: topic briefs (strategy output), asset manifests (media output), edit sheet entries (compiler output). Consistent key naming (snake_case), ISO 8601 timestamps, relative paths.
6. `## Formatting Conventions [DETERMINISTIC]` -- From V5: markdown heading hierarchy, table formatting rules, code block usage, link formatting, citation format (`[Source Name](URL) (Tier N)`).
7. `## Content Organization [HEURISTIC]` -- Principles for organizing long-form content: lead with the most important finding, group related items, use progressive disclosure (summary -> detail -> appendix), handle uncertainty explicitly (flag unverified claims).
8. `## Script References` -- Mark as "available after Phase 6 integration": No direct scripts for this domain skill. Reference general pipeline output patterns.
9. `## Reflection Phase` -- Standard reflection template

**insights.md:** Standard template.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const r=p.resolve('D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6');const s=fs.readFileSync(p.join(r,'.claude/skills/structured-output/SKILL.md'),'utf8');const i=fs.existsSync(p.join(r,'.claude/skills/structured-output/insights.md'));console.log('SKILL exists:',s.length>0);console.log('insights exists:',i);console.log('has frontmatter:',s.includes('user-invocable: true'));console.log('has Phase 0:',s.includes('Phase 0'));console.log('has Reflection:',s.includes('Reflection Phase'));console.log('has HEURISTIC:',s.includes('[HEURISTIC]'));console.log('has DETERMINISTIC:',s.includes('[DETERMINISTIC]'));const ok=s.length>0&&i&&s.includes('user-invocable: true')&&s.includes('Phase 0')&&s.includes('Reflection Phase')&&s.includes('[HEURISTIC]')&&s.includes('[DETERMINISTIC]');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/structured-output/SKILL.md exists and contains `name: structured-output`
    - .claude/skills/structured-output/SKILL.md contains `user-invocable: true`
    - .claude/skills/structured-output/SKILL.md contains `## Phase 0: Context Loading`
    - .claude/skills/structured-output/SKILL.md contains `Output Format Selection`
    - .claude/skills/structured-output/SKILL.md contains `Report Structure Templates`
    - .claude/skills/structured-output/SKILL.md contains `JSON Schema Patterns`
    - .claude/skills/structured-output/SKILL.md contains `[HEURISTIC]` and `[DETERMINISTIC]` tags
    - .claude/skills/structured-output/SKILL.md contains `## Reflection Phase`
    - .claude/skills/structured-output/SKILL.md does NOT contain behavioral protocols (those are in agent-protocols)
    - .claude/skills/structured-output/insights.md exists and contains `Append new insights below this line`
  </acceptance_criteria>
  <done>structured-output skill directory exists with SKILL.md containing adapted V5 formatting knowledge reframed as domain expertise (format selection, templates, JSON schemas, conventions, content organization) plus insights.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates static markdown files only. No user input processing, no network access, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Script path references in SKILL.md | accept | Skills document future script paths but scripts do not exist yet. No sensitive paths exposed. |
</threat_model>

<verification>
After both tasks complete:
1. Two directories exist under `.claude/skills/`: autoresearch, structured-output
2. Each directory contains SKILL.md with valid frontmatter (`name`, `description`, `user-invocable: true`)
3. Each SKILL.md has Phase 0, at least one [HEURISTIC] section, at least one [DETERMINISTIC] section, and Reflection Phase
4. Autoresearch SKILL.md contains expertise about iterative loops, NOT step-by-step agent procedures
5. Structured-output SKILL.md contains formatting expertise, NOT behavioral protocols (those are in agent-protocols)
6. Each directory contains insights.md with standard template
</verification>

<success_criteria>
- Two skill directories created with correct structure
- Both skills are user-invocable via their slash commands
- Autoresearch adapts 560-line V5 source into focused domain expertise (not procedure duplication)
- Structured-output reframes V5 behavioral skill as domain expertise
- Phase 0 and Reflection Phase present in both skills
- [HEURISTIC] and [DETERMINISTIC] tags present in both skills
</success_criteria>

<output>
After completion, create `.planning/phases/02-skills-library/02-03-SUMMARY.md`
</output>
