---
phase: 05-memory-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .claude/skills/evolve/SKILL.md
  - .claude/agents/observer.md
autonomous: true
requirements: [EVLV-04, MEML-02]
user_setup: []

must_haves:
  truths:
    - "/evolve skill flow includes decay step after promote and before summary display (D-02)"
    - "/evolve unified summary has three sections: Promoted, Expired, Capacity warnings (D-06)"
    - "User can remove expired entries by number and kept entries auto-upgrade to HIGH (D-05)"
    - "When capacity warnings exist, /evolve dispatches observer with consolidation prompt (D-07)"
    - "Observer recognizes consolidation mode and produces rewritten Permanent block (D-09)"
    - "Observer skips normal 10-step pipeline when in consolidation mode"
  artifacts:
    - path: ".claude/skills/evolve/SKILL.md"
      provides: "Extended /evolve flow with decay, consolidation, unified summary"
      contains: "Step 4: Decay Scan"
    - path: ".claude/agents/observer.md"
      provides: "Consolidation mode documentation"
      contains: "Consolidation Mode"
  key_links:
    - from: ".claude/skills/evolve/SKILL.md"
      to: ".claude/scripts/memory/evolve.js"
      via: "Bash tool invocation"
      pattern: "node .claude/scripts/memory/evolve.js decay"
    - from: ".claude/skills/evolve/SKILL.md"
      to: ".claude/agents/observer.md"
      via: "Agent tool dispatch with consolidation prompt"
      pattern: "Consolidate the ## Permanent section"
    - from: ".claude/skills/evolve/SKILL.md"
      to: ".claude/scripts/memory/evolve.js"
      via: "Bash tool invocation for removal and upgrade"
      pattern: "evolve.js decay-remove"
---

<objective>
Extend the /evolve skill with decay scanning, consolidation dispatch, and unified summary. Add consolidation mode to the observer agent definition.

Purpose: Wires the decay engine (Plan 01) into the user-facing /evolve flow so expired entries are surfaced, capacity warnings trigger LLM consolidation, and the user gets a single unified interaction covering promotions, expirations, and capacity management.

Output: Updated SKILL.md with 10 steps (was 8), updated observer.md with Consolidation Mode section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-memory-lifecycle/05-CONTEXT.md
@.planning/phases/05-memory-lifecycle/05-RESEARCH.md
@.planning/phases/05-memory-lifecycle/05-PATTERNS.md
@.planning/phases/05-memory-lifecycle/05-01-SUMMARY.md

<interfaces>
<!-- Decay subcommand JSON output schema from Plan 01 -->
<!-- evolve.js decay output: { command, expired: [{ path, type, entries: [{ global_index, line, text, confidence, age_days, ttl_days }] }], capacity_warnings: [{ path, type, lines }], total_expired } -->
<!-- evolve.js decay-remove output: { command, removed: [{ global_index, path, entry }], total, errors? } -->
<!-- evolve.js decay-upgrade output: { command, upgraded: [{ global_index, path, entry }], total, errors? } -->

<!-- Current SKILL.md step numbering (8 steps):
Step 1: Dispatch Observer | Step 2: Scan | Step 3: Promote | Step 4: Display Summary | Step 5: Revert Prompt | Step 6: Execute Reverts | Step 7: Commit | Step 8: Done

New step numbering (10 steps) after insertion per D-02:
Step 1: Dispatch Observer (unchanged) | Step 2: Scan (unchanged) | Step 3: Promote (unchanged) | Step 4: Decay Scan (NEW) | Step 5: Consolidation Check (NEW) | Step 6: Display Unified Summary (MODIFIED) | Step 7: User Interaction (MODIFIED) | Step 8: Execute Actions (MODIFIED) | Step 9: Commit (renumbered+extended) | Step 10: Done (renumbered+updated) -->

<!-- Current observer.md structure:
Lines 1-21: YAML frontmatter | Lines 23-29: Identity | Lines 31-38: Protocol Overrides | Lines 40-49: Instruction Priority | Lines 51-235: Processing Pipeline (Steps 1-10) | Lines 237-244: Scope-Test Questions | Lines 246-251: Entry Format Reference | Lines 253-303: Few-Shot Examples | Lines 305-318: Guardrails | Lines 320-339: Completion Report -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend evolve SKILL.md with decay, consolidation, and unified summary steps</name>
  <files>.claude/skills/evolve/SKILL.md</files>
  <read_first>
    - .claude/skills/evolve/SKILL.md (current full source, 189 lines)
    - .planning/phases/05-memory-lifecycle/05-CONTEXT.md (D-01 through D-09 decisions)
    - .planning/phases/05-memory-lifecycle/05-RESEARCH.md (Updated /evolve Skill Flow section, Pitfall 2 on index collision, Pitfall 3 on line index corruption)
    - .planning/phases/05-memory-lifecycle/05-PATTERNS.md (SKILL.md step pattern, display summary pattern, user interaction pattern, observer dispatch pattern, commit pattern)
    - .planning/phases/05-memory-lifecycle/05-01-SUMMARY.md (Plan 01 output for decay JSON schema)
  </read_first>
  <action>
    Rewrite .claude/skills/evolve/SKILL.md from the current 8-step flow to a 10-step flow. Preserve Steps 1-3 verbatim (Dispatch Observer, Scan, Promote). Replace Steps 4-8 with Steps 4-10 as specified below. Follow the existing step format pattern exactly: `## Step N: Name` heading, prose description, bash code block with exact command, JSON parsing instructions, conditional logic, display format.

    YAML frontmatter `description` field: change to "Memory evolution command. Dispatches @observer for new events, auto-promotes all Pending Review entries to Permanent sections, scans for expired entries via confidence-based decay, triggers LLM consolidation at capacity threshold, shows a unified summary, and lets the user manage entries by number."

    Intro paragraph: after "Pending Review to Permanent memory" add ", flags expired entries for removal or re-evaluation, and triggers LLM-powered consolidation when files approach capacity limits."

    Constraints section: add new bullet "- Do NOT auto-remove expired entries. Flag them for user decision per D-04."

    NEW Step 4 (Decay Scan): Run `node .claude/scripts/memory/evolve.js decay` via Bash tool. Parse JSON output into `decay_result`. The result contains: `expired` (array of files with expired entries, each entry has `global_index`, `confidence`, `age_days`, `ttl_days`), `capacity_warnings` (files at/above 180 lines), `total_expired` (count).

    NEW Step 5 (Consolidation Check): If `decay_result.capacity_warnings` is empty, skip to Step 6. For each warned file: (1) Read the file's current content, (2) extract the ## Permanent section text, (3) dispatch @observer via Agent tool with a consolidation prompt that starts with "Consolidate the ## Permanent section of {file_path}" and includes: the line count and cap, rewrite rules (merge same-topic entries, remove superseded entries, tighten wording, preserve [HIGH] entries, preserve entry format), and the full current ## Permanent content. The prompt must end with "Output ONLY the complete rewritten ## Permanent section (heading + entries). Do not include other sections. Do not explain your changes." (4) Parse observer response for the rewritten block. (5) Show before/after comparison to user with "Apply this consolidation? (yes/no)" prompt. (6) If yes: use Edit tool to replace ## Permanent section in the file, store path for Step 9 commit. (7) If no: skip, capacity warning still shown in summary.

    MODIFIED Step 6 (Display Unified Summary, was old Step 4): Show observer stats first (same as before). Then display unified summary with three sections and continuous numbering. Index scheme: promoted entries use `global_index` from `promote_result` (range 1..N where N = promote_result.total). Expired entries use display number = `promote_result.total + decay_entry.global_index`. Section 1 "Promoted entries:" lists each as `{display_num}. [{skill-or-agent-name}] [{CONF}] {insight text}`. Section 2 "Expired entries (flagged for review):" lists each as `{display_num}. [{agent-name}] [{CONF}] {insight text} (age: {age_days}d, TTL: {ttl_days}d)`. Section 3 "Capacity warnings:" lists each as `{file_path}: {lines}/200 lines`. If all three sections are empty (promote total 0, decay total_expired 0, no capacity warnings), display "No promoted entries. No expired entries. No capacity warnings." and skip to Step 9. If `promote_result.permanent_sections_created > 0`, append "(Created ## Permanent section in {N} files)".

    MODIFIED Step 7 (User Interaction, was old Step 5): Display "Revert any promoted? Remove any expired? (numbers, or Enter to keep all)". Wait for input. If Enter (empty): skip to Step 9 -- all expired entries will be upgraded to HIGH in Step 8. If numbers provided: parse into integer list, classify each: numbers 1..promote_result.total are promoted reverts, numbers (promote_result.total+1)..(promote_result.total+decay_result.total_expired) are expired removals, numbers outside both ranges get a warning and are skipped.

    MODIFIED Step 8 (Execute Actions, was old Step 6): Three sequential operations. (a) Revert promoted: if any numbers fell in promoted range, run `node .claude/scripts/memory/evolve.js revert {indices}`, store as `revert_result`. (b) Remove expired: if any numbers fell in expired range, map display numbers back to decay global indices via `decay_global_index = display_number - promote_result.total`, run `node .claude/scripts/memory/evolve.js decay-remove {indices}`, store as `remove_result`. (c) Upgrade kept: identify decay global indices NOT in removal list (these are "kept" entries), run `node .claude/scripts/memory/evolve.js decay-upgrade {kept_indices}`, store as `upgrade_result`. If user pressed Enter (kept all), ALL expired entries get upgraded -- run decay-upgrade with all global indices from `decay_result.expired[*].entries[*].global_index`. Display action summary listing non-zero counts only.

    RENUMBERED Step 9 (Commit Changes, was old Step 7): Same git add pattern but extended to include paths from decay-remove, decay-upgrade, and consolidation. Deduplicate paths. Commit message: no decay actions = "evolve: promote {N} entries to permanent memory", with decay = "evolve: promote {N}, decay {removed}rem/{upgraded}upg entries", with consolidation = append ", consolidate {count} files".

    RENUMBERED Step 10 (Done, was old Step 8): Display "Evolution complete. {net_promoted} entries promoted, {reverted} reverted, {removed} expired removed, {upgraded} upgraded to HIGH." where net_promoted = promote_result.total - revert_result.total. Omit zero-count items.

    IMPORTANT: When the decay_result has zero expired and zero capacity warnings, Steps 5/7/8 should gracefully no-op (no consolidation needed, user interaction only covers promoted entries using the original revert-only prompt "Revert any? (enter numbers, or press Enter to keep all)" from old Step 5).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.claude/skills/evolve/SKILL.md','utf8'); const checks=['Step 4: Decay Scan','Step 5: Consolidation','Step 6:','Step 7:','Step 8: Execute','decay-remove','decay-upgrade','Consolidate the']; const missing=checks.filter(s=>!c.includes(s)); if(missing.length){console.log('MISSING:',missing);process.exit(1)}else{console.log('All checks passed');process.exit(0)}"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/evolve/SKILL.md` contains "Step 4: Decay Scan"
    - `.claude/skills/evolve/SKILL.md` contains "Step 5: Consolidation"
    - `.claude/skills/evolve/SKILL.md` contains "Step 6: Display Unified Summary" (or similar with "Unified Summary")
    - `.claude/skills/evolve/SKILL.md` contains "Step 7: User Interaction"
    - `.claude/skills/evolve/SKILL.md` contains "Step 8: Execute Actions"
    - `.claude/skills/evolve/SKILL.md` contains "Step 9: Commit"
    - `.claude/skills/evolve/SKILL.md` contains "Step 10: Done"
    - `.claude/skills/evolve/SKILL.md` contains "node .claude/scripts/memory/evolve.js decay"
    - `.claude/skills/evolve/SKILL.md` contains "decay-remove"
    - `.claude/skills/evolve/SKILL.md` contains "decay-upgrade"
    - `.claude/skills/evolve/SKILL.md` contains "Consolidate the ## Permanent section"
    - `.claude/skills/evolve/SKILL.md` description field mentions "decay" and "consolidation"
    - `.claude/skills/evolve/SKILL.md` contains "Do NOT auto-remove expired entries"
    - Steps 1-3 are preserved from original (Dispatch Observer, Scan, Promote)
  </acceptance_criteria>
  <done>
    evolve SKILL.md has 10 steps covering the full lifecycle: observe, scan, promote, decay, consolidation, unified summary, interaction, execute, commit, done. Decay is invoked after promote per D-02. Unified summary has three sections per D-06. Expired entries are flagged not auto-removed per D-04. Kept entries upgrade to HIGH per D-05. Consolidation dispatches observer at 180 lines per D-07/D-08/D-09.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add consolidation mode to observer agent definition</name>
  <files>.claude/agents/observer.md</files>
  <read_first>
    - .claude/agents/observer.md (current full source, 339 lines)
    - .planning/phases/05-memory-lifecycle/05-CONTEXT.md (D-07, D-09 decisions)
    - .planning/phases/05-memory-lifecycle/05-RESEARCH.md (Pattern 4: Observer Consolidation Dispatch, Pitfall 6: Consolidation vs Learning Extraction prompt confusion)
    - .planning/phases/05-memory-lifecycle/05-PATTERNS.md (observer.md analog section)
  </read_first>
  <action>
    Make three targeted edits to .claude/agents/observer.md:

    1. YAML frontmatter `description` field (line 3-8): Append to existing description text: " In consolidation mode, rewrites a memory file's ## Permanent section to reduce size while preserving essential knowledge."

    2. Identity section (line 23-29): After the existing paragraph ending "...report results when done.", add a new sentence: "When dispatched with a consolidation prompt, you rewrite a memory file's ## Permanent section to be more concise while preserving all essential knowledge."

    3. Add a new `## Consolidation Mode` section. Insert it AFTER the `## Completion Report` section (after line 339, at end of file). Content:

    ---BEGIN CONSOLIDATION MODE SECTION---

    ## Consolidation Mode

    When your dispatch prompt begins with "Consolidate the ## Permanent section", you are in consolidation mode. **Skip the entire 10-step Processing Pipeline above.**

    Instead, follow these steps:

    ### C1: Read the Target File

    The dispatch prompt contains the file path and the current ## Permanent section content. Read the file to confirm its current state matches what was provided.

    ### C2: Analyze Entries

    Review all entries in the ## Permanent section. Identify:
    - Entries that cover the same topic (merge candidates)
    - Entries superseded by later, more specific entries (removal candidates)
    - Entries with verbose wording that can be tightened
    - [HIGH] confidence entries (MUST be preserved -- never remove these)

    ### C3: Rewrite the Section

    Produce a rewritten ## Permanent section that:
    - Merges entries covering the same topic into a single, tighter entry
    - Removes entries superseded by later entries
    - Tightens wording without losing meaning
    - Preserves ALL [HIGH] confidence entries unchanged
    - Preserves the entry format conventions:
      - MEMORY.md: `- [CONF] agent: insight text`
      - insights.md: `- [YYYY-MM-DD] [CONF] insight text`
    - Maintains the `## Permanent` heading
    - Targets at least 20% line reduction from the original

    ### C4: Output

    Output ONLY the complete rewritten ## Permanent section (heading + all entries). Do not include other sections (## Pending Review, file preamble, etc.). Do not explain your changes or reasoning.

    ---END CONSOLIDATION MODE SECTION---

    Do NOT modify any other section of observer.md. The Processing Pipeline (Steps 1-10), Scope-Test Questions, Entry Format Reference, Few-Shot Examples, Guardrails, and Completion Report sections must remain unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.claude/agents/observer.md','utf8'); const checks=['Consolidation Mode','consolidation mode','Skip the entire 10-step','Preserve ALL [HIGH]','MEMORY.md:','insights.md:','C1: Read','C2: Analyze','C3: Rewrite','C4: Output']; const missing=checks.filter(s=>!c.includes(s)); if(missing.length){console.log('MISSING:',missing);process.exit(1)}else{console.log('All checks passed');process.exit(0)}"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/agents/observer.md` contains "## Consolidation Mode"
    - `.claude/agents/observer.md` contains "Skip the entire 10-step Processing Pipeline"
    - `.claude/agents/observer.md` contains "C1: Read the Target File"
    - `.claude/agents/observer.md` contains "C2: Analyze Entries"
    - `.claude/agents/observer.md` contains "C3: Rewrite the Section"
    - `.claude/agents/observer.md` contains "C4: Output"
    - `.claude/agents/observer.md` contains "Preserve ALL [HIGH] confidence entries"
    - `.claude/agents/observer.md` description field mentions "consolidation mode"
    - `.claude/agents/observer.md` Identity section mentions "consolidation prompt"
    - The original Processing Pipeline (Steps 1-10) is unchanged
    - The Guardrails section is unchanged
    - The Completion Report section is unchanged
  </acceptance_criteria>
  <done>
    observer.md has a Consolidation Mode section with 4 steps (C1-C4) that instructs the observer to bypass its normal 10-step pipeline and produce a rewritten ## Permanent block. Description and Identity sections updated to acknowledge the dual-mode capability. All existing sections preserved unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> SKILL.md number parsing | User provides entry numbers for revert/remove; parsed as integers and classified by range |
| Observer output -> file write | Consolidation mode: observer produces markdown that gets written to memory file via Edit tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | User input in Step 7 | mitigate | Parse as integers, classify by range (promoted vs expired), reject out-of-range with warning -- same pattern as existing Step 5/6 revert logic |
| T-05-06 | Tampering | Observer consolidation output | mitigate | User sees before/after diff and explicitly approves per D-09; observer output is never auto-written |
| T-05-07 | Elevation of Privilege | Observer consolidation bypassing pipeline | accept | Consolidation mode is explicitly triggered by /evolve skill with a specific prompt prefix; observer cannot self-trigger consolidation |
</threat_model>

<verification>
1. SKILL.md contains all 10 steps with correct numbering (Step 1 through Step 10)
2. SKILL.md references `node .claude/scripts/memory/evolve.js decay` (and decay-remove, decay-upgrade)
3. SKILL.md includes consolidation dispatch with "Consolidate the ## Permanent section" prompt
4. observer.md contains `## Consolidation Mode` section with C1-C4 steps
5. observer.md Processing Pipeline (Steps 1-10) is unchanged from pre-edit
6. Both files pass their automated verify checks
</verification>

<success_criteria>
- /evolve skill flow runs decay after promote (D-02) and shows unified summary with three sections (D-06)
- Expired entries are flagged for user decision, not auto-removed (D-04)
- Kept entries upgrade to HIGH (D-05)
- Consolidation triggers at 180 lines via observer dispatch (D-07, D-08)
- Observer consolidation produces rewritten section with user approval (D-09)
- Observer recognizes consolidation mode and skips normal pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/05-memory-lifecycle/05-02-SUMMARY.md`
</output>
