---
phase: 05-feedback-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - feedback/signals.yaml
  - .claude/skills/agent-protocols/SKILL.md
  - tests/smoke-test-feedback.js
autonomous: true
requirements: [AGNT-14, MEMO-08, MEMO-09, MEMO-10]

must_haves:
  truths:
    - "feedback/signals.yaml exists at project root with correct YAML schema"
    - "Agent-protocols skill has full signal processing logic replacing the stub"
    - "Agents read signals filtered by domain at task start via agent-protocols"
    - "Agents write cross-agent signals at task end via agent-protocols"
    - "Self-promotion to MEMORY.md is the default signal lifecycle"
    - "Definition-level signals are flagged for meta/human review, not self-promoted"
    - "Both pipeline-dispatched and directly-invoked agents use the same signal path"
  artifacts:
    - path: "feedback/signals.yaml"
      provides: "Cross-agent signal inbox with YAML schema"
      contains: "signals:"
    - path: ".claude/skills/agent-protocols/SKILL.md"
      provides: "Full signal processing protocol"
      contains: "signals.yaml"
    - path: "tests/smoke-test-feedback.js"
      provides: "Smoke test validating signal system and agent-protocols"
      contains: "testCases"
  key_links:
    - from: ".claude/skills/agent-protocols/SKILL.md"
      to: "feedback/signals.yaml"
      via: "Read tool at task start, Write tool at task end"
      pattern: "feedback/signals\\.yaml"
    - from: ".claude/skills/agent-protocols/SKILL.md"
      to: ".claude/agent-memory/*/MEMORY.md"
      via: "Self-promotion of resolved signals"
      pattern: "MEMORY\\.md"
---

<objective>
Create the cross-agent signal system and upgrade agent-protocols with full signal processing logic.

Purpose: This is the core of Phase 5 -- the feedback inbox (signals.yaml) and the protocol that every agent uses to read, promote, and write signals. Without this, downstream insights cannot flow back to upstream agents.

Output: feedback/signals.yaml with seed example, upgraded agent-protocols SKILL.md with complete Feedback Signal Protocol replacing the stub, and smoke test validating both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-feedback-propagation/05-CONTEXT.md
@.planning/phases/05-feedback-propagation/05-RESEARCH.md

<interfaces>
<!-- Current agent-protocols SKILL.md structure (upgrade target) -->
From .claude/skills/agent-protocols/SKILL.md:
- Frontmatter: name: agent-protocols, user-invocable: false
- Section 1: "## Memory Lifecycle" -- complete, keep as-is
- Section 2: "## Feedback Signal Protocol" -- STUB, replace entirely
- Section 3: "## Project Context" -- keep as-is
- The stub currently references `feedback/SIGNALS.md` (wrong format) and uses markdown signal format with `to_agent` targeting (wrong pattern)

<!-- Existing test pattern from smoke-test-pipeline.js -->
Pattern: const testCases = []; testCases.push({name, check}); for loop with PASS/FAIL output; process.exit(passed === total ? 0 : 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feedback directory and signals.yaml with schema and seed example</name>
  <files>feedback/signals.yaml</files>
  <read_first>
    .claude/skills/agent-protocols/SKILL.md
    .planning/phases/05-feedback-propagation/05-RESEARCH.md
  </read_first>
  <action>
Create directory `feedback/` at project root. Create `feedback/signals.yaml` with the following exact content:

```yaml
# feedback/signals.yaml
# Cross-agent feedback signals -- domain-tagged, timestamped
# Agents read signals for their domain at task start via agent-protocols
# Resolved signals stay as history; prune when >50 entries
#
# Domains: editorial, visual, strategy, meta
# Types: quality, content, technical, process
# Promotion: memory (self-promote to MEMORY.md) | definition (needs meta/human review)

signals:
  - id: SIG-001
    date: "2026-04-11"
    source_agent: writer
    domain: editorial
    type: quality
    promotion: memory
    resolved: false
    insight: "Research dossiers missing source_manifest.json cause broken source attribution in scripts -- researcher should always generate this file"
```

Per D-01: YAML format, not markdown. Per D-02: domain-tagged (editorial/visual/strategy/meta). Per D-03: type field included. Per D-04: global location at project root. The seed entry is an example of a real cross-agent insight (writer observing something that helps researcher).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=require('path');const f=p.resolve('feedback','signals.yaml');if(!fs.existsSync(f)){process.exit(1)};const c=fs.readFileSync(f,'utf8');const checks=['signals:','id: SIG-001','domain: editorial','type: quality','promotion: memory','resolved: false','insight:'];const ok=checks.every(k=>c.includes(k));process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - feedback/ directory exists at project root
    - feedback/signals.yaml exists and is non-empty
    - File contains `signals:` as top-level key
    - Seed entry contains fields: id, date, source_agent, domain, type, promotion, resolved, insight
    - File header comment mentions domains (editorial, visual, strategy, meta) and types (quality, content, technical, process)
    - No colons in date values (uses "2026-04-11" format, not ISO with time)
  </acceptance_criteria>
  <done>feedback/signals.yaml exists with valid schema, seed example, and header documentation</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade agent-protocols Feedback Signal Protocol from stub to full logic</name>
  <files>.claude/skills/agent-protocols/SKILL.md</files>
  <read_first>
    .claude/skills/agent-protocols/SKILL.md
    .planning/phases/05-feedback-propagation/05-CONTEXT.md
    .planning/phases/05-feedback-propagation/05-RESEARCH.md
  </read_first>
  <action>
Read the current `.claude/skills/agent-protocols/SKILL.md`. Keep the frontmatter, the `## Memory Lifecycle` section, and the `## Project Context` section exactly as they are. Replace ONLY the `## Feedback Signal Protocol` section (lines 50-70 approximately) with the following complete replacement:

```markdown
## Feedback Signal Protocol

Cross-agent insights are stored in `feedback/signals.yaml` at the project root. This file is the feedback inbox -- a staging area where insights land, then get promoted into permanent agent changes. Signals permanently alter agent behavior; they are NOT ephemeral prompt injections.

### Domain Mapping

Identify your domain by matching your agent name:

| Domain | Agents |
|--------|--------|
| editorial | researcher, writer, style-extractor, editorial-lead |
| visual | visual-researcher, visual-planner, asset-processor, asset-curator |
| strategy | strategy |
| meta | meta, code-reviewer, compiler |

### At Task Start (after reading MEMORY.md)

1. Read `feedback/signals.yaml` from the project root using the Read tool
   - If the file or directory does not exist, skip signal processing (no signals yet)
2. Identify your domain from the mapping above
3. Filter for signals where `domain` matches your domain AND `resolved: false`
4. For each unresolved signal in your domain:
   - If `promotion: memory` -- evaluate whether the insight is actionable for your current or future tasks. If actionable:
     a. Add a corresponding entry to your MEMORY.md in the appropriate section (patterns, decisions, or observations) with format: `- [YYYY-MM-DD] [From SIG-NNN] insight text`
     b. Update signals.yaml: set `resolved: true`, add `resolved_by: your-agent-name`, add `resolved_date: "YYYY-MM-DD"`
   - If `promotion: definition` -- do NOT self-promote. Note it for your task completion summary.
5. If signals.yaml has more than 50 entries, remove the oldest `resolved: true` entries to keep the file manageable. Never remove unresolved entries.

### At Task End

1. Review your work for cross-agent insights -- observations that would help agents in OTHER domains
   - Self-only learnings go to your MEMORY.md, NOT to signals
   - Focus on insights that would change how another agent works
2. If you have cross-agent insights, read the current `feedback/signals.yaml`
3. Find the highest existing SIG-NNN ID number and increment for your new entries
4. Append new signal entries to the `signals:` array with these fields:
   ```yaml
   - id: SIG-NNN           # Next sequential ID
     date: "YYYY-MM-DD"    # Today's date, no colons
     source_agent: your-agent-name
     domain: target-domain  # editorial | visual | strategy | meta
     type: category         # quality | content | technical | process
     promotion: memory      # memory (default) | definition (only for procedure/guardrail changes)
     resolved: false
     insight: "One-line actionable insight"
   ```
5. Write the complete updated signals.yaml file (read full file first, append entries, write back)
6. If any `promotion: definition` signals were noted during task start, report them in your completion message:
   "Flagged for review: [SIG-NNN] insight text -- needs @meta or human review for agent definition change"
```

IMPORTANT: The `## Memory Lifecycle` section and `## Project Context` section must remain unchanged. Only the `## Feedback Signal Protocol` section is replaced.

Per D-05: signals permanently alter behavior. Per D-06: hybrid promotion (memory default, definition flagged). Per D-07: agent-protocols is the upgrade target. Per D-08: both pipeline-dispatched and directly-invoked agents use this same protocol (no orchestrator injection). Per D-13: broad cross-agent focus -- self-only learnings go to MEMORY.md. Per D-14: resolve-on-promotion lifecycle with pruning at 50 entries.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=require('path');const f=p.resolve('.claude','skills','agent-protocols','SKILL.md');const c=fs.readFileSync(f,'utf8');const must=['signals.yaml','Domain Mapping','editorial.*researcher.*writer','visual.*visual-researcher','promotion: memory','promotion: definition','resolved: true','resolved_by','SIG-NNN','At Task Start','At Task End','MEMORY.md'];const ok=must.every(k=>new RegExp(k).test(c));const noOld=!c.includes('feedback/SIGNALS.md')&&!c.includes('to_agent');console.log(ok&&noOld?'PASS':'FAIL');process.exit(ok&&noOld?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - agent-protocols SKILL.md contains `signals.yaml` (not `SIGNALS.md`)
    - agent-protocols SKILL.md contains `## Feedback Signal Protocol` section
    - agent-protocols SKILL.md contains "Domain Mapping" table with editorial, visual, strategy, meta
    - agent-protocols SKILL.md contains `At Task Start` and `At Task End` subsections
    - agent-protocols SKILL.md contains `promotion: memory` and `promotion: definition` instructions
    - agent-protocols SKILL.md contains `resolved: true` and `resolved_by` for signal resolution
    - agent-protocols SKILL.md does NOT contain `feedback/SIGNALS.md` (old stub reference)
    - agent-protocols SKILL.md does NOT contain `to_agent` (old agent-to-agent targeting)
    - `## Memory Lifecycle` section is unchanged
    - `## Project Context` section is unchanged
  </acceptance_criteria>
  <done>agent-protocols SKILL.md has complete Feedback Signal Protocol with domain mapping, read/promote/write/prune logic, and hybrid promotion model. Old stub fully replaced.</done>
</task>

<task type="auto">
  <name>Task 3: Create smoke-test-feedback.js validating signal system and agent-protocols</name>
  <files>tests/smoke-test-feedback.js</files>
  <read_first>
    tests/smoke-test-pipeline.js
    feedback/signals.yaml
    .claude/skills/agent-protocols/SKILL.md
  </read_first>
  <action>
Create `tests/smoke-test-feedback.js` following the exact same pattern as `tests/smoke-test-pipeline.js` (testCases array, PASS/FAIL console output, process.exit).

The test must include these exact checks:

**Signal system checks (MEMO-08):**
1. `feedback/directory_exists` -- `fs.existsSync(path.join(projectRoot, 'feedback'))`
2. `feedback/signals.yaml_exists` -- `fs.existsSync(path.join(projectRoot, 'feedback', 'signals.yaml'))`
3. `feedback/signals.yaml_has_signals_key` -- content includes `signals:`
4. `feedback/signals.yaml_has_required_fields` -- content includes all of: `id:`, `date:`, `source_agent:`, `domain:`, `type:`, `promotion:`, `resolved:`, `insight:`
5. `feedback/signals.yaml_no_colons_in_dates` -- no match for `/date: "\d{4}-\d{2}-\d{2}T/` (rejects ISO timestamps with time component)

**Agent-protocols upgrade checks (AGNT-14, MEMO-09, MEMO-10):**
6. `agent-protocols/references_signals_yaml` -- content includes `signals.yaml`
7. `agent-protocols/no_old_signals_md_reference` -- content does NOT include `SIGNALS.md`
8. `agent-protocols/has_domain_mapping` -- content includes all four domains: `editorial`, `visual`, `strategy`, `meta`
9. `agent-protocols/has_at_task_start` -- content includes `At Task Start`
10. `agent-protocols/has_at_task_end` -- content includes `At Task End`
11. `agent-protocols/has_promotion_memory` -- content includes `promotion: memory`
12. `agent-protocols/has_promotion_definition` -- content includes `promotion: definition`
13. `agent-protocols/has_resolve_lifecycle` -- content includes `resolved: true` and `resolved_by`
14. `agent-protocols/has_pruning_guidance` -- content includes `50` (pruning threshold reference)
15. `agent-protocols/no_to_agent_targeting` -- content does NOT include `to_agent`

**Header comment:**
```javascript
// tests/smoke-test-feedback.js
// Phase 5 Feedback Propagation validation -- checks AGNT-14, MEMO-08, MEMO-09, MEMO-10, SKIL-13
// Run: node tests/smoke-test-feedback.js
// Full suite: node tests/smoke-test-paths.js && node tests/smoke-test-skills.js && node tests/smoke-test-agents.js && node tests/smoke-test-pipeline.js && node tests/smoke-test-feedback.js
```

Verification gate checks for SKIL-13 will be added in Plan 02 after gates are implemented.
  </action>
  <verify>
    <automated>node tests/smoke-test-feedback.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/smoke-test-feedback.js exists
    - Running `node tests/smoke-test-feedback.js` exits with code 0
    - Output shows PASS for all 15 checks
    - Script uses same pattern as smoke-test-pipeline.js (testCases array, PASS/FAIL, process.exit)
    - Header comment references AGNT-14, MEMO-08, MEMO-09, MEMO-10, SKIL-13
  </acceptance_criteria>
  <done>Smoke test validates signal file structure, agent-protocols upgrade, domain mapping, promotion model, and resolve lifecycle. All 15 checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> signals.yaml | Any agent can read/write the shared signal file |
| Signal -> MEMORY.md | Signals influence agent behavior via memory promotion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | feedback/signals.yaml | accept | Single-user local system; agents are user-invoked one at a time per CLAUDE.md architecture rules. No concurrent write risk. Git tracks all changes. |
| T-05-02 | Information Disclosure | feedback/signals.yaml | accept | No PII or secrets in signals -- only pipeline methodology insights. File is in a local git repo. |
| T-05-03 | Elevation of Privilege | promotion: definition signals | mitigate | Definition-level signals are NOT self-promoted. Agent-protocols instructs agents to flag them for meta/human review only. Agent definitions are protected from self-editing. |
</threat_model>

<verification>
- `node tests/smoke-test-feedback.js` exits 0 with all checks passing
- feedback/signals.yaml exists with valid YAML structure and seed entry
- agent-protocols SKILL.md has complete signal protocol with no old stub references
</verification>

<success_criteria>
- Signal inbox created with correct schema (MEMO-08)
- Agent-protocols upgraded with full read/promote/write/prune logic (AGNT-14, MEMO-09, MEMO-10)
- Smoke test validates all structural requirements
- No references to old SIGNALS.md format or to_agent targeting remain
</success_criteria>

<output>
After completion, create `.planning/phases/05-feedback-propagation/05-01-SUMMARY.md`
</output>
