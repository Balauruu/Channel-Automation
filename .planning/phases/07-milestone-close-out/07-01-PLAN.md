---
phase: 07-milestone-close-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-evolve-command/03-VERIFICATION.md
  - .claude/agents/observer.md
  - .claude/skills/agent-observability/SKILL.md
autonomous: true
requirements:
  - EVLV-03
  - MEML-05
  - MEML-06

must_haves:
  truths:
    - "03-VERIFICATION.md override has accepted_by and accepted_at filled in"
    - "observer.md Q1 skill list does not reference autoresearch"
    - "observer.md example 3 does not reference autoresearch"
    - "agent-observability SKILL.md documents the 10-step /evolve flow including decay commands"
    - "agent-observability SKILL.md lists all 6 evolve.js subcommands with signatures"
  artifacts:
    - path: ".planning/phases/03-evolve-command/03-VERIFICATION.md"
      provides: "EVLV-03 override acceptance stamp"
      contains: "accepted_by: \"Balauruu\""
    - path: ".claude/agents/observer.md"
      provides: "Stale reference cleanup"
    - path: ".claude/skills/agent-observability/SKILL.md"
      provides: "Updated /evolve flow and subcommand docs"
      contains: "decay-remove"
  key_links:
    - from: ".claude/skills/agent-observability/SKILL.md"
      to: ".claude/skills/evolve/SKILL.md"
      via: "10-step flow mirrored from canonical source"
      pattern: "Decay scan"
---

<objective>
Fix integration gaps and the EVLV-03 override acceptance identified by the v1 milestone audit.

Purpose: Three artifacts have stale or incomplete content that blocks milestone close-out: the EVLV-03 override lacks acceptance stamps, observer.md references a deleted skill, and agent-observability SKILL.md documents an outdated 6-step /evolve flow (now 10-step with decay commands from Phase 5).

Output: Updated 03-VERIFICATION.md, observer.md, and agent-observability SKILL.md with all gaps resolved.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-milestone-close-out/07-CONTEXT.md
@.planning/phases/07-milestone-close-out/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accept EVLV-03 override and clean observer stale references</name>
  <files>.planning/phases/03-evolve-command/03-VERIFICATION.md, .claude/agents/observer.md</files>
  <read_first>
    - .planning/phases/03-evolve-command/03-VERIFICATION.md
    - .claude/agents/observer.md
    - .planning/phases/07-milestone-close-out/07-PATTERNS.md
  </read_first>
  <action>
**03-VERIFICATION.md (per D-01):**
- Line 6: Change `overrides_applied: 0` to `overrides_applied: 1`
- In the overrides block, change `accepted_by: ""` to `accepted_by: "Balauruu"`
- In the overrides block, change `accepted_at: ""` to `accepted_at: "2026-04-22"`
- No other changes to this file.

**observer.md (per D-02, D-04):**

Stale reference 1 -- Q1 skill list (line 117):
Current line contains: `archive-search, autoresearch, crawl4ai-scraping, media-evaluation, pipeline-design, structured-output, visual-narrative`
Replace with: `archive-search, crawl4ai-scraping, media-evaluation, pipeline-design, structured-output, visual-narrative`
(Simply remove `autoresearch, ` from the comma-separated list.)

Stale reference 2 -- Example 3 scope-test Q1 answer (line 298):
Current: `- Q1: YES -- changes how autoresearch skill runs (adds a verification pass)`
Replace with: `- Q1: YES -- changes how crawl4ai-scraping skill runs (adds a verification pass)`
Rationale: The example is about researcher verification of sourced claims. crawl4ai-scraping is the skill that handles web scraping for research sources, making it a coherent replacement.

Note: editorial-lead references are already removed from observer.md (confirmed by grep returning zero matches). No action needed for D-03.
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6" && node -e "const fs=require('fs'); const v=fs.readFileSync('.planning/phases/03-evolve-command/03-VERIFICATION.md','utf8'); const ok1=v.includes('accepted_by: \"Balauruu\"'); const ok2=v.includes('accepted_at: \"2026-04-22\"'); const ok3=v.includes('overrides_applied: 1'); const o=fs.readFileSync('.claude/agents/observer.md','utf8'); const ok4=!o.includes('autoresearch'); const ok5=o.includes('crawl4ai-scraping skill runs'); console.log('override_by:',ok1,'override_at:',ok2,'overrides_count:',ok3,'no_autoresearch:',ok4,'replacement_example:',ok5); if(!ok1||!ok2||!ok3||!ok4||!ok5) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - 03-VERIFICATION.md contains `overrides_applied: 1`
    - 03-VERIFICATION.md contains `accepted_by: "Balauruu"`
    - 03-VERIFICATION.md contains `accepted_at: "2026-04-22"`
    - observer.md does NOT contain the string `autoresearch` anywhere
    - observer.md line ~117 lists exactly: `archive-search, crawl4ai-scraping, media-evaluation, pipeline-design, structured-output, visual-narrative`
    - observer.md line ~298 contains `crawl4ai-scraping skill runs`
  </acceptance_criteria>
  <done>EVLV-03 override accepted with Balauruu stamp. Observer.md has zero references to deleted autoresearch skill.</done>
</task>

<task type="auto">
  <name>Task 2: Update agent-observability SKILL.md with 10-step /evolve flow and full subcommand docs</name>
  <files>.claude/skills/agent-observability/SKILL.md</files>
  <read_first>
    - .claude/skills/agent-observability/SKILL.md
    - .claude/skills/evolve/SKILL.md
    - .claude/scripts/memory/evolve.js (first 12 lines for subcommand signatures)
    - .planning/phases/07-milestone-close-out/07-PATTERNS.md
  </read_first>
  <action>
**Replace the /evolve Command section (per D-05, D-06):**

Find the section starting at approximately line 131 (`## /evolve Command`) through line 142 (the `Helper script:` line). Replace the entire section content (keeping the `## /evolve Command` heading) with:

```markdown
## /evolve Command

User-invoked skill that orchestrates the learning cycle:

1. Dispatch @observer to process new obs.jsonl events
2. Scan all memory files for ## Pending Review entries (`evolve.js scan`)
3. Auto-promote all entries to ## Permanent (`evolve.js promote`)
4. Decay scan -- flag expired entries by confidence TTL (`evolve.js decay`)
5. Consolidation check -- if any file hits capacity warning (180+ lines), dispatch @observer in consolidation mode to propose rewrite of ## Permanent section
6. Display unified summary with continuous numbering: promoted entries (insights, MEMORY, PLAYBOOK), expired entries, capacity warnings
7. User interaction -- revert promoted entries and/or remove expired entries by number
8. Execute actions -- revert (`evolve.js revert`), remove expired (`evolve.js decay-remove`), upgrade kept expired to [HIGH] (`evolve.js decay-upgrade`)
9. Commit changes (targeted `git add` of modified files only)
10. Display final evolution summary

PLAYBOOK.md is excluded from evolve.js scan/promote (its Open/Resolved lifecycle is managed by the observer, not /evolve).

Helper script: `.claude/scripts/memory/evolve.js` with subcommands:
- `scan` -- discover all memory files, return pending entry counts as JSON
- `promote` -- move all Pending Review entries to Permanent sections, return promoted array as JSON
- `revert <idx> [<idx> ...]` -- remove promoted entries from Permanent by global index
- `decay` -- scan Permanent sections for expired entries (LOW > 14d, MED > 30d), return expired array and capacity warnings
- `decay-remove <idx> [<idx> ...]` -- delete expired entries by decay global index
- `decay-upgrade <idx> [<idx> ...]` -- upgrade expired entries to [HIGH] confidence (making them permanent)

Decay TTL rules:
- LOW confidence entries expire after 14 days
- MED confidence entries expire after 30 days
- HIGH confidence entries never expire
```

This replaces the previous 6-step flow and the single-line `Helper script:` line with the full 10-step flow and detailed subcommand documentation.
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6" && node -e "const fs=require('fs'); const s=fs.readFileSync('.claude/skills/agent-observability/SKILL.md','utf8'); const checks=[['has_step4',/4\. Decay scan/],['has_step5',/5\. Consolidation check/],['has_step8',/8\. Execute actions/],['has_step10',/10\. Display final/],['has_decay_remove',/decay-remove/],['has_decay_upgrade',/decay-upgrade/],['has_ttl_low',/LOW confidence entries expire after 14 days/],['has_ttl_med',/MED confidence entries expire after 30 days/],['no_old_6step',!/^1\. Dispatch.*\n2\. Scan.*\n3\. Auto-promote.*\n4\. Display.*\n5\. Offer revert/m.test(s)]]; let ok=true; checks.forEach(([name,re])=>{const pass=re instanceof RegExp?re.test(s):re; console.log(name+':',pass); if(!pass)ok=false;}); if(!ok) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - agent-observability SKILL.md contains `4. Decay scan`
    - agent-observability SKILL.md contains `5. Consolidation check`
    - agent-observability SKILL.md contains `8. Execute actions`
    - agent-observability SKILL.md contains `10. Display final evolution summary`
    - agent-observability SKILL.md contains `decay-remove`
    - agent-observability SKILL.md contains `decay-upgrade`
    - agent-observability SKILL.md contains `LOW confidence entries expire after 14 days`
    - agent-observability SKILL.md contains `MED confidence entries expire after 30 days`
    - agent-observability SKILL.md does NOT contain the old 6-step pattern "Offer revert by number"
    - agent-observability SKILL.md lists all 6 subcommands: scan, promote, revert, decay, decay-remove, decay-upgrade
  </acceptance_criteria>
  <done>Agent-observability SKILL.md documents the accurate 10-step /evolve flow with all 6 evolve.js subcommands including decay/decay-remove/decay-upgrade from Phase 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This plan modifies only internal documentation and tracking files. No user input, API endpoints, or external data involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I (Information Disclosure) | 03-VERIFICATION.md | accept | Contains only a username and date, no secrets or PII |
</threat_model>

<verification>
All three files modified, grep confirms:
1. `autoresearch` absent from observer.md
2. `accepted_by: "Balauruu"` present in 03-VERIFICATION.md
3. `decay-remove` and `decay-upgrade` present in agent-observability SKILL.md
4. `Offer revert by number` absent from agent-observability SKILL.md (old 6-step pattern gone)
</verification>

<success_criteria>
- 03-VERIFICATION.md EVLV-03 override has accepted_by and accepted_at filled in
- observer.md contains zero references to autoresearch or editorial-lead
- agent-observability SKILL.md documents all 10 steps of /evolve including decay scanning, consolidation, and the 3 new Phase 5 subcommands
</success_criteria>

<output>
After completion, create `.planning/phases/07-milestone-close-out/07-01-SUMMARY.md`
</output>
