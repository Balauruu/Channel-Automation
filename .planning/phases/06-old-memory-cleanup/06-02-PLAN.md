---
phase: 06-old-memory-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STACK.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements:
  - CLEANUP-04
  - CLEANUP-05

must_haves:
  truths:
    - "Codebase maps describe the current unified memory system, not the old broken one"
    - "No live file in the repo contains actionable references to project-memories/, signals.yaml, obs.js (the old hook), logs/runs, scratchpad, or check-definition-signals"
    - "Historical planning artifacts retain narrative context but have no misleading paths in frontmatter or action blocks"
  artifacts:
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Architecture description reflecting unified memory system"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Directory structure without deleted directories"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Concerns list without resolved old-system issues"
  key_links:
    - from: ".planning/codebase/*.md"
      to: "future phase planners"
      via: "loaded during planning step load_codebase_context"
      pattern: "pipeline-observe.js"
---

<objective>
Regenerate codebase maps and run a full grep audit to verify zero stale references remain in any live file.

Purpose: Codebase maps (ARCHITECTURE.md, STRUCTURE.md, CONCERNS.md, etc.) are loaded by GSD planning workflows. They currently describe the old project-memories/signals.yaml system. After Plan 01 deleted deprecated files and updated live docs, the maps must be regenerated to reflect the clean state. A final grep audit per D-07 confirms no stale references leaked through.

Output: Fresh codebase maps reflecting the unified memory system, documented grep audit results with zero actionable stale references.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-old-memory-cleanup/06-CONTEXT.md
@.planning/phases/06-old-memory-cleanup/06-PATTERNS.md
@.planning/phases/06-old-memory-cleanup/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate codebase maps</name>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md (current state — will be overwritten)
    - .planning/codebase/STRUCTURE.md (current state — will be overwritten)
    - .planning/phases/06-old-memory-cleanup/06-CONTEXT.md (D-05 — regeneration instruction)
  </read_first>
  <files>
    .planning/codebase/ARCHITECTURE.md
    .planning/codebase/STRUCTURE.md
    .planning/codebase/CONCERNS.md
    .planning/codebase/CONVENTIONS.md
    .planning/codebase/INTEGRATIONS.md
    .planning/codebase/STACK.md
    .planning/codebase/TESTING.md
  </files>
  <action>
Per D-05: regenerate all codebase maps using the `/gsd-map-codebase` command. This must happen AFTER Plan 01's commit so the maps reflect the final clean state (no deleted files, updated CLAUDE.md, etc.).

Run: `/gsd-map-codebase`

This command scans the entire codebase and regenerates all `.planning/codebase/*.md` files. The executor does not manually edit these files — the command handles everything.

After regeneration, verify the maps no longer contain old-system references by running:
```bash
grep -r "project-memories\|signals\.yaml\|check-definition-signals\|obs\.js" .planning/codebase/ -l
```

Expected result: zero matches. If any codebase map file still references old-system terms, that indicates the map generator picked up a stale reference from somewhere — investigate and report.

Note: `obs.jsonl` and `obs-summarize.js` are NEW system terms and are expected/correct. Only `obs.js` (the old hook, no `onl` suffix) is stale.
  </action>
  <verify>
    <automated>grep -rlc "project-memories\|signals\.yaml\|check-definition-signals" .planning/codebase/ 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files in `.planning/codebase/` have modification timestamps after Plan 01's commit
    - `grep -r "project-memories" .planning/codebase/` returns zero matches
    - `grep -r "signals\.yaml" .planning/codebase/` returns zero matches
    - `grep -r "check-definition-signals" .planning/codebase/` returns zero matches
    - `.planning/codebase/STRUCTURE.md` does NOT list `.claude/project-memories/` or `.claude/feedback/` as directories
    - `.planning/codebase/ARCHITECTURE.md` describes pipeline-observe.js (not obs.js) as the capture hook
  </acceptance_criteria>
  <done>All codebase maps regenerated reflecting the clean unified memory system state</done>
</task>

<task type="auto">
  <name>Task 2: Full repo grep audit for old-system terms</name>
  <read_first>
    - .planning/phases/06-old-memory-cleanup/06-CONTEXT.md (D-07 — audit scope, D-08 — historical artifact handling)
    - .planning/phases/06-old-memory-cleanup/06-PATTERNS.md (grep patterns and expected results)
  </read_first>
  <files></files>
  <action>
Per D-07: run a full codebase grep for each old-system term across the entire repo. Per D-08: classify each match as actionable (live config/code/active skill) or narrative (historical planning docs).

Run these 6 grep commands from the project root:
```bash
grep -rn "project-memories" . --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" --include="*.sh" -l
grep -rn "signals\.yaml" . --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" --include="*.sh" -l
grep -rn "obs\.js" . --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" --include="*.sh" -l
grep -rn "logs/runs" . --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" --include="*.sh" -l
grep -rn "scratchpad" . --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" --include="*.sh" -l
grep -rn "check-definition-signals" . --include="*.md" --include="*.js" --include="*.json" --include="*.yaml" --include="*.sh" -l
```

**Classification rules:**

ACTIONABLE (must fix) — matches in:
- `CLAUDE.md`, `.gitignore`, any file under `.claude/agents/`, `.claude/skills/`, `.claude/hooks/`, `.claude/scripts/`
- `.planning/PROJECT.md` (except "What Failed" section which is narrative)
- `.planning/codebase/*.md`
- `.planning/REQUIREMENTS.md` requirement descriptions that reference old paths as current
- `.planning/ROADMAP.md` phase descriptions
- Any `.js`, `.json`, `.yaml`, `.sh` file that is not in `.planning/`

NARRATIVE (leave intact per D-08) — matches in:
- `.planning/phases/*/XX-CONTEXT.md` — discussion records
- `.planning/phases/*/XX-DISCUSSION-LOG.md` — conversation logs
- `.planning/phases/*/XX-RESEARCH.md` — research findings
- `.planning/phases/*/XX-PATTERNS.md` — pattern maps (reference old patterns)
- `.planning/phases/*/XX-*-SUMMARY.md` — execution summaries
- `.planning/phases/*/XX-*-PLAN.md` — historical plan actions/context
- `.planning/phases/*/XX-VERIFICATION.md` — verification results
- `.planning/phases/*/XX-REVIEW.md` — review records
- `.planning/phases/*/XX-VALIDATION.md` — validation records
- `.planning/phases/*/XX-HUMAN-UAT.md` — UAT records
- `.planning/phases/*/XX-AI-SPEC.md` — AI specifications
- `.planning/STATE.md` — historical decision log entries
- `.planning/PROJECT.md` "What Failed (Prior Attempts)" section

EDGE CASE for `obs.js`: This pattern will also match `obs.jsonl` and `obs-summarize.js`. Those are NEW system files and are NOT stale. Only `obs.js` when referring to the old hook file `.claude/hooks/obs.js` is stale. Use context to distinguish: if the match is `obs.jsonl` or `obs-summarize.js`, it's fine. If it's `hooks/obs.js` or just `obs.js` referring to the old hook, it's stale.

**If ANY actionable match is found:**
1. Fix it in place (remove stale reference or update to new path)
2. Stage the fix: `git add <specific-file>`
3. Commit: `cleanup(phase-06): fix stale reference in <file>`

**If all matches are narrative only:**
Document the results in the SUMMARY with the count of narrative matches per term and confirm zero actionable findings.

Note: `.planning/REQUIREMENTS.md` line about MEML-03 says "no signals, no project-memories, no scratchpad" — this is a REQUIREMENT DESCRIPTION of what was built, not a reference to the old system. It's correct as-is and should not be changed.
  </action>
  <verify>
    <automated>grep -rn "project-memories\|signals\.yaml\|check-definition-signals" CLAUDE.md .gitignore .claude/agents/ .claude/skills/ .claude/hooks/ .claude/scripts/ .planning/codebase/ .planning/PROJECT.md .planning/ROADMAP.md 2>/dev/null | grep -v "What Failed" | grep -v "Prior Attempts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The verification command above returns 0 (zero actionable stale references in any live file)
    - `grep "project-memories" CLAUDE.md` returns zero matches
    - `grep "signals.yaml" CLAUDE.md` returns zero matches
    - `grep "project-memories" .gitignore` returns zero matches
    - `grep "project-memories\|signals\.yaml\|check-definition-signals" .planning/codebase/ARCHITECTURE.md` returns zero matches
    - Any remaining matches are in `.planning/phases/` historical artifacts (narrative — documented as acceptable)
    - The SUMMARY documents total match count per term, split into actionable (should be 0) and narrative
  </acceptance_criteria>
  <done>Full grep audit complete with zero actionable stale references. All remaining matches are in historical planning artifacts (narrative context preserved per D-08)</done>
</task>

<task type="auto">
  <name>Task 3: Commit codebase maps and close phase</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 6 entry to update)
  </read_first>
  <files>
    .planning/codebase/ARCHITECTURE.md
    .planning/codebase/STRUCTURE.md
    .planning/codebase/CONCERNS.md
    .planning/codebase/CONVENTIONS.md
    .planning/codebase/INTEGRATIONS.md
    .planning/codebase/STACK.md
    .planning/codebase/TESTING.md
  </files>
  <action>
Stage and commit the regenerated codebase maps:

```bash
git add .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/CONCERNS.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md .planning/codebase/STACK.md .planning/codebase/TESTING.md
```

If Task 2 found and fixed any actionable stale references, those files should already be committed in separate fixup commits. The codebase maps are the remaining changes.

Commit message:
```
docs(phase-06): regenerate codebase maps for clean unified memory system

Regenerated all .planning/codebase/ maps after Phase 6 file deletions
and doc updates. Maps now accurately describe pipeline-observe.js,
obs.jsonl, @observer, /evolve, and PLAYBOOK.md lifecycle. No references
to project-memories/, signals.yaml, or obs.js remain in any live file.
```

Do NOT stage any files outside `.planning/codebase/`. Verify with `git diff --cached --name-only` before committing.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep "docs(phase-06)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` contains "docs(phase-06): regenerate codebase maps"
    - `git show --stat HEAD` lists only `.planning/codebase/` files
    - `git diff --cached` is empty after commit
  </acceptance_criteria>
  <done>Codebase maps committed. Phase 6 cleanup is complete — the repo contains only the new unified memory system with no stale references in any live file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codebase maps -> planners | Stale maps mislead future phase planning; regeneration must happen after all deletions |
| Grep audit -> confidence | False negatives (missed stale refs) leave landmines for future agents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T (Tampering) | Codebase map regeneration before deletions | mitigate | Plan 02 depends_on Plan 01 (wave 2); executor verifies Plan 01 commit exists before running /gsd-map-codebase |
| T-06-05 | R (Repudiation) | Grep audit misses stale reference | mitigate | Task 2 runs 6 explicit grep patterns with full file-type coverage; results documented in SUMMARY with match counts per term |
| T-06-06 | T (Tampering) | obs.js grep false positives on obs.jsonl | accept | Task 2 action provides explicit disambiguation rule: obs.jsonl and obs-summarize.js are new-system, only hooks/obs.js is stale |
</threat_model>

<verification>
After this plan completes:
1. `.planning/codebase/ARCHITECTURE.md` describes pipeline-observe.js, obs.jsonl, @observer
2. `.planning/codebase/STRUCTURE.md` does not list project-memories/ or feedback/ directories
3. `grep -r "project-memories\|signals\.yaml\|check-definition-signals" CLAUDE.md .gitignore .claude/agents/ .claude/skills/ .claude/hooks/ .claude/scripts/ .planning/codebase/` returns zero matches
4. Two Phase 6 commits exist in git log
</verification>

<success_criteria>
- All codebase maps regenerated and committed, reflecting the clean unified memory system
- Full grep audit completed with zero actionable stale references across the entire repo
- Any remaining matches documented as narrative context in historical planning artifacts (acceptable per D-08)
- Phase 6 is fully complete — the old broken memory system has been entirely removed
</success_criteria>

<output>
After completion, create `.planning/phases/06-old-memory-cleanup/06-02-SUMMARY.md`
</output>
