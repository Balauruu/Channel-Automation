---
phase: 06-old-memory-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .gitignore
  - .planning/PROJECT.md
  - .claude/settings.local.json
  - .claude/hooks/obs.js
  - .claude/hooks/check-definition-signals.js
  - .claude/skills/autoresearch/SKILL.md
  - .claude/skills/autoresearch/insights.md
  - .claude/tests/fixtures/observability/tool-events.jsonl
  - .claude/tests/fixtures/observability/transcript-errored.jsonl
  - .claude/tests/fixtures/observability/transcript-stopped.jsonl
  - .claude/tests/fixtures/observability/transcript.jsonl
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02
  - CLEANUP-03

must_haves:
  truths:
    - "CLAUDE.md Folder Map has no entries for project-memories/ or feedback/"
    - "CLAUDE.md Folder Map includes PLAYBOOK.md and observations/ entries"
    - ".gitignore has no project-memories/ entry"
    - "PROJECT.md Current State section describes the working unified memory system, not the broken one"
    - "All 8 deprecated files are removed from git tracking"
    - "settings.local.json has no signals.yaml permission entry"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate Folder Map for all agents"
      contains: "PLAYBOOK.md"
    - path: ".gitignore"
      provides: "Clean exclusion list with no dead entries"
    - path: ".planning/PROJECT.md"
      provides: "Accurate project state description"
      contains: "### Current State"
  key_links:
    - from: "CLAUDE.md"
      to: "all agents"
      via: "Folder Map read at task start"
      pattern: "PLAYBOOK.md"
    - from: ".planning/PROJECT.md"
      to: "future milestone planners"
      via: "Context section read at milestone start"
      pattern: "### Current State"
---

<objective>
Remove deprecated files and update live configuration docs to reflect the new unified memory system.

Purpose: Every agent reads CLAUDE.md at task start. PROJECT.md is read at milestone start. Stale entries for deleted directories (project-memories/, feedback/signals.yaml) and the outdated "Current State (Broken)" section actively mislead all downstream consumers. The 8 deprecated files cluttering git status must be committed as deletions.

Output: Clean CLAUDE.md Folder Map, accurate PROJECT.md Current State, committed file deletions, clean .gitignore.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-old-memory-cleanup/06-CONTEXT.md
@.planning/phases/06-old-memory-cleanup/06-PATTERNS.md

<interfaces>
<!-- Current CLAUDE.md Folder Map (lines 8-25) — executor modifies this section -->
From CLAUDE.md:
```markdown
## Folder Map

- `channel/` -- Channel identity (voice, style, visual guide)
- `.claude/agents/` -- Agent definitions
- `.claude/skills/` -- Shared skills (agent-protocols)
- `.claude/agent-memory/` -- Per-agent persistent memory (universal, cross-project)
- `.claude/project-memories/` -- Per-project agent notes (project-scoped, archived with project)
- `.claude/references/` -- Reference guides (skill crafting)
- `.claude/feedback/` -- Cross-agent feedback signals (signals.yaml)
- `.claude/logs/` -- Agent dispatch/completion session logs
- `.claude/tests/` -- Smoke tests and validation scripts
- `.claude/hooks/` -- Pre/PostToolUse and SubagentStop hooks
- `.claude/scripts/` -- Python scripts (strategy/, editorial/, media/ subdirs)
- `.claude/rules/` -- Modular on-demand rules (git-workflow, etc.). Read when relevant; not auto-loaded.
- `data/` -- SQLite databases (channel_assistant.db, asset_catalog.db)
- `docs/` -- plans & specs
- `channel/strategy/` -- Strategy outputs (competitors.json, analysis, topics)
- `channel/voice-analysis/` -- Style-extractor workspace (reconstructed scripts)
- `projects/` -- Per-documentary outputs
```

<!-- Current .gitignore lines 25-26 — executor removes these two lines -->
From .gitignore:
```gitignore
# -- Project-scoped agent memories ------------------------------------------
.claude/project-memories/
```

<!-- Current PROJECT.md lines 46-53 — executor rewrites this section -->
From .planning/PROJECT.md:
```markdown
### Current State (Broken)

- pipeline-observe.sh exists (342 lines) but only captures subagent events -- main conversation is invisible
- No actual logging is happening in practice
- Information bleeds between layers: agent-specific knowledge ends up in PLAYBOOK.md
- agent-protocols skill still references deleted `project-memories/` directory and `signals.yaml`
- 11 agent MEMORY.md files and 8 skill insights.md files exist but aren't systematically maintained
- PLAYBOOK.md skeleton exists but isn't populated by any automated system
```

<!-- settings.local.json line 7 — executor removes this entry -->
From .claude/settings.local.json:
```json
"Bash(cp \"D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6/feedback/signals.yaml\" \"D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6/.claude/feedback/\")",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage and commit all 8 deprecated file deletions</name>
  <read_first>
    - .planning/phases/06-old-memory-cleanup/06-CONTEXT.md (D-03, D-04 — file list and intent)
  </read_first>
  <files>
    .claude/hooks/obs.js
    .claude/hooks/check-definition-signals.js
    .claude/skills/autoresearch/SKILL.md
    .claude/skills/autoresearch/insights.md
    .claude/tests/fixtures/observability/tool-events.jsonl
    .claude/tests/fixtures/observability/transcript-errored.jsonl
    .claude/tests/fixtures/observability/transcript-stopped.jsonl
    .claude/tests/fixtures/observability/transcript.jsonl
  </files>
  <action>
Per D-03 and D-04: these 8 files already show as deleted in `git status` (unstaged deletions from prior phases). Stage each deletion individually using `git rm` on each path. If a file is already deleted from disk, `git rm` will still work (it stages the deletion from the index).

Run these commands:
```bash
git rm .claude/hooks/obs.js
git rm .claude/hooks/check-definition-signals.js
git rm .claude/skills/autoresearch/SKILL.md
git rm .claude/skills/autoresearch/insights.md
git rm .claude/tests/fixtures/observability/tool-events.jsonl
git rm .claude/tests/fixtures/observability/transcript-errored.jsonl
git rm .claude/tests/fixtures/observability/transcript-stopped.jsonl
git rm .claude/tests/fixtures/observability/transcript.jsonl
```

After staging, verify with `git status` that all 8 show as "deleted" in the staged area and nothing unexpected is staged.

Do NOT commit yet — Task 2 will stage additional changes and Task 3 will create the commit.
  </action>
  <verify>
    <automated>git diff --cached --name-status | grep "^D" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --cached --name-status` shows exactly 8 lines starting with `D`
    - The 8 deleted files are: obs.js, check-definition-signals.js, autoresearch/SKILL.md, autoresearch/insights.md, tool-events.jsonl, transcript-errored.jsonl, transcript-stopped.jsonl, transcript.jsonl
    - `git status` shows no unintended files in the staged area
  </acceptance_criteria>
  <done>All 8 deprecated files staged for deletion in git index</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md, .gitignore, PROJECT.md, and settings.local.json</name>
  <read_first>
    - CLAUDE.md (current Folder Map section, lines 7-25)
    - .gitignore (full file, 44 lines)
    - .planning/PROJECT.md (lines 44-60, Current State and What Failed sections)
    - .claude/settings.local.json (full file, 32 lines)
    - .planning/phases/06-old-memory-cleanup/06-CONTEXT.md (D-01, D-02, D-06 — exact changes)
    - .planning/phases/06-old-memory-cleanup/06-PATTERNS.md (Folder Map entry format, section patterns)
  </read_first>
  <files>
    CLAUDE.md
    .gitignore
    .planning/PROJECT.md
    .claude/settings.local.json
  </files>
  <action>
**CLAUDE.md Folder Map (per D-01):**

Replace the Folder Map section (lines 8-25) with the following. Changes: remove `project-memories/` line, remove `feedback/` line, add `PLAYBOOK.md` entry, update `logs/` description to mention observations/:

```markdown
## Folder Map

- `channel/` -- Channel identity (voice, style, visual guide)
- `.claude/agents/` -- Agent definitions
- `.claude/skills/` -- Shared skills (agent-protocols)
- `.claude/agent-memory/` -- Per-agent persistent memory (universal, cross-project)
- `.claude/PLAYBOOK.md` -- Cross-agent coordination log (observer-managed, Open/Resolved lifecycle)
- `.claude/references/` -- Reference guides (skill crafting)
- `.claude/logs/` -- Agent dispatch/completion session logs and observations/ (obs.jsonl per project)
- `.claude/tests/` -- Smoke tests and validation scripts
- `.claude/hooks/` -- Pre/PostToolUse and SubagentStop hooks (pipeline-observe.js)
- `.claude/scripts/` -- Python scripts (strategy/, editorial/, media/ subdirs)
- `.claude/rules/` -- Modular on-demand rules (git-workflow, etc.). Read when relevant; not auto-loaded.
- `data/` -- SQLite databases (channel_assistant.db, asset_catalog.db)
- `docs/` -- plans & specs
- `channel/strategy/` -- Strategy outputs (competitors.json, analysis, topics)
- `channel/voice-analysis/` -- Style-extractor workspace (reconstructed scripts)
- `projects/` -- Per-documentary outputs
```

Key changes from current:
1. REMOVED: `.claude/project-memories/` line (directory deleted)
2. REMOVED: `.claude/feedback/` line (signals.yaml system removed)
3. ADDED: `.claude/PLAYBOOK.md` line after agent-memory
4. UPDATED: `.claude/logs/` description now mentions `observations/` and `obs.jsonl`
5. UPDATED: `.claude/hooks/` parenthetical now mentions `pipeline-observe.js`

**.gitignore (per D-02):**

Remove lines 25-26 entirely:
```
# -- Project-scoped agent memories ------------------------------------------
.claude/project-memories/
```

Also remove lines 22-23 (the feedback section, since `.claude/feedback/` is dead):
```
# -- Feedback signal files (cross-agent state, regenerated per session) -----
feedback/
.claude/feedback/
```

Wait -- check first: the `feedback/` and `.claude/feedback/` entries on lines 22-24 may still serve a purpose if those directories exist on disk. Verify: if `.claude/feedback/` directory does not exist AND `feedback/` directory does not exist, remove those gitignore lines. If either exists on disk (even empty), leave the gitignore entries to prevent accidental tracking.

**PROJECT.md Current State (per D-06):**

Replace lines 46-53. Change the heading from `### Current State (Broken)` to `### Current State` and replace the bullet list:

```markdown
### Current State

- pipeline-observe.js (PostToolUse/SubagentStop hook) captures all conversation events to `.claude/logs/observations/<project>/obs.jsonl` with 10MB rotation and 30-day archive purge
- @observer subagent (Sonnet 4.6) reads obs.jsonl incrementally, extracts learnings, classifies via 3-layer scope tests, writes to ## Pending Review sections with confidence tags
- /evolve command dispatches observer then presents pending entries for promote/edit/revert with git-based rollback
- Confidence decay: LOW entries expire after 14 days, MED after 30 days, with consolidation proposals at 200-line cap
- PLAYBOOK.md uses Open/Resolved lifecycle; observer routes cross-agent insights to target agent MEMORY.md files
- agent-protocols rewritten thin (no signals, no project-memories, no scratchpad); agents consume memory passively
```

Leave the `### What Failed (Prior Attempts)` section (lines 55-60) untouched per D-08 -- it's historical narrative.

**settings.local.json:**

This file is untracked (local-only). Remove the stale permission entry on line 7:
```json
"Bash(cp \"D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6/feedback/signals.yaml\" \"D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6/.claude/feedback/\")",
```

Also remove line 4 (mkdir feedback -- dead directory):
```json
"Bash(mkdir -p \"D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-Automation V0.6/.claude/feedback\")",
```

After editing, stage only the tracked files: `git add CLAUDE.md .gitignore .planning/PROJECT.md`

Do NOT stage settings.local.json (it's untracked and gitignored via `.claude/settings.local.json` entry).
Do NOT stage .claude/agent-memory/researcher/MEMORY.md or .claude/skills/pipeline-design/SKILL.md or .planning/config.json or any other modified file shown in git status.
  </action>
  <verify>
    <automated>grep -c "project-memories" CLAUDE.md && grep -c "feedback.*signals" CLAUDE.md && grep -c "PLAYBOOK.md" CLAUDE.md && grep -c "project-memories" .gitignore && grep -c "Current State (Broken)" .planning/PROJECT.md && grep -c "pipeline-observe.js" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "project-memories" CLAUDE.md` returns zero matches
    - `grep "feedback.*signals" CLAUDE.md` returns zero matches
    - `grep "PLAYBOOK.md" CLAUDE.md` returns exactly 1 match
    - `grep "observations/" CLAUDE.md` returns at least 1 match
    - `grep "pipeline-observe.js" CLAUDE.md` returns at least 1 match
    - `grep "project-memories" .gitignore` returns zero matches
    - `grep "Current State (Broken)" .planning/PROJECT.md` returns zero matches
    - `grep "### Current State" .planning/PROJECT.md` returns exactly 1 match
    - `grep "pipeline-observe.js" .planning/PROJECT.md` returns at least 1 match
    - `grep "signals.yaml" .claude/settings.local.json` returns zero matches
    - `git diff --cached --name-only` includes CLAUDE.md, .gitignore, .planning/PROJECT.md (but NOT settings.local.json)
  </acceptance_criteria>
  <done>All 4 live config files updated: CLAUDE.md Folder Map accurate, .gitignore clean, PROJECT.md Current State reflects working system, settings.local.json stale entries removed</done>
</task>

<task type="auto">
  <name>Task 3: Create single cleanup commit</name>
  <read_first>
    - .planning/phases/06-old-memory-cleanup/06-CONTEXT.md (D-03 — single commit intent)
  </read_first>
  <files></files>
  <action>
Per D-03: commit all staged changes (8 deletions + 3 file updates) in a single Phase 6 cleanup commit.

Verify staged changes with `git diff --cached --stat` before committing. Expected:
- 8 deleted files (D lines)
- 3 modified files (CLAUDE.md, .gitignore, .planning/PROJECT.md)

Commit message:
```
cleanup(phase-06): remove old memory system traces and update live docs

Remove 8 deprecated files (obs.js, check-definition-signals.js,
autoresearch skill, old test fixtures). Update CLAUDE.md Folder Map
(remove project-memories/ and feedback/, add PLAYBOOK.md and
observations/). Remove .gitignore dead entries. Rewrite PROJECT.md
Current State to reflect working unified memory system.
```
  </action>
  <verify>
    <automated>git log -1 --oneline | grep "cleanup(phase-06)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` contains "cleanup(phase-06)"
    - `git diff --cached` is empty (nothing left staged after commit)
    - `git show --stat HEAD` lists 11 files (8 deleted + 3 modified)
  </acceptance_criteria>
  <done>Single cleanup commit created with all file deletions and live doc updates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live config docs -> all agents | CLAUDE.md Folder Map is read by every agent at task start; incorrect entries cause agent confusion |
| PROJECT.md -> milestone planners | Outdated Current State section misleads future planning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | git rm of wrong files | mitigate | Task 1 lists all 8 files explicitly by path; verify with `git diff --cached --name-status` before commit; no glob patterns used |
| T-06-02 | I (Information Disclosure) | settings.local.json in commit | mitigate | settings.local.json is untracked and gitignored; task action explicitly says DO NOT stage it |
| T-06-03 | D (Denial of Service) | Accidental staging of unrelated modified files | mitigate | Task 2 stages only 3 named files (`git add CLAUDE.md .gitignore .planning/PROJECT.md`); explicit list of files NOT to stage |
</threat_model>

<verification>
After this plan completes:
1. `git show --stat HEAD` lists exactly 11 files changed (8 D + 3 M)
2. `grep -c "project-memories\|signals.yaml\|feedback/" CLAUDE.md` returns 0
3. `grep "PLAYBOOK.md" CLAUDE.md` returns a match
4. `grep "Current State (Broken)" .planning/PROJECT.md` returns no match
5. `grep "project-memories" .gitignore` returns no match
</verification>

<success_criteria>
- All 8 deprecated files removed from git tracking in a single commit
- CLAUDE.md Folder Map accurately reflects the current directory structure (no dead entries, PLAYBOOK.md and observations/ present)
- .gitignore has no entries for removed directories
- PROJECT.md "Current State" section describes the working unified memory system
- settings.local.json has no stale signals.yaml/feedback permission entries
- No unrelated files accidentally staged or committed
</success_criteria>

<output>
After completion, create `.planning/phases/06-old-memory-cleanup/06-01-SUMMARY.md`
</output>
